var documenterSearchIndex = {"docs":
[{"category":"section","location":"accs/threadsafe/#Thread-safe-accumulation","page":"Thread-safe accumulation","text":"DynamicPPL contains a 'thread-safe model evaluation mode', which can be accessed by calling DynamicPPL.setthreadsafe on a model.\n\nusing DynamicPPL, Distributions\n\n@model function g(y)\n    x ~ Normal()\n    Threads.@threads for i in eachindex(y)\n        y[i] ~ Normal(x)\n    end\nend\ny = [2.0, 3.0, 4.0]\nmodel = setthreadsafe(g(y), true)\n\nThis is accomplished by creating one copy of each accumulator per thread (using DynamicPPL.split), and then after the model evaluation is complete, merging the result of each thread's accumulator with DynamicPPL.combine.\n\nThis means that if you are implementing your own accumulator, you will need to implement the split and combine methods for it in order for it to work correctly in thread-safe mode.\n\nEach accumulator sees only the tilde-statements that were executed on its own thread. However, the intent is that after merging the results from all threads, the final accumulator should be equivalent to what would have been obtained by a single-threaded evaluation (modulo ordering). Because the accumulation process is not always commutative, you may in general end up with a different ordering of results. However, for many accumulators such as log-probability accumulators, this is not an issue.\n\nWe can see this in action if we step through the internal DynamicPPL calls. (Note that calling DynamicPPL.init!! on a model where thread-safe mode has been enabled will automatically perform these steps for you.)\n\nThreads.nthreads()\n\nvi = DynamicPPL.OnlyAccsVarInfo(DynamicPPL.LogLikelihoodAccumulator())\ntsvi = DynamicPPL.ThreadSafeVarInfo(vi)\ntsvi.accs_by_thread\n\n(Here it actually creates a vector of length maxthreadid(). This is slightly hacky, see the warning below and links therein for more discussion.)\n\nx = 1.0\nmodel = setleafcontext(model, DynamicPPL.InitContext(InitFromParams((; x=x)), UnlinkAll()))\n_, tsvi = DynamicPPL._evaluate!!(model, tsvi)\ntsvi.accs_by_thread\n\nIn the above output, the accumulators that have non-zero log-likelihoods are the ones corresponding to the threads that executed tilde-statements.\n\nFinally, to collapse the per-thread accumulators into a single accumulator, we can call getacc. This does the combine step for us.\n\noutput_acc = DynamicPPL.getacc(tsvi, Val(:LogLikelihood))\n\nWe can check whether this is correct:\n\noutput_acc.logp ≈ sum(logpdf.(Normal(x), y))\n\nwarning: Warning\nThe current implementation of thread safety, with one accumulator per thread, is not fully safe since it relies on indexing into a vector with threadid(). See this issue for details. In practice, though, we have not observed any problems with the current approach.There is also a possibility that DynamicPPL may shift to using 'atomic' accumulators in the future, where only one set of accumulators is maintained, but modifications to it must be performed atomically. See this draft PR for details.\n\nIgnoring the caveats above, it can be generally said that any output that is obtained from an accumulator can be accumulated correctly in a thread-safe manner. In other words, full thread safety in DynamicPPL is possible as long as all the outputs you need are obtained from accumulators.\n\nThe main situation where this is not yet true is when using a full VarInfo, which stores a VarNamedTuple in its varinfo.values field. Modifications to this field are currently not thread-safe. However, the values VNT is entirely equivalent to a VectorValueAccumulator.\n\nIn the near future it should hopefully be possible to use a OnlyAccsVarInfo with a VectorValueAccumulator instead of a full VarInfo, which would allow DynamicPPL to be fully thread-safe (though see also this issue for another caveat).\n\n","title":"Thread-safe accumulation"},{"category":"section","location":"ldf/models/#ldf-model","page":"Using LDFs in model evaluation","text":"As mentioned on the previous page, a LogDensityFunction contains more information than just the mapping from vectors to log-densities. It also contains enough information to generate an initialisation strategy, and on top of that it directly wraps a transform strategy.\n\nThis means that, once you have constructed a LogDensityFunction, you can use it in conjunction with a parameter vector to evaluate models using the init!! framework established previously.\n\nLet's start by generating the same LogDensityFunction as on the previous page:\n\nusing DynamicPPL, Distributions\n\n@model function f()\n    x ~ Normal()\n    y ~ Beta(2, 2)\n    return nothing\nend\nmodel = f()\n\nldf = LogDensityFunction(model, getlogjoint_internal, LinkAll())","title":"Using LDFs in model evaluation"},{"category":"section","location":"ldf/models/#Evaluating-models-using-vectorised-parameters","page":"Using LDFs in model evaluation","text":"You can regenerate the initialisation strategy using the constructor InitFromVector(vect, ldf):\n\ninit_strategy = InitFromVector([3.0, 4.0], ldf)\n\nand access the transform strategy via\n\nldf.transform_strategy\n\nWith these two pieces of information, we can add whatever accumulators we like to the mix, and then evaluate using init!! as shown before. For example, let's say we'd like to know what raw values the vector [3.0, 4.0] corresponds to.\n\nnote: Extracting the model\nIf you only have access to ldf and not the original model, you can just extract it via ldf.model. See the LogDensityFunction docstring for more details.\n\naccs = OnlyAccsVarInfo(RawValueAccumulator(false))\n_, accs = init!!(model, accs, init_strategy, ldf.transform_strategy)\nget_raw_values(accs)\n\nUnsurprisingly, the raw (i.e., untransformed) value of x is 3.0, and y is equal to:\n\nusing StatsFuns: logistic\nlogistic(4.0)\n\nNotice that you can include all the necessary log-density accumulators in the above:\n\naccs = OnlyAccsVarInfo(\n    LogPriorAccumulator(), LogLikelihoodAccumulator(), LogJacobianAccumulator()\n)\n_, accs = init!!(model, accs, init_strategy, ldf.transform_strategy)\naccs\n\nand from this you can, in one fell swoop, obtain the log-prior, log-likelihood, and log-Jacobian corresponding to the vector [3.0, 4.0].\n\nIn fact, this is exactly what calling LogDensityProblems.logdensity(ldf, [3.0, 4.0] does, except that it also collapses this information into a single number based on which log-density getter you specified when creating the LogDensityFunction. For example, in the above ldf, we used getlogjoint_internal, which is equal to logprior + loglikelihood - logjacobian:\n\nusing LogDensityProblems\n\nLogDensityProblems.logdensity(ldf, [3.0, 4.0])\n\nThis represents a loss of information since we no longer know the individual components of the log-density. LogDensityFunction itself must do this to obey the LogDensityProblems interface, but as a user of DynamicPPL you need not be limited to it; you can use InitFromVector to obtain all the information you need.\n\nnote: What happened to `unflatten!!`?\nIf you are familiar with older versions of DynamicPPL, you may realise that this workflow is similar to the old version of calling unflatten(varinfo, vector), and then re-evaluating the model.unflatten!! still exists (albeit with a double exclamation mark now), and it still does the same thing as before, but we strongly recommend against using it. The reason is because unflatten!! leaves the VarInfo in an inconsistent state, since the parameters are updated but the log-density and transformations are not. Using this invalid VarInfo without reevaluating the model can lead to incorrect results.It is much safer to use InitFromVector, which encapsulates all the information needed to rerun the model, but does not pretend to also contain other information that is not actually updated.","title":"Evaluating models using vectorised parameters"},{"category":"section","location":"ldf/models/#Obtaining-new-vectorised-parameters","page":"Using LDFs in model evaluation","text":"We can also do the reverse process here, which is to obtain new vectorised parameters that are consistent with this LogDensityFunction.\n\nSuppose that we want to evaluate the LogDensityFunction at the new raw values x = 5.0 and y = 0.6. The corresponding vectorised parameters should be\n\nusing StatsFuns: logit\n[5.0, logit(0.6)]\n\n(Of course, in general you can't just write these down by hand, since it will depend on the model.)\n\nNaturally, we need to re-evaluate the model with the initialisation strategy we are interested in. We can use a special accumulator, VectorParamAccumulator, to get the newly vectorised set of parameters. This accumulator must take the LogDensityFunction as an argument, since it uses the information stored in there to ensure that the values it collects are consistent with the LogDensityFunction.\n\nNote that we must use the same transform strategy as the one contained in the LogDensityFunction, or else we will be generating vectorised parameters that are inconsistent with the LogDensityFunction (an error will be thrown if that happens).\n\ninit_strategy = InitFromParams(VarNamedTuple(; x=5.0, y=0.6))\ntransform_strategy = ldf.transform_strategy\n\naccs = OnlyAccsVarInfo(VectorParamAccumulator(ldf))\n_, accs = init!!(model, accs, init_strategy, transform_strategy)\nvec = get_vector_params(accs)\n\nYou can of course also bundle any extra accumulators you like into the above if you are interested not only in the vectorised parameters but also (e.g.) the log-density. This allows you to obtain all the information you need with only one model evaluation.\n\nnote: Note\nNote that VectorParamAccumulator is not the same as VectorValueAccumulator. The former collects a single vector of parameters, whereas the latter collects a VarNamedTuple of vectorised parameters for each variable.If you used the latter instead, e.g.,accs = OnlyAccsVarInfo(VectorValueAccumulator())\n_, accs = init!!(model, accs, init_strategy, transform_strategy)\nvec_vals = get_vector_values(accs)you can convert vec_vals::VarNamedTuple to a single vector usingto_vector_params(vec_vals, ldf)which is equivalent. However, this is slower since it has to generate an intermediate VarNamedTuple.\n\nIf you only need vectorised parameters, and not other information such as log-densities, then you can use rand() on a LogDensityFunction to directly get this:\n\nusing Random: Xoshiro\n\nrand(Xoshiro(468), ldf)\n\nnote: What happened to `varinfo[:]`?\nJust like before, if you are familiar with older versions of DynamicPPL, you may realise that this workflow is similar to the old version of calling varinfo[:] to obtain a set of vectorised parameters.This still exists (although we prefer that you use internal_values_as_vector(varinfo) instead, since that name is more descriptive). Although internal_values_as_vector is not as unsafe as unflatten!!, it still does not perform any checks to ensure that the vectorised parameters are consistent with the LogDensityFunction. For example, you could extract a length-2 vector of parameters from a VarInfo, and it would work with any LogDensityFunction that also expects a length-2 vector, even if the actual transformations and model are completely different.The approach above based on VectorParamAccumulator does preserve the necessary information, and to_vector_params will carry out the necessary checks to ensure correctness. It is no less performant than before (apart from the time needed to run said checks!).\n\n","title":"Obtaining new vectorised parameters"},{"category":"section","location":"accs/values/#Storing-vectorised-vs.-raw-values","page":"Storing vectorised vs. raw values","text":"warning: Warning\nThis page describes design decisions in DynamicPPL, and is primarily intended for developers of DynamicPPL. If you are a user of DynamicPPL, you do not need to understand this in detail to use DynamicPPL effectively.\n\nIn the Existing accumulators page, we mentioned that there are two accumulators that store values, namely RawValueAccumulator and VectorValueAccumulator. This page attempts to explain the rationale behind this, when each of these needs to be used, and how we could potentially remove the need for VectorValueAccumulator in the future.\n\nBefore we go into this, we need to start by talking about VarInfo, which we have not covered at all yet.","title":"Storing vectorised vs. raw values"},{"category":"section","location":"accs/values/#The-role-of-VarInfo","page":"Storing vectorised vs. raw values","text":"As described in the model evaluation documentation page, each tilde-statement is split up into three parts:\n\nInitialisation;\nTransformation; and\nAccumulation.\n\nUnfortunately, not everything in DynamicPPL follows this clean structure yet. In particular, there is a struct, called VarInfo:\n\nstruct VarInfo{Tfm<:AbstractTransformStrategy,V<:VarNamedTuple,A<:AccumulatorTuple}\n    transform_strategy::Tfm\n    values::V\n    accs::A\nend\n\nThe values field stores DynamicPPL.TransformedValues, but it is mandatory that these transformed values are vectorised. That is, it is permissible to store (for example) TransformedValue([1.0], Unlink()), but not TransformedValue(1.0, NoTransform()).\n\nFurthermore, the transform_strategy field stores an AbstractTransformStrategy which is (as far as possible) consistent with the type of values stored in values.\n\nHere is an example:\n\nusing DynamicPPL, Distributions\n\n@model function dirichlet()\n    x = zeros(3)\n    return x[1:3] ~ Dirichlet(ones(3))\nend\ndirichlet_model = dirichlet()\nvi = VarInfo(dirichlet_model)\nvi\n\nIn vi.values, it is mandatory to store TransformedValues as ArrayLikeBlocks (see the Array-like blocks documentation for information on this). The reason is because, if the value is linked, it may have a different size than the number of indices in the VarName. This means that when retrieving the keys, we obtain each block as a single key:\n\nkeys(vi.values)","title":"The role of VarInfo"},{"category":"section","location":"accs/values/#Towards-a-new-framework","page":"Storing vectorised vs. raw values","text":"In a VarInfo, the accs field is responsible for the accumulation step, just like an ordinary AccumulatorTuple.\n\nHowever, values serves three purposes in one:\n\nit is sometimes used for initialisation (when the model's leaf context is DefaultContext, the TransformedValue to be used in the transformation step is read from it)\nit also determines whether the log-Jacobian term should be included or not (by virtue of specifying a transformation)\nit is sometimes also used for accumulation (when evaluating a model with a VarInfo, we will potentially store a new TransformedValue in it!).\n\nThe path to removing VarInfo is essentially to separate these three roles:\n\nThe initialisation role of varinfo.values can be taken over by an initialisation strategy that wraps it. Recall that the only role of an initialisation strategy is to provide an TransformedValue via DynamicPPL.init. This can be trivially done by indexing into the VarNamedTuple stored in the strategy.\nWhether the log-Jacobian term should be included or not can be determined by a transform strategy. Much like how we can have an initialisation strategy that takes values from a VarInfo, we can also have a transform strategy that is defined by the existing status of a VarInfo. This is implemented in the DynamicPPL.get_link_strategy(::AbstractVarInfo) function.\nThe accumulation role of varinfo.values can be taken over by a new accumulator, which we call VectorValueAccumulator. This name is chosen because it does not store generic TransformedValues, but only vectorised ones, i.e., TransformedValue{V} where V <: AbstractVector. VectorValueAccumulator is implemented inside src/accs/vector_value.jl.\n\nnote: Note\nDecoupling all of these components also means that we can mix and match different initialisation strategies, link strategies, and accumulators more easily.For example, previously, to create a linked VarInfo, you would need to first generate an unlinked VarInfo and then link it. Now, you can directly create a linked VarInfo by initialising with InitFromPrior() and LinkAll().","title":"Towards a new framework"},{"category":"section","location":"accs/values/#RawValueAccumulator","page":"Storing vectorised vs. raw values","text":"Earlier we said that VectorValueAccumulator stores only values that have been vectorised. One might therefore ask about unvectorised values — and in particular, values that have not been transformed at all, i.e., TransformedValue(val, NoTransform()).\n\nIt turns out that it is very often useful to store such untransformed values. Additionally, since the values must always correspond exactly to the indices they are assigned to, we can unwrap them and do not need to store them as array-like blocks!\n\nThis is the role of RawValueAccumulator.\n\noavi = DynamicPPL.OnlyAccsVarInfo(DynamicPPL.RawValueAccumulator(false))\n_, oavi = DynamicPPL.init!!(dirichlet_model, oavi, InitFromPrior(), UnlinkAll())\nraw_vals = get_raw_values(oavi)\n\nNote that when we unwrap TransformedValues, we also lose the block structure that was present in the model. That means that in RawValueAccumulator, there is no longer any notion that x[1:3] was set together, so the keys correspond to the individual indices.\n\nkeys(raw_vals)\n\nIn particular, the outputs of RawValueAccumulator are used for chain construction. This is why indices of keys like x[1:3] ~ dist end up being split up in chains.\n\nnote: Note\nIf you have an entire vector belonging to a top-level symbol, e.g. x ~ Dirichlet(ones(3)), it will not be broken up (as long as you use FlexiChains).","title":"RawValueAccumulator"},{"category":"section","location":"accs/values/#Why-do-we-still-need-to-store-TransformedValues?","page":"Storing vectorised vs. raw values","text":"Given that RawValueAccumulator exists, one may wonder why we still need to store the other TransformedValues at all, i.e. what the purpose of VectorValueAccumulator is.\n\nCurrently, the only remaining reason for transformed values is the fact that we may sometimes need to perform DynamicPPL.unflatten!! on a VarInfo, to insert new values into it from a vector.\n\nvi = VarInfo(dirichlet_model)\nDynamicPPL.getindex_internal(vi, @varname(x[1:3]))\n\nvi = DynamicPPL.unflatten!!(vi, [0.2, 0.5, 0.3])\nDynamicPPL.getindex_internal(vi, @varname(x[1:3]))\n\nIf we do not store the vectorised form of the values, we will not know how many values to read from the input vector for each key.\n\nRemoving upstream usage of unflatten!! would allow us to completely get rid of TransformedValueAccumulator and only ever use RawValueAccumulator. See this DynamicPPL issue for more information.\n\nOne possibility for removing unflatten!! is to turn it into a function that, instead of generating a new VarInfo, instead generates a tuple of new initialisation and transform strategies similar to InitFromVector. This would be conceptually very similar to how LogDensityFunction currently works.\n\n","title":"Why do we still need to store TransformedValues?"},{"category":"section","location":"init/#init-strategies","page":"Initialisation strategies","text":"In DynamicPPL, initialisation strategies are used to determine the parameters used to evaluate a model. Every time an assume tilde-statement is seen (i.e., a random variable), the initialisation strategy is used to generate a value for that variable.\n\nnote: Note\nOne might perhaps more appropriately call them parameter generation strategies. Even the name initialisation is a bit of a historical misnomer (the original intent was that they would be used to populate an empty VarInfo with some values). However, over time, it has become clear that these are general enough to describe essentially any way of choosing parameters to evaluate a model with.\n\nDynamicPPL provides four initialisation strategies out of the box. For many purposes you should be able to get away with only using these.\n\nInitFromPrior: samples from the prior distribution.\nInitFromParams: reads from a set of given parameters. The parameters may be supplied in many different forms, but a VarNamedTuple is the most robust.\nInitFromUniform: samples from a uniform distribution in linked space.\nInitFromVector: reads from a set of vectorised parameters. This also needs a LogDensityFunction to provide the necessary information about how the vectorised parameters map to the model variables.\n\nHowever, sometimes you will need to implement your own initialisation strategy. The subsequent sections will demonstrate how this can be done.","title":"Initialisation strategies"},{"category":"section","location":"init/#The-required-interface","page":"Initialisation strategies","text":"Each initialisation strategy must subtype AbstractInitStrategy, and implement DynamicPPL.init(rng, vn, dist, strategy), which must return a TransformedValue.","title":"The required interface"},{"category":"section","location":"init/#An-example","page":"Initialisation strategies","text":"Consider the following model:\n\nusing DynamicPPL, Distributions, Random\n\n@model function f()\n    x ~ Normal()\n    return x\nend\nmodel = f()\n\nSuppose we are writing a Metropolis–Hastings sampler, and we want to perform a random walk where the next proposed value of x depends on the previous value of x. Given a previous value x_prev we can define a custom initialisation strategy as follows:\n\nstruct InitRandomWalk <: DynamicPPL.AbstractInitStrategy\n    x_prev::Float64\n    step_size::Float64\nend\n\nfunction DynamicPPL.init(rng, vn::VarName, ::Distribution, strategy::InitRandomWalk)\n    new_x = rand(rng, Normal(strategy.x_prev, strategy.step_size))\n    # Insert some printing to see when this is called.\n    @info \"init() is returning: $new_x\"\n    return DynamicPPL.TransformedValue(new_x, DynamicPPL.NoTransform())\nend\n\nGiven a previous value of x\n\nx_prev = 4.0\nnothing # hide\n\nwe can then make a proposal for x as follows:\n\nnew_x, new_vi = DynamicPPL.init!!(\n    model, VarInfo(), InitRandomWalk(x_prev, 0.5), UnlinkAll()\n)\nnothing # hide\n\nWhen evaluating the model, the value for x will be exactly that new value we proposed. We can see this from the return value:\n\nnew_x\n\nFurthermore, we can read off the associated log-probability from the newly returned VarInfo:\n\nDynamicPPL.getlogjoint(new_vi) ≈ logpdf(Normal(), new_x)\n\nFrom this log-probability, we can compute the acceptance ratio for the Metropolis–Hastings step, and thereby create a valid MCMC sampler.\n\nIn this case, we have defined an initialisation strategy that is random (and thus uses the rng argument for reproducibility). However, initialisation strategies can also be fully deterministic, in which case the rng argument is not needed. For example, DynamicPPL.InitFromParams reads from a set of parameters which are known ahead of time.","title":"An example"},{"category":"section","location":"init/#The-returned-TransformedValue","page":"Initialisation strategies","text":"As mentioned above, the init function must return a TransformedValue. The transform stored inside this does not affect the result of the model evaluation, but it may have performance implications. In particular, the returned subtype does not determine whether the log-Jacobian term is accumulated or not: that is determined by a separate transform strategy.\n\nWhat this means is that initialisation strategies should always choose the laziest possible version of TransformedValue, electing to do as few transformations as possible inside init.\n\nFor example, in the above example, we simply wrapped the untransformed value in TransformedValue(..., NoTransform()), which is the simplest possible choice. If a linked value is required by a later step inside tilde_assume!! (either the transformation or accumulation steps), it is the responsibility of that step to perform the linking.\n\nConversely, DynamicPPL.InitFromUniform samples inside linked space. Instead of performing the inverse link transform eagerly, it directly returns a TransformedValue(val, DynamicLink()), where val is already the linked vector: this means that if a linked value is required by a later step, it is not necessary to link it again. Even if no linked value is required, this lazy approach does not hurt performance, as it just defers the inverse linking to the later step.\n\nIn both cases, only one linking operation is performed (at most).\n\n","title":"The returned TransformedValue"},{"category":"type","location":"init/#DynamicPPL.InitFromPrior","page":"Initialisation strategies","text":"InitFromPrior()\n\nObtain new values by sampling from the prior distribution.\n\n\n\n\n\n","title":"DynamicPPL.InitFromPrior"},{"category":"type","location":"init/#DynamicPPL.InitFromParams","page":"Initialisation strategies","text":"InitFromParams(\n    params::Any,\n    fallback::Union{AbstractInitStrategy,Nothing}=InitFromPrior()\n)\n\nObtain new values by extracting them from the given set of params.\n\nThe most common use case is to provide a VarNamedTuple, which provides a mapping from variable names to values. However, we leave the type of params open in order to allow for custom parameter storage types.\n\nDynamicPPL also provides extra implementations for params::NamedTuple and params::AbstractDict{<:VarName}, which both convert the input to a VarNamedTuple internally.\n\nCustom parameter storage types\n\nFor InitFromParams to work correctly with a custom params::P, you need to implement\n\nDynamicPPL.init(rng, vn::VarName, dist::Distribution, p::InitFromParams{P}) where {P}\n\nThis tells you how to obtain values for the random variable vn from p.params. Note that the last argument is InitFromParams(params), not just params itself. Please see the docstring of DynamicPPL.init for more information on the expected behaviour.\n\nIn some cases (specifically, when you expect that the type of log-probabilities will need to be expanded: the most common example is when running AD with ForwardDiff.jl), you may also need to implement:\n\nDynamicPPL.get_param_eltype(p::InitFromParams{P}) where {P}\n\nSee the docstring of DynamicPPL.get_param_eltype for more information on when this is needed.\n\nThe argument fallback specifies how new values are to be obtained if they cannot be found in params, or they are specified as missing. fallback can either be an initialisation strategy itself, in which case it will be used to obtain new values, or it can be nothing, in which case an error will be thrown. The default for fallback is InitFromPrior().\n\n\n\n\n\n","title":"DynamicPPL.InitFromParams"},{"category":"method","location":"init/#DynamicPPL.InitFromParams-Tuple{ParamsWithStats, Union{Nothing, AbstractInitStrategy}}","page":"Initialisation strategies","text":"InitFromParams(\n    ps::ParamsWithStats,\n    fallback::Union{Nothing,AbstractInitStrategy}=InitFromPrior()\n)\n\nInitialise a model using the parameters stored in ps. The stats are ignored. fallback is used if the model requires the value of a parameter which is not present in ps.params.\n\n\n\n\n\n","title":"DynamicPPL.InitFromParams"},{"category":"type","location":"init/#DynamicPPL.InitFromUniform","page":"Initialisation strategies","text":"InitFromUniform()\nInitFromUniform(lower, upper)\n\nObtain new values by first transforming the distribution of the random variable to unconstrained space, then sampling a value uniformly between lower and upper.\n\nIf lower and upper are unspecified, they default to (-2, 2), which mimics Stan's default initialisation strategy (see the Stan reference manual page on initialisation for more details).\n\nRequires that lower <= upper.\n\n\n\n\n\n","title":"DynamicPPL.InitFromUniform"},{"category":"type","location":"init/#DynamicPPL.InitFromVector","page":"Initialisation strategies","text":"InitFromVector(\n    vect::AbstractVector{<:Real},\n    varname_ranges::VarNamedTuple,\n    transform_strategy::AbstractTransformStrategy\n) <: AbstractInitStrategy\n\nwarning: Warning\nThis constructor is only meant for internal use. Please use InitFromVector(vect, ldf::LogDensityFunction) instead, which will automatically construct the varname_ranges and transform_strategy arguments for you.\n\nA struct that wraps a vector of parameter values, plus information about how random variables map to ranges in that vector.\n\nThe transform_strategy argument in fact duplicates information stored inside varname_ranges. For example, if every RangeAndTransform in varname_ranges has transform == DynamicLink(), then transform_strategy will be LinkAll().\n\nHowever, storing transform_strategy here is a way to communicate at the type level whether all variables are linked or unlinked, which provides much better performance in the case where all variables are linked or unlinked, due to improved type stability.\n\n\n\n\n\n","title":"DynamicPPL.InitFromVector"},{"category":"method","location":"init/#DynamicPPL.InitFromVector-Tuple{AbstractVector{<:Real}, LogDensityFunction}","page":"Initialisation strategies","text":"InitFromVector(\n    vect::AbstractVector{<:Real},\n    ldf::LogDensityFunction\n)\n\nConstructor for InitFromVector that extracts the necessary information about VarName ranges and transform strategy from a pre-existing LogDensityFunction.\n\n\n\n\n\n","title":"DynamicPPL.InitFromVector"},{"category":"method","location":"init/#DynamicPPL.InitFromVector-Tuple{MarginalLogDensity{<:DynamicPPLMarginalLogDensitiesExt.LogDensityFunctionWrapper}, Union{Nothing, AbstractVector}}","page":"Initialisation strategies","text":"DynamicPPL.InitFromVector(\n    mld::MarginalLogDensities.MarginalLogDensity{<:LogDensityFunctionWrapper},\n    unmarginalized_params::Union{AbstractVector,Nothing}=nothing,\n)\n\nCreate a new initialisation strategy using the parameter values stored in mld (and optionally unmarginalized_params).\n\nIf a Laplace approximation was used for the marginalisation, the values of the marginalized parameters are set to their mode (note that this only happens if the mld object has been used to compute the marginal log-density at least once, so that the mode has been computed).\n\nIf a vector of unmarginalized_params is specified, the values for the corresponding parameters will also be available as part of the initialisation strategy. This vector may be obtained e.g. by performing an optimization of the marginal log-density.\n\nTo use this initialisation strategy to obtain e.g. updated log-probabilities, you should re-evaluate the model with the values inside the returned VarInfo, for example using:\n\ninit_strategy = DynamicPPL.InitFromVector(mld, unmarginalized_params)\naccs = DynamicPPL.OnlyAccsVarInfo((\n    DynamicPPL.LogPriorAccumulator(),\n    DynamicPPL.LogLikelihoodAccumulator(),\n    DynamicPPL.RawValueAccumulator(false),\n    # ... whatever else you need\n))\n_, accs = DynamicPPL.init!!(rng, model, oavi, init_strategy, DynamicPPL.UnlinkAll())\n\nYou can then extract all the updated data from accs using DynamicPPL's existing API (see the DynamicPPL documentation for more details).\n\nExample\n\njulia> using DynamicPPL, Distributions, MarginalLogDensities\n\njulia> @model function demo()\n           x ~ Normal()\n           y ~ Beta(2, 2)\n       end\ndemo (generic function with 2 methods)\n\njulia> # Note that by default `marginalize` uses a linked VarInfo.\n       mld = marginalize(demo(), [@varname(x)]);\n\njulia> using MarginalLogDensities: Optimization, OptimizationOptimJL\n\njulia> # Find the mode of the marginal log-density of `y`, with an initial point of `y0`.\n       y0 = 2.0; opt_problem = Optimization.OptimizationProblem(mld, [y0])\nOptimizationProblem. In-place: true\nu0: 1-element Vector{Float64}:\n 2.0\n\njulia> # This tells us the optimal (linked) value of `y` is around 0.\n       opt_solution = Optimization.solve(opt_problem, OptimizationOptimJL.NelderMead())\nretcode: Success\nu: 1-element Vector{Float64}:\n 4.88281250001733e-5\n\njulia> # Get the an initialisation strategy representing the mode of `y`.\n       init_strategy = InitFromVector(mld, opt_solution.u);\n\njulia> # Evaluate the model with this initialisation strategy.\n       accs = DynamicPPL.OnlyAccsVarInfo((\n           DynamicPPL.LogPriorAccumulator(),\n           DynamicPPL.LogLikelihoodAccumulator(),\n           DynamicPPL.RawValueAccumulator(false),\n       ));\n       _, accs = DynamicPPL.init!!(demo(), accs, init_strategy, DynamicPPL.UnlinkAll());\n\njulia> # Extract the raw (i.e. untransformed) values for all variables.\n       # `x` is set to its mode (which for `Normal()` is zero).\n       # Furthermore, `y` is set to the optimal value we found above.\n       vals = DynamicPPL.get_raw_values(accs)\nVarNamedTuple\n├─ x => 0.0\n└─ y => 0.5000122070312476\n\n\n\n\n\n","title":"DynamicPPL.InitFromVector"},{"category":"type","location":"init/#DynamicPPL.AbstractInitStrategy-init","page":"Initialisation strategies","text":"AbstractInitStrategy\n\nAbstract type representing the possible ways of initialising new values for the random variables in a model (e.g., when creating a new VarInfo).\n\nAny subtype of AbstractInitStrategy must implement the DynamicPPL.init method, and in some cases, DynamicPPL.get_param_eltype (see its docstring for details).\n\n\n\n\n\n","title":"DynamicPPL.AbstractInitStrategy"},{"category":"function","location":"init/#DynamicPPL.init-init","page":"Initialisation strategies","text":"init(rng::Random.AbstractRNG, vn::VarName, dist::Distribution, strategy::AbstractInitStrategy)\n\nGenerate a new value for a random variable with the given distribution.\n\nThis function must return a TransformedValue.\n\nIf strategy provides values that are already untransformed (e.g., a Float64 within (0, 1) for dist::Beta, then you should return a TransformedValue with a NoTransform().\n\nOtherwise, often there are cases where this will return a transformed value; for example, if the strategy is reading from an existing VarInfo.\n\n\n\n\n\n","title":"DynamicPPL.init"},{"category":"type","location":"init/#DynamicPPL.TransformedValue-init","page":"Initialisation strategies","text":"TransformedValue{V,T<:AbstractTransform}\n\nA struct to represent a value that has undergone some transformation.\n\nThe transformed value is stored in the value field, and the inverse transformation is stored in the transform field. \n\nThat means that get_transform(tv)(get_internal_value(tv)) should return the raw, untransformed, value associated with tv.\n\n\n\n\n\n","title":"DynamicPPL.TransformedValue"},{"category":"section","location":"accs/existing/#existing-accumulators","page":"Existing accumulators in DynamicPPL","text":"DynamicPPL provides a number of built-in accumulators which can be used as-is; they cover many of the typical use-cases for accumulators.","title":"Existing accumulators in DynamicPPL"},{"category":"section","location":"accs/existing/#Log-probability-accumulators","page":"Existing accumulators in DynamicPPL","text":"There are various convenience functions in DynamicPPL to combine the outputs from these accumulators:","title":"Log-probability accumulators"},{"category":"section","location":"accs/existing/#Storing-values","page":"Existing accumulators in DynamicPPL","text":"and their associated convenience functions:","title":"Storing values"},{"category":"section","location":"accs/existing/#Miscellaneous","page":"Existing accumulators in DynamicPPL","text":"","title":"Miscellaneous"},{"category":"type","location":"accs/existing/#DynamicPPL.LogPriorAccumulator","page":"Existing accumulators in DynamicPPL","text":"LogPriorAccumulator{T<:Real} <: LogProbAccumulator{T}\n\nAn accumulator that tracks the cumulative log prior during model execution.\n\nNote that the log prior stored in here is always calculated based on unlinked parameters, i.e., the value of logp is independent of whether tha VarInfo is linked or not.\n\nFields\n\nlogp::Real: the scalar log prior value\n\n\n\n\n\n","title":"DynamicPPL.LogPriorAccumulator"},{"category":"type","location":"accs/existing/#DynamicPPL.LogLikelihoodAccumulator","page":"Existing accumulators in DynamicPPL","text":"LogLikelihoodAccumulator{T<:Real} <: LogProbAccumulator{T}\n\nAn accumulator that tracks the cumulative log likelihood during model execution.\n\nFields\n\nlogp::Real: the scalar log likelihood value\n\n\n\n\n\n","title":"DynamicPPL.LogLikelihoodAccumulator"},{"category":"type","location":"accs/existing/#DynamicPPL.LogJacobianAccumulator","page":"Existing accumulators in DynamicPPL","text":"LogJacobianAccumulator{T<:Real} <: LogProbAccumulator{T}\n\nAn accumulator that tracks the cumulative log Jacobian (technically, log(abs(det(J)))) during model execution. Specifically, J refers to the Jacobian of the link transform, i.e., from the space of the original distribution to unconstrained space.\n\nnote: Note\nThis accumulator is only incremented if the variable is transformed by a link function, i.e., if the VarInfo is linked (for the particular variable that is currently being accumulated). If the variable is not linked, the log Jacobian term will be 0.In general, for the forward Jacobian mathbfJ corresponding to the function mathbfy = f(mathbfx),log(q(mathbfy)) = log(p(mathbfx)) - log (mathbfJ)and correspondingly:getlogjoint_internal(vi) = getlogjoint(vi) - getlogjac(vi)\n\nFields\n\nlogjac::Real: the logabsdet of the link transform Jacobian\n\n\n\n\n\n","title":"DynamicPPL.LogJacobianAccumulator"},{"category":"function","location":"accs/existing/#DynamicPPL.getlogjoint-accs-existing","page":"Existing accumulators in DynamicPPL","text":"getlogjoint(vi::AbstractVarInfo)\n\nReturn the log of the joint probability of the observed data and parameters in vi.\n\nSee also: getlogprior, getloglikelihood.\n\n\n\n\n\n","title":"DynamicPPL.getlogjoint"},{"category":"function","location":"accs/existing/#DynamicPPL.getlogjoint_internal-accs-existing","page":"Existing accumulators in DynamicPPL","text":"getlogjoint_internal(vi::AbstractVarInfo)\n\nReturn the log of the joint probability of the observed data and parameters as they are stored internally in vi, including the log-Jacobian for any linked parameters.\n\nIn general, we have that:\n\ngetlogjoint_internal(vi) == getlogjoint(vi) - getlogjac(vi)\n\n\n\n\n\n","title":"DynamicPPL.getlogjoint_internal"},{"category":"function","location":"accs/existing/#DynamicPPL.getlogprior-accs-existing","page":"Existing accumulators in DynamicPPL","text":"getlogprior(vi::AbstractVarInfo)\n\nReturn the log of the prior probability of the parameters in vi.\n\nSee also: getlogjoint, getloglikelihood, setlogprior!!.\n\n\n\n\n\n","title":"DynamicPPL.getlogprior"},{"category":"function","location":"accs/existing/#DynamicPPL.getlogprior_internal-accs-existing","page":"Existing accumulators in DynamicPPL","text":"getlogprior_internal(vi::AbstractVarInfo)\n\nReturn the log of the prior probability of the parameters as stored internally in vi. This includes the log-Jacobian for any linked parameters.\n\nIn general, we have that:\n\ngetlogprior_internal(vi) == getlogprior(vi) - getlogjac(vi)\n\n\n\n\n\n","title":"DynamicPPL.getlogprior_internal"},{"category":"function","location":"accs/existing/#DynamicPPL.getloglikelihood-accs-existing","page":"Existing accumulators in DynamicPPL","text":"getloglikelihood(vi::AbstractVarInfo)\n\nReturn the log of the likelihood probability of the observed data in vi.\n\nSee also: getlogjoint, getlogprior, setloglikelihood!!.\n\n\n\n\n\n","title":"DynamicPPL.getloglikelihood"},{"category":"function","location":"accs/existing/#DynamicPPL.getlogjac-accs-existing","page":"Existing accumulators in DynamicPPL","text":"getlogjac(vi::AbstractVarInfo)\n\nReturn the accumulated log-Jacobian term for any linked parameters in vi. The Jacobian here is taken with respect to the forward (link) transform.\n\nSee also: setlogjac!!.\n\n\n\n\n\n","title":"DynamicPPL.getlogjac"},{"category":"function","location":"accs/existing/#DynamicPPL.RawValueAccumulator","page":"Existing accumulators in DynamicPPL","text":"RawValueAccumulator(include_colon_eq)::Bool <: AbstractAccumulator\n\nAn accumulator that keeps tracks of the model parameters exactly as they are seen in the model.\n\nThe parameter include_colon_eq controls whether variables on the LHS of := are also included in the accumulator. If true, then these variables will be included; if false, they will not be included.\n\n\n\n\n\n","title":"DynamicPPL.RawValueAccumulator"},{"category":"function","location":"accs/existing/#DynamicPPL.VectorValueAccumulator","page":"Existing accumulators in DynamicPPL","text":"VectorValueAccumulator()\n\nAn accumulator that collects vectorised values, i.e. TransformedValue{<:AbstractVector}.\n\nThe exact type of the vectorised value (i.e., tval.transform) will depend on the transform strategy that the model was evaluated with, and specifically, is equal to target_transform(transform_strategy, vn); except for the case where target_transform is Untransformed, i.e., no transformation is to be applied. In this case, the VectorValueAccumulator will apply a vectorisation transform to the untransformed value, i.e., generate a TransformedValue with Unlink() as the transform.\n\n\n\n\n\n","title":"DynamicPPL.VectorValueAccumulator"},{"category":"type","location":"accs/existing/#DynamicPPL.VectorParamAccumulator","page":"Existing accumulators in DynamicPPL","text":"VectorParamAccumulator(ldf::DynamicPPL.LogDensityFunction)\n\nAn accumulator for collecting vectorised parameters from a model evaluation. The resulting vector will be consistent with the LogDensityFunction used to construct the accumulator, both in terms of whether parameters are transformed or not, as well as the order of the parameters in the vector.\n\nThis accumulator allows you to re-evaluate a LogDensityFunction with a different initialisation strategy and collect the vectorised parameters corresponding to that strategy.\n\n\n\n\n\n","title":"DynamicPPL.VectorParamAccumulator"},{"category":"function","location":"accs/existing/#DynamicPPL.get_raw_values","page":"Existing accumulators in DynamicPPL","text":"get_raw_values(vi::AbstractVarInfo)\n\nExtract a VarNamedTuple of values from the RawValueAccumulator in vi. This is the 'raw' values as they are seen in the model, without any transformations applied to them.\n\nIf vi does not contain a RawValueAccumulator, this function will throw an error.\n\n\n\n\n\n","title":"DynamicPPL.get_raw_values"},{"category":"function","location":"accs/existing/#DynamicPPL.get_vector_values","page":"Existing accumulators in DynamicPPL","text":"get_vector_values(accs::OnlyAccsVarInfo)\n\nGet a VarNamedTuple containing vectorised values from accs. This will error if accs does not contain a VectorValueAccumulator.\n\nNote that this function is implemented for OnlyAccsVarInfo, but not VarInfo since that could be ambiguous (VarInfo stores its own vectorised values!). If you want to extract the vectorised values from varinfo.values where varinfo isa VarInfo, you should use DynamicPPL.internal_values_as_vector(varinfo).\n\n\n\n\n\n","title":"DynamicPPL.get_vector_values"},{"category":"function","location":"accs/existing/#DynamicPPL.get_vector_params","page":"Existing accumulators in DynamicPPL","text":"get_vector_params(vi::DynamicPPL.AbstractVarInfo)\n\nExtract the vectorised parameters from the VectorParamAccumulator stored in the AbstractVarInfo. If there is no VectorParamAccumulator in the AbstractVarInfo, or if some indices in the output vector were not set, an error will be thrown.\n\n\n\n\n\n","title":"DynamicPPL.get_vector_params"},{"category":"function","location":"accs/existing/#DynamicPPL.PriorDistributionAccumulator","page":"Existing accumulators in DynamicPPL","text":"PriorDistributionAccumulator()\n\nAn accumulator that stores the prior distributions of every variable seen in a model.\n\n\n\n\n\n","title":"DynamicPPL.PriorDistributionAccumulator"},{"category":"function","location":"accs/existing/#DynamicPPL.get_priors","page":"Existing accumulators in DynamicPPL","text":"get_priors(vi::DynamicPPL.AbstractVarInfo)\n\nExtract the priors stored in the PriorDistributionAccumulator of an AbstractVarInfo. Errors if the AbstractVarInfo does not have a PriorDistributionAccumulator.\n\n\n\n\n\n","title":"DynamicPPL.get_priors"},{"category":"function","location":"accs/existing/#DynamicPPL.FixedTransformAccumulator","page":"Existing accumulators in DynamicPPL","text":"FixedTransformAccumulator()\n\nAn accumulator that calculates and stores the 'fixed' transforms for all variables in a model.\n\nNormally, when running a model with a transform strategy such as LinkAll, the transforms are calculated during model execution and not cached. This ensures that the transforms are up-to-date with the current variable values, which can matter in cases such as\n\nx ~ Normal()\ny ~ truncated(Normal(); lower=x)\n\nor\n\nx ~ Normal()\ny ~ (x > 0 ? Normal() : Exponential())\n\nwhere the transforms for y depend on the value of x.\n\n\n\n\n\n","title":"DynamicPPL.FixedTransformAccumulator"},{"category":"function","location":"accs/existing/#DynamicPPL.get_fixed_transforms","page":"Existing accumulators in DynamicPPL","text":"get_fixed_transforms(vi::DynamicPPL.AbstractVarInfo)\n\nExtract the transforms stored in the FixedTransformAccumulator of an AbstractVarInfo. Errors if the AbstractVarInfo does not have a FixedTransformAccumulator.\n\n\n\n\n\nget_fixed_transforms(\n    model::DynamicPPL.Model,\n    transform_strategy::AbstractTransformStrategy\n)\n\nExtract the fixed transforms for all variables in a model by running the model with the given transform strategy.\n\nNote that, even though this method evaluates the model once, this method does not accept an RNG argument to control that evaluation. This is because the fixed transforms are supposed to be fixed, i.e., they should not depend on random choices made during model execution!\n\nIf you are unsure about whether the transforms for your model are fixed, you can use DynamicPPL.DebugUtils.has_static_constraints. Note though that this relies on executing the model multiple times and is thus not foolproof (it may be that your transforms just happen to be the same each time).\n\n\n\n\n\n","title":"DynamicPPL.get_fixed_transforms"},{"category":"section","location":"conditionfix/#Conditioning-and-fixing","page":"Conditioning and fixing","text":"DynamicPPL allows you to first define a model, and then modify it by either conditioning on observed data, or fixing variables to specific values. This is useful for defining models once and then using them in different ways.\n\nAs an example, one could define a linear regression model as follows:\n\nusing DynamicPPL, Distributions\n\n@model function linear_regression(x)\n    m ~ Normal(0, 1)\n    c ~ Normal(0, 1)\n\n    y = Vector{Float64}(undef, length(x))\n    for i in eachindex(x)\n        y[i] ~ Normal(m * x[i] + c, 1.0)\n    end\nend\n\nThis model right now does not have any observed data: the variable y is not part of the model arguments, nor is it conditioned on any values, so all the y[i]'s are treated as latent variables.\n\nnote: Why do we need to define `y` in the model?\nThe definition of y in the model is needed so that there is somewhere to assign y[i] to after the tilde-statement runs. If we did not define y, we would get an error when trying to call setindex! on an undefined variable.The fact that we defined y within the model does not change this: all variables on the left-hand side of a tilde-statement are treated as latent variables unless explicitly conditioned on or provided as an argument to the model function.\n\nLet's create some synthetic data to work with:\n\ntrue_m, true_c = 5.0, 3.0\n\nx = 0:0.1:0.5\ny_data = true_m .* x .+ true_c .+ randn(length(x))\n\nIf we run the model before conditioning on y, we will find that all of m, c, and y are drawn from the prior distribution.\n\nmodel = linear_regression(x)\n\n# Here, `rand(model())` samples from the prior distribution and returns a\n# VarNamedTuple of latent variables.\nrand(model)\n\nWe could, for example, do this many times, and compute the prior mean of y. This is analogous to using Turing's Prior() sampler.\n\nvnts = [rand(model) for _ in 1:1000]\nmean(vnt[@varname(y)] for vnt in vnts)\n\nThis is useful for prior predictive checks, for example.","title":"Conditioning and fixing"},{"category":"section","location":"conditionfix/#Conditioning","page":"Conditioning and fixing","text":"To condition the model on observed data, we can use the condition function, or its alias |. The most robust way of conditioning is to provide a VarNamedTuple that holds the values to condition on.\n\n# Construct a `VarNamedTuple` that holds the conditioning values.\nobservations = @vnt begin\n    y := y_data\nend\n\n# Equivalently: conditioned_model = condition(model, observations).\ncond_model = model | observations\n\nWe can inspect the values that have been conditioned on, using the conditioned function:\n\nconditioned(cond_model)\n\nIf we were to run this model, we would now find that y is an observed variable, and thus it is not sampled:\n\nparameters = rand(cond_model)\n\nWe can't directly draw from the posterior using DynamicPPL (rand still draws from the prior). However, since this is now an observed variable, the log-likelihood associated with the newly provided y will be computed:\n\nloglikelihood(cond_model, parameters)\n\nand this quantity can be used by MCMC algorithms to draw samples from the posterior distribution.","title":"Conditioning"},{"category":"section","location":"conditionfix/#Fixing","page":"Conditioning and fixing","text":"Fixing is exactly the same as conditioning, except that instead of incrementing the log-likelihood, there is no log-probability contribution from the fixed variables.\n\nIn essence, fixing a variable x ~ dist to a value x_val is equivalent to replacing the statement with x = x_val, which removes it from the model entirely.\n\nWe can illustrate this by fixing the intercept c to its true value:\n\n# Construct a `VarNamedTuple` that holds the fixed values.\nfix_values = VarNamedTuple(; c=true_c)\n\nfixed_model = fix(model, fix_values)\n\nand sampling from the prior again:\n\nparameters_fixed = rand(fixed_model)\n\nIf we were to repeat this many times, we would find that y is drawn from its prior, but because c is fixed, the samples will reflect that:\n\nmean(vnt[@varname(y)] for vnt in [rand(fixed_model) for _ in 1:1000])","title":"Fixing"},{"category":"section","location":"conditionfix/#Supplying-parameters-to-condition-or-fix-on","page":"Conditioning and fixing","text":"In the above examples we have provided the conditioning and fixing values as VarNamedTuples. Internally, DynamicPPL stores the values as VarNamedTuples, and it is strongly recommended that you construct them this way.\n\nFor convenience, both condition and fix also accept a variety of different input formats:\n\n# NamedTuple\nmodel | (; y=y_data)\n\n# AbstractDict{VarName}\nmodel | Dict(@varname(y) => y_data)\n\n# Pair\nmodel | (@varname(y) => y_data)\n\nNote, however, that these alternative input formats are not necessarily rich enough to capture all the necessary information. We recommend using VarNamedTuples directly in all cases.\n\nFor example, if you only wanted to condition y[1] but not the other y[i]'s, you cannot specify this via a NamedTuple, since NamedTuples require Symbols as keys.\n\nYou can easily specify this via VarNamedTuple and its helper macro @vnt:\n\nvnt = @vnt begin\n    y[1] := y_data[1]\nend\n\nNote that in this case since the VarNamedTuple has no knowledge of the length or shape of y, DynamicPPL will assume that y is a Base.Vector of unknown length (hence the GrowableArray above).\n\nThis will work fine as long as y is indeed a Base.Vector. However, if you want to avoid this, you should provide the full shape of y when defining the VarNamedTuple:\n\nvnt = @vnt begin\n    @template y = y_data\n    y[1] := y_data[1]\nend\n\nNow, the variable y is known to have the same shape and type as y_data.\n\nwarning: Warning\nIf you use custom array types in DynamicPPL that have different indexing semantics from Base.Array, then the templating shown here becomes mandatory. For example, OffsetArrays may behave incorrectly if templates are not supplied.\n\nIf we run the model again, we should find that y[1] is no longer sampled:\n\ncond_model_partial = model | vnt\nrand(cond_model_partial)","title":"Supplying parameters to condition or fix on"},{"category":"section","location":"conditionfix/#Missing-data","page":"Conditioning and fixing","text":"warning: Warning\nThe details in this section are tied closely to internal DynamicPPL details and we recommend that you use the above methods on conditioning on subsets of data. This is merely documented for completeness, and to avoid confusion since these details have been discussed in previous issues and Discourse threads.\n\nSometimes, in order to condition on a part of y, you can in fact condition on a vector y that has some of its entries missing.\n\nFor this to work, it is mandatory that each y[i] is individually on the left-hand side of a tilde-statement, as in the linear regression example above. That means that you can write\n\nfor i in eachindex(x)\n    y[i] ~ Normal(m * x[i] + c, 1.0)\nend\n\nbut not\n\ny ~ MvNormal(m .* x .+ c, I)\n\nThe reason why this works is if DynamicPPL finds a conditioned value of missing, it will treat the variable as not actually being conditioned. When each y[i] is individually on the left-hand side of a tilde-statement, this means that DynamicPPL can identify individual y[i]'s that are missing, and treat them as latent variables.\n\nvnt = @vnt begin\n    y := [missing, missing, 1.0, missing, 2.0, missing]\nend\ncond_model_missing = model | vnt\n\nrand(cond_model_missing)\n\nOn the other hand, if the entire y vector is on the left-hand side of a single tilde-statement, DynamicPPL cannot separate it into its missing and non-missing parts.\n\n","title":"Missing data"},{"category":"section","location":"ldf/overview/#ldf","page":"Overview","text":"Much of the Julia numerical computing ecosystem is built on top of an assumption that model parameters are provided as an AbstractVector{<:Real}. This is a very general format that is agnostic towards the details of the model, and is also compatible with automatic differentiation (although it is worth pointing out that modern AD packages, such as Mooncake and Enzyme, are able to differentiate through more complex data structures).\n\nThis poses a challenge to DynamicPPL: how can we 'translate' between a model, e.g.,\n\nusing DynamicPPL, Distributions\n\n@model function f()\n    x ~ Normal()\n    y ~ Beta(2, 2)\n    return nothing\nend\n\nwhere we supply named parameters, e.g. using VarNamedTuple(x = 1.0, y = 0.5), and the vectorised format where parameters are unnamed, e.g. [1.0, 0.5]?\n\nOn top of that, it is often very useful to supply transformed parameters such that any vector in ℝⁿ is a valid input to the algorithm. We therefore also need a way to encode a particular transform strategy into this translation layer.","title":"Overview"},{"category":"section","location":"ldf/overview/#LogDensityFunction,-conceptually","page":"Overview","text":"DynamicPPL provides a struct, LogDensityFunction, which acts as this translation layer. At its core, it is a wrapper around a model, a transform strategy, plus a mapping of VarNames to ranges. For example, in the above example, we could store the information as follows:\n\nmodel = f()\ntransform_strategy = LinkAll()\nranges = @vnt begin\n    x := 1:1\n    y := 2:2\nend\n\nGiven all of this, plus a vector (say [3.0, 4.0]), we can figure out how to rerun our model:\n\nSince the transform strategy is LinkAll(), we know that both values are transformed. (That is the transform strategy.)\nWe use the ranges to figure out that the transformed value of x is [3.0], and the transformed value of y is [4.0]. (This essentially specifies an initialisation strategy.)\nWe can then use any accumulators we like to obtain the information we are interested in.\n\nThe actual implementation of LogDensityFunction is a bit more complex than this, but is otherwise not too different.\n\nnote: Why store ranges instead of indices?\nOne might ask why we store ranges like 1:1 instead of just the index 1 itself. A major reason is because ranges generalise better to multivariate and other distributions, and Bijectors.jl contains an interface for transforming vectorised parameters into raw values. Storing indices for univariate distributions would require special-casing them, and would also make type stability more difficult.","title":"LogDensityFunction, conceptually"},{"category":"section","location":"ldf/overview/#Creating-a-LogDensityFunction","page":"Overview","text":"The main constructor for LogDensityFunction is\n\nLogDensityFunction(model, logdensityfunc, transform_strategy; adtype)\n\nmodel is of course the model itself, but the other arguments deserve more explanation.\n\nlogdensityfunc is a function that takes an OnlyAccsVarInfo and returns some real number. For example, it could be getlogjoint_internal (most of the time that is what you will want!). This is the argument that makes LogDensityFunction actually obey the interface that e.g. optimisers expect.\ntransform_strategy is an AbstractTransformStrategy that specifies whether the LogDensityFunction belongs in linked space or not.\n\nnote: Passing a set of vectorised values yourself\nWhen using an `AbstractTransformStrategy` as the third argument, the model will be evaluated once to generate a set of vectorised values.\nIf you want to avoid the cost of this model evaluation, you can instead directly pass a `VarNamedTuple` or `VarInfo` object that contains these vectorised values as the third argument.\nPlease see the docstring of `LogDensityFunction` for more details.\n\nIf you need to also obtain gradients with respect to the vectorised parameters, you can pass adtype::ADTypes.AbstractADType as a keyword argument. If gradients are not needed, you can omit this.\n\nSo, to make our LogDensityFunction for the model above, we can do\n\nldf = LogDensityFunction(model, getlogjoint_internal, LinkAll())","title":"Creating a LogDensityFunction"},{"category":"section","location":"ldf/overview/#The-LogDensityProblems.jl-interface","page":"Overview","text":"Once you have constructed a LogDensityFunction, it will obey the LogDensityProblems.jl interface. The most important part of this is\n\nusing LogDensityProblems\n\nLogDensityProblems.logdensity(ldf, [3.0, 4.0])\n\nWe can verify quickly that this is what we expect by running the model manually:\n\nusing StatsFuns: logistic\n\nx = 3.0\ny = logistic(4.0)\nparams = VarNamedTuple(; x=x, y=y)\n_, accs = init!!(model, OnlyAccsVarInfo(), InitFromParams(params), LinkAll())\naccs\n\nand we see that the log-prior minus the log-Jacobian is indeed the same as what LogDensityProblems.logdensity returns.\n\nIf you created the LogDensityFunction with an adtype, you can also obtain gradients via\n\nLogDensityProblems.logdensity_and_gradient(ldf, [3.0, 4.0])\n\n(although that is not demonstrated here since it requires an AD backend to be loaded).\n\nOther functions such as LogDensityProblems.capabilities and LogDensityProblems.dimension will also work as expected with LogDensityFunction.","title":"The LogDensityProblems.jl interface"},{"category":"section","location":"ldf/overview/#Is-LogDensityFunction-less-powerful-than-model-evaluation?","page":"Overview","text":"Given that the core purpose of LogDensityFunction is to evaluate a function mapping from vectors to log-densities, it might seem that it contains less information than the original model itself.\n\nHowever, this is not true! As alluded to above, the LogDensityFunction is a convenient wrapper that knows how to generate an initialisation strategy, and also contains a transform strategy.\n\nOn the next page we'll see how we can access these ourselves and use them to re-evaluate the model with the vectorised parameters.\n\n","title":"Is LogDensityFunction less powerful than model evaluation?"},{"category":"section","location":"vnt/motivation/#Motivation","page":"Motivation","text":"When executing a DynamicPPL model, it is very often necessary to store information about the random variables in the model. Consider, e.g.\n\nusing DynamicPPL, Distributions\n\n@model function f1()\n    x ~ Normal(0, 1)\n    y ~ Normal(x, 1)\n    z ~ Normal(y, 1)\n    return nothing\nend\nnothing # hide\n\nWhen we evaluate the model, we want to know the values of x, y, and z (even if the return value of the function is nothing; the return value is typically used for other purposes). Sometimes we might want to store other information about them, like their distributions. This information can be extracted and used by inference algorithms, for example.\n\nIn general this is done by storing some kind of mapping from AbstractPPL.VarNames to values. This is the job of VarNamedTuples, or VNT for short. Before we go into that, let's take a short view back to the past to see various approaches that don't quite work. This will provide a motivation for the design of VNT.","title":"Motivation"},{"category":"section","location":"vnt/motivation/#Historical-approaches","page":"Motivation","text":"Julia contains two main data structures for key-value maps: NamedTuples and Dicts.","title":"Historical approaches"},{"category":"section","location":"vnt/motivation/#NamedTuple","page":"Motivation","text":"NamedTuples are extremely lightweight data structures and are completely type stable. For example, in the above, we could store the values in a NamedTuple like so:\n\n(x=0.5, y=1.2, z=-0.3)\nnothing # hide\n\n(or whatever the sampled values are.) The issue with this, however, is that the keys of NamedTuples are symbols. This is fine for variables that are single identifiers (i.e., VarNames with an optic of AbstractPPL.Iden; which we'll call 'identity VarNames').\n\nBut in a model like the following, we have the VarNames x[1], x[2], x[3], and y.a. These cannot be properly converted into Symbols without loss of information, and this makes the output NamedTuple unsuitable for subsequent use.\n\n@model function f2()\n    x = Vector{Float64}(undef, 3)\n    for i in eachindex(x)\n        x[i] ~ Normal()\n    end\n\n    y = (; a=1.0)\n    return y.a ~ Normal()\nend\nnothing # hide\n\nIn the past, there was something called SimpleVarInfo which stored variables in NamedTuples (possibly converting non-identity VarNames to symbols with loss of information). This was a very fast data structure, but was unsuitable for general use.","title":"NamedTuple"},{"category":"section","location":"vnt/motivation/#Dict","page":"Motivation","text":"In order to be completely general, we could use a Dict{VarName, Any}. This allows us to store arbitrary VarNames as keys, and arbitrary values. Again, there was a SimpleVarInfo implementation that used this approach (specifically an OrderedDict).\n\nDict(\n    @varname(x[1]) => 0.5,\n    @varname(x[2]) => -1.2,\n    @varname(x[3]) => 1.7,\n    @varname(y.a) => 0.3,\n)\n\nThe main issue with Dicts is performance. In general there is substantial overhead in hashing and looking up keys in a Dict, compared to NamedTuple field access which is just direct memory access. Furthermore, accessing values is type-unstable (unless all the values can be made to share the same concrete type, which is the case above, but is unrealistic).","title":"Dict"},{"category":"section","location":"vnt/motivation/#Metadata-and-VarNamedVector","page":"Motivation","text":"In an attempt to get around the performance issue, DynamicPPL used to have data structures called Metadata and VarNamedVector. These structures essentially attempted to remove as much overhead as possible from Dicts by flattening their elements (i.e. variable values) and storing vectorised forms of them in contiguous vectors. Furthermore, they were often wrapped in NamedTuples to get some type stability benefits. This approach was somewhat successful and was used for many years: it is also described in the Turing.jl ACM Transactions on Probabilistic Machine Learning paper.\n\nWhile this fixed many performance problems with Dict, at its very core, it is still a mapping of VarNames to values, and therefore suffers from a lack of what we call 'constructiveness'.","title":"Metadata and VarNamedVector"},{"category":"section","location":"vnt/motivation/#constructiveness","page":"Motivation","text":"This is a very subtle issue, but caused a number of headaches with DynamicPPL. As we will soon see, this is the main motivation behind VNT, and even so VNT does not fully solve it.\n\nConsider the following model:\n\n@model function f3()\n    x = Vector{Float64}(undef, 2)\n    x[1] ~ Normal()\n    return x[2] ~ Normal()\nend\n\nIf at the end of the model we were to ask what the value of x was, we would have no way of knowing this. This is what we refer to as a lack of constructiveness: it is not possible to reconstruct the value of x from the stored information.\n\nLet's say we are using a Dict to store the values. We would have something like\n\nd = Dict(@varname(x[1]) => 0.5, @varname(x[2]) => -1.2)\n\nbut attempting to get d[@varname(x)] would return nothing, since there is no such key in the Dict. In fact, we cannot even access x[1:2], despite both indices obviously being defined.\n\nhaskey(d, @varname(x)), haskey(d, @varname(x[1:2]))","title":"Constructiveness"},{"category":"section","location":"vnt/motivation/#What's-the-problem-with-this?","page":"Motivation","text":"One issue that a lack of constructiveness causes is when reading information back from a Chains object. MCMCChains.jl, for example, breaks up all variables into its constituent scalar components. Thus, even if you have a multivariate distribution\n\nusing LinearAlgebra\n\n@model function f4()\n    x ~ MvNormal(zeros(2), I)\n    return x\nend\nnothing # hide\n\nsampling from this model would give a chain with keys x[1] and x[2]. When calling functions such as returned or predict on the chain, we have to somehow 'reconstruct' the value of x from its components, so that when executing the model we can use the actual value of x.\n\ninfo: Info\nSome workarounds exist for this (see this PR, and AbstractPPLDistributionsExt).using AbstractPPL\n\nno_dist = AbstractPPL.hasvalue(d, @varname(x))\nwith_dist = AbstractPPL.hasvalue(d, @varname(x), MvNormal(zeros(2), I))\n\n(no_dist, with_dist)\n\nAnother problem was when conditioning on values. In such cases, you had to condition on x in full, rather than its components:\n\nmodel = f4() | Dict(@varname(x) => [1.0, -1.0])  # This would work.\nmodel = f4() | Dict(@varname(x[1]) => 1.0, @varname(x[2]) => -1.0)  # This would not.\n\nFinally, we are unable to properly use different indexing schemes. For example, x[1] and x[1,1] mean the same thing if x is a Matrix, but the dictionary will think that they are different VarNames. Fundamentally, the issue is that the indexing semantics inside a Dict are not the same as the indexing semantics inside the model itself.","title":"What's the problem with this?"},{"category":"section","location":"vnt/motivation/#Desiderata","page":"Motivation","text":"In summary, we want a data structure that:\n\nCan store arbitrary VarNames as keys, with arbitrary values.\nIs performant and as type stable where possible. In particular, property access should always be type-stable, and indexing should be type-stable as long as the indexed values are homogeneous in type.\nIs constructive.\n\nVarNamedTuple generally solves these issues, with a few very niche edge cases that will be discussed at the very end.\n\n","title":"Desiderata"},{"category":"section","location":"vnt/arraylikeblocks/#array-like-blocks","page":"Array-like blocks","text":"In a number of VNT use cases, it is necessary to associate multiple indices in a VarNamedTuple with an object that is not necessarily the same number of elements.\n\nConsider, for example, this model:\n\n@model function f()\n    x = zeros(3)\n    return x[1:3] ~ Dirichlet(ones(3))\nend\n\nand suppose we want to store the prior distribution associated with each variable in a VarNamedTuple. With a Dict{VarName,Distribution}, we can do this:\n\nd = Dict{VarName,Distribution}(@varname(x[1:3]) => Dirichlet(ones(3)))\n\nbut we incur all the costs associated with the use of a Dict, as described before.\n\nWith a VarNamedTuple, we cannot store this directly:\n\nvnt.data.x = ... # some array\nvnt.data.x[1:3] = Dirichlet(ones(3))  # will error\n\nbecause Dirichlet is not an array, and setindex! will fail. Nor can we write\n\nvnt.data.x = ... # some array\nvnt.data.x[1:3] .= Dirichlet(ones(3))\n\nbecause although this will not error, it is semantically different: this means that every element x[1], x[2], and x[3] will be assigned the same Dirichlet(ones(3)) object, which is not what we want.\n\nThe current solution to this is to use ArrayLikeBlocks, which are thin wrappers around the actual value, but additionally also store the indices used to set the value. The second and third arguments here are the indices (positional and keyword) used to set the value, and the fourth argument is the size of the block.\n\nusing DynamicPPL, Distributions\nusing DynamicPPL.VarNamedTuples: ArrayLikeBlock\n\nalb = ArrayLikeBlock(Dirichlet(ones(3)), 1:3, (;), (3,))\n\nWe then set this ArrayLikeBlock in all the relevant indices. The extra information in the ArrayLikeBlock allows us to forbid partial indexing into it later on. In particular, we want to ensure that users can only retrieve the entire block at once, and not e.g. just x[1] or x[2:3].","title":"Array-like blocks"},{"category":"section","location":"vnt/arraylikeblocks/#Getting-and-setting:-in-practice","page":"Array-like blocks","text":"As a user you should not have to deal with ArrayLikeBlocks directly. Under the hood, templated_setindex!! will automatically wrap values in ArrayLikeBlocks when necessary:\n\nx = zeros(5)\nvnt = DynamicPPL.templated_setindex!!(\n    VarNamedTuple(), Dirichlet(ones(3)), @varname(x[1:3]), x\n)\n\nYou can access the value again as long as you refer to the full range:\n\nvnt[@varname(x[1:3])]\n\nBecause we provided template information, you can access this via any other combination of indexing, as long as it refers to all three indices:\n\nvnt[@varname(x[begin:(end - 2)])]\n\nHowever, if you try to access only part of the block, you will get an error:\n\nvnt[@varname(x[1])]\n\nFurthermore, if you set a value into any of the indices covered by the block, the entire block is invalidated and thus removed:\n\nvnt = DynamicPPL.templated_setindex!!(vnt, Normal(), @varname(x[2]), x)","title":"Getting and setting: in practice"},{"category":"section","location":"vnt/arraylikeblocks/#Which-parts-of-DynamicPPL-use-array-like-blocks?","page":"Array-like blocks","text":"Simply put, anywhere where we don't store raw values. Some examples follow. (This list is meant to only be illustrative, not exhaustive!)","title":"Which parts of DynamicPPL use array-like blocks?"},{"category":"section","location":"vnt/arraylikeblocks/#VarInfo","page":"Array-like blocks","text":"In VarInfo, we need to be able to store either linked or unlinked values (in general, TransformedValues). These are always vectorised values, and the linked and unlinked vectors may have different sizes (this is indeed the case for Dirichlet distributions). This means that we have to collectively assign multiple indices in the VarNamedTuple to a single vector, which may or may not have the same size as the indices.\n\n@model function dirichlet()\n    x = zeros(3)\n    return x[1:3] ~ Dirichlet(ones(3))\nend\ndirichlet_model = dirichlet()\nvi = VarInfo(dirichlet_model)\nvi.values\n\nThis means that in the actual VarInfo we do not have a notion of what x[1] is:\n\nDynamicPPL.getindex_internal(vi, @varname(x[1]))\n\nSee the documentation on storing values for more details.","title":"VarInfo"},{"category":"section","location":"vnt/arraylikeblocks/#Prior-distributions","page":"Array-like blocks","text":"In extract_priors we use a VNT to store the prior distributions seen at each point in the model. This is exactly the same use case as in the introduction.\n\nDynamicPPL.extract_priors(dirichlet_model)","title":"Prior distributions"},{"category":"section","location":"vnt/arraylikeblocks/#LogDensityFunction","page":"Array-like blocks","text":"DynamicPPL.LogDensityFunction has to retain information about whether each VarNames is linked or not, and the indices in the vectorised parameters that correspond to each variable.\n\nFor example, consider creating a linked LogDensityFunction for (a slightly expanded version of) the Dirichlet model above:\n\n@model function expanded_dirichlet()\n    x = zeros(4)\n    x[1] ~ Normal()\n    return x[2:4] ~ Dirichlet(ones(3))\nend\nmodel = expanded_dirichlet()\n\nldf = LogDensityFunction(model, DynamicPPL.getlogjoint_internal, LinkAll())\nnothing # hide\n\nWhen linking a Dirichlet(ones(3)), the resulting vector will have length 2. So, the LogDensityFunction takes in a vector of length 3, for which the first index belongs to x[1], and the next two indices belong to the linked parameters for x[2:4]. Furthermore, it needs to remember that all the variables have been linked. We can see that this is exactly how the LogDensityFunction has stored the information:\n\nldf._varname_ranges\n\nnote: Note\nFrom the example above, we can see that indexing into this PartialArray is necessarily type-unstable (since the element type is Any). This immediately suggests a performance optimisation for the user: do not mix and match different kinds of distributions within the same vector. For example, if all the x[i]'s were distributed according to univariate distributions, this would not be an issue.\n\n","title":"LogDensityFunction"},{"category":"section","location":"migration/#Migrating-old-VarInfo-code","page":"Migrating old VarInfo code","text":"Please get in touch if you have some old code you're unsure how to migrate, and we will be happy to add it to this list.\n\nusing DynamicPPL, Distributions, Random\n\n@model function f()\n    x ~ Normal()\n    y ~ LogNormal()\n    return 1.0 ~ Normal(x + y)\nend\n\nmodel = f()","title":"Migrating old VarInfo code"},{"category":"section","location":"migration/#Sampling-from-the-prior","page":"Migrating old VarInfo code","text":"Old:\n\nvi = VarInfo(Xoshiro(468), model)\n\nNew:\n\naccs = OnlyAccsVarInfo()\n_, vi = init!!(Xoshiro(468), model, accs, InitFromPrior(), UnlinkAll())\nvi","title":"Sampling from the prior"},{"category":"section","location":"migration/#Getting-parameter-values","page":"Migrating old VarInfo code","text":"Old:\n\nvi = VarInfo(Xoshiro(468), model)\n# This no longer works, but you may have used it.\n# vi[@varname(x)], vi[@varname(y)]\n\n# This still works\nDynamicPPL.getindex_internal(vi, @varname(x))\n\nNew:\n\n# Set to true if you want to include results of `:=` statements.\naccs = OnlyAccsVarInfo(RawValueAccumulator(false))\n_, vi = init!!(Xoshiro(468), model, accs, InitFromPrior(), UnlinkAll())\nget_raw_values(vi)","title":"Getting parameter values"},{"category":"section","location":"migration/#Generating-vectorised-parameters-from-linked-VarInfo","page":"Migrating old VarInfo code","text":"Old:\n\nvi = VarInfo(Xoshiro(468), model)\nvi = DynamicPPL.link!!(vi, model)\nvi[:]\n\nThe new pattern recognises that in practice you are likely using vi[:] in conjunction with a LogDensityFunction. So we make one first:\n\nldf = LogDensityFunction(model, getlogjoint_internal, LinkAll())\nnothing # hide\n\nThen you can do:\n\nrand(Xoshiro(468), ldf)\n\nThis gives you a set of parameters, but if you want to also obtain the log-density at the new parameters, you can do this in a single call to init!!; please see the documentation on LogDensityFunction for more details on how to do this.","title":"Generating vectorised parameters from linked VarInfo"},{"category":"section","location":"migration/#Re-evaluating-log-density-at-new-parameters","page":"Migrating old VarInfo code","text":"Old:\n\nvi = VarInfo(Xoshiro(468), model)\n\nvals = [1.0, 1.0]\n# Note this was `unflatten` (no exclamation mark) in the old code\nvi = DynamicPPL.unflatten!!(vi, vals)\n_, vi = DynamicPPL.evaluate!!(model, vi)\nvi\n\nThe new path also assumes that you are using a LogDensityFunction:\n\n# Note that we use `UnlinkAll()` here to match the VarInfo above.\n# If your VarInfo was linked, you should use `LinkAll()` instead.\n\nldf = LogDensityFunction(model, getlogjoint_internal, UnlinkAll())\n\nThen you can do:\n\ninit_strategy = InitFromVector(vals, ldf)\n\nvi = OnlyAccsVarInfo()\n_, vi = init!!(Xoshiro(468), model, vi, init_strategy, ldf.transform_strategy)\nvi\n\n","title":"Re-evaluating log density at new parameters"},{"category":"section","location":"vnt/implementation/#Implementation","page":"Implementation","text":"Having discussed what a VarNamedTuple should look like, we now turn our attention to how it is constructed. The core entry point is a function called _setindex_optic!!, which essentially says: 'set the value that can be accessed using this optic, creating it if necessary'. For example,\n\nusing AbstractPPL, DynamicPPL\nusing DynamicPPL.VarNamedTuples: NoTemplate, _setindex_optic!!, AllowAll\n\ncollection = VarNamedTuple()\n_setindex_optic!!(collection, 1.0, @opticof(_.x[1].a), NoTemplate(), AllowAll())\n\nmeans: 'modify this collection such that the value at the path _.x[1].a is set to 1.0, creating any missing structure along the way, with no template provided.' (AllowAll() here is a marker that says that _setindex_optic!! is allowed to create a new key if it doesn't already exist, and also overwrite an old key if it does already exist.)\n\nNotice that, if collection is the top-level VarNamedTuple being used to hold variable values, this is the same as saying \"add the VarName @varname(x[1].a) with value 1.0 to the VarNamedTuple vnt\". Indeed, BangBang.setindex!!(vnt, 1.0, @varname(x[1].a)) directly calls the above.\n\nIf a template is provided, it must be for the entire structure being created, that is, the template should be the shape of vnt.\n\ninfo: Info\nWhen we called templated_setindex!!, we said that the template should be for the top-level symbol, i.e. x. It seems like we are introducing an inconsistency here, since the template above would be for the entire structure. That is why templated_setindex!! does not pass the template as-is; it wraps the template in one level of SkipTemplate{1}(template), which effectively means 'don't use a template for the first level, then use it for the next'.\n\nBecause VarNames have a nested structure, it should not be surprising to find that VarNamedTuples are constructed by recursing into _setindex_optic!!. For an optic like _.x[1].a, the strategy is as follows:\n\nLook at the outermost layer of the optic (.x above).\nIf collection already has a value for that layer (i.e. a property called x), get that sub-value, and call _setindex_optic!! on that sub-value with the rest of the optic ([1].a above).\nIf not, call the function make_leaf, which constructs the necessary sub-structure, using any template provided.\n\nmake_leaf is itself of course also recursive, and is where the bulk of the complicated logic occurs.","title":"Implementation"},{"category":"section","location":"vnt/implementation/#Making-leaves","page":"Implementation","text":"Following on from the explanation above, it follows that make_leaf(value, optic, template) is responsible for creating a new structure s, which already holds value at the path specified by optic. If a template is provided, s should be constructed to match the shape and type of template.\n\nLet's build this up using some simple examples to illustrate the idea, first ignoring any templates.\n\nHere, this is an identity optic, which is the base case. Of course, the value itself (1.0) can be indexed into with the identity optic to give itself. So we can just return the value.\n\nusing DynamicPPL.VarNamedTuples: make_leaf\n\nmake_leaf(1.0, @opticof(_), NoTemplate())\n\nNext, consider a field optic like _.a. To create a structure that holds 1.0 at that path, we can create a VarNamedTuple with a single field a set to 1.0.\n\nmake_leaf(1.0, @opticof(_.a), NoTemplate())\n\nFinally, consider an index optic like _[3]. Since no template is provided, we will need to make a guess: in this case it will be a 1-dimensional GrowableArray, with a size of at least 3.\n\nl = make_leaf(1.0, @opticof(_[3]), NoTemplate())\n# l isa PartialArray, which is quite boring to print, so let's peek inside it.\ntypeof(l.data), l.data, l.mask\n\nConversely, if a template is provided, we'll just use that directly. (But we still have to make sure to set the value at the correct index!)\n\nl = make_leaf(1.0, @opticof(_[3]), zeros(4))\nl.data, l.mask\n\nNow, consider a recursive case, like @opticof(_.a.b). make_leaf needs to create something which can hold 1.0 at the path .a.b.\n\nTo do so, it first has to create the sub-structure that will hold 1.0 at .b, and then it can create an outer structure that holds that sub-structure at .a.\n\nmake_leaf(1.0, @opticof(_.a.b), NoTemplate())\n\nThis should conceptually be fairly understandable. Here is a simplified version of the recursive implementation:\n\n# Let's say `optic` is _[1].a\nfunction make_leaf(leaf_value, optic, template)\n    this_optic, child_optic = split_optic(optic)\n    # ^ _[1]    ^ _.a \n\n    sub_value = make_leaf(leaf_value, child_optic, child(template))\n    # ^ VNT                           ^ _.a        ^ template[1]\n\n    empty_value = create_empty_structure(this_optic, template)\n    # ^ PA                               ^ _[1]\n\n    value = setindex!!(empty_value, this_optic, sub_value)\n    # ^ PA->VNT        ^ PA         ^ _.[1]     ^ VNT\n\n    return value\nend\n\nSome explanatory notes:\n\nThe whole purpose of the function is to ensure that value is something where you can index into with optic to get leaf_value.\nSince sub_value is also created with the same function, that means it must be something you can index into with child_optic to get leaf_value.\nempty_value needs to be something that can hold sub_value at this_optic. We don't yet insert the data. However, to ensure type stability, we should instantiate the PA with the correct element type: in this case that's just typeof(sub_value). (If we don't use the correct element type, the subsequent call to setindex!! will have to change the element type of the PA.)\nvalue is then created by putting sub_value into empty_value at this_optic.\n\ninfo: Info\nRegarding point (3): we haven't yet covered ArrayLikeBlocks (that will be on the next page). If sub_value is something that would be stored as an ArrayLikeBlock, we need to instantiate empty_value with typeof(ArrayLikeBlock(sub_value)) instead of typeof(sub_value), again for type stability reasons. If you are not familiar with this, don't worry about it for now.","title":"Making leaves"},{"category":"section","location":"vnt/implementation/#Multi-indices","page":"Implementation","text":"Multi-indexing, or slices, is where things get a bit more complicated, as it enforces extra constraints that are not present above. To illustrate this, we'll now consider the case where the optic is _[2:3][1].\n\nLogically speaking, this is exactly the same as _[2]. So we should be creating a PartialArray that holds leaf_value at index 2. But on top of that, we also need to make sure that the created structure has at least three indices: otherwise, the index 2:3 would be invalid.\n\n# Let's say `optic` is _[2:3][1]\nfunction make_leaf(leaf_value, optic, template)\n    this_optic, child_optic = split_optic(optic)\n    # ^ _[2:3]  ^ _[1]\n\n    sub_value = make_leaf(leaf_value, child_optic, child(template))\n    # ^ PA (x)                        ^ _[1]       ^ template[2:3]\n\n    empty_value = create_empty_structure(this_optic, template)\n    # ^ PA (y)                           ^ _[2:3]\n\n    value = setindex!!(empty_value, this_optic, sub_value)\n    # ^ PA (z)         ^ PA (y)     ^ _[2:3]    ^ PA (x)   \n\n    return value\nend\n\nThere are three PartialArrays being created here, which are called x, y, and z in the comments above.\n\nThe first point of difference is when creating empty_value: previously, for type stability purposes, we would create the PA with an element type of typeof(sub_value). However, in this case, sub_value is actually a slice of empty_value, and so we can't use its type: we need to use eltype(sub_value) instead.\n\nThe other tricky part is about lengths. Let's work backwards from the last line. z and y will have the same length, since they are related by the setindex!! call. They must be at least of length 3. This is easy to guarantee: when creating y, we either have a template to work with (which must already be of more than length 3), or we can use a GrowableArray and deduce the minimum length from the index 2:3.\n\nHowever, x must also exactly have length 2; otherwise, when setting it into the indices 2:3 of y, an error will occur. This part is more problematic, because the recursive call to make_leaf only ever sees the index 1. If it doesn't have a template to work with, this will error, because it will create a GrowableArray of length 1, which cannot then be set into indices 2:3 of y.\n\n(If it does have a template, we are all good, because the template will be created by indexing into the upper-level template with 2:3, which will give a template of the right size.)\n\nTo solve this, we have two choices:\n\nRecursively pass down information about the expected size of the template; or\nAfter getting sub_value, check if this_index is a multi-index, and if so, expand sub_value to the correct length.\n\nThe current implementation uses the second approach. Note that this is only needed when there is no template provided, and when there is a multi-index.","title":"Multi-indices"},{"category":"section","location":"vnt/implementation/#Detecting-multi-indices","page":"Implementation","text":"This means that in general we need a good way of differentiating between single-element indexing and multi-element indexing. One could iterate over the indices and check which of them are ranges or colons, for example; however, that's not general and doesn't allow for user-defined indices or arbitrary index types. (Of course, if there is no template, then we do fall back on that approach.)\n\nThe solution is this helper function, which is used liberally throughout the VarNamedTuple implementation:\n\nfunction _is_multiindex(template::AbstractArray, ix...; kw...)\n    return ndims(view(template, ix...; kw...)) > 0\nend\n\nwhich works really well across different array types, and is frequently (if not always?) constant propagated.\n\nusing DimensionalData;\nusing InteractiveUtils: @code_warntype;\n\nda = DimArray(randn(2, 3), (X(), :y));\n@code_warntype _is_multiindex(da; a=X(Not(2)), y=2)\n\n","title":"Detecting multi-indices"},{"category":"section","location":"api/#API","page":"API","text":"Part of the API of DynamicPPL is defined in the more lightweight interface package AbstractPPL.jl and reexported here.","title":"API"},{"category":"section","location":"api/#Model","page":"API","text":"","title":"Model"},{"category":"section","location":"api/#Macros","page":"API","text":"A core component of DynamicPPL is the @model macro. It can be used to define probabilistic models in an intuitive way by specifying random variables and their distributions with ~ statements. These statements are rewritten by @model as calls of internal functions for sampling the variables and computing their log densities.","title":"Macros"},{"category":"section","location":"api/#Type","page":"API","text":"A Model can be created by calling the model function, as defined by @model.\n\nModels are callable structs.\n\nThe context of a model can be set using contextualize:\n\nSome models require threadsafe evaluation (see the Turing docs for more information on when this is necessary). If this is the case, one must enable threadsafe evaluation for a model:","title":"Type"},{"category":"section","location":"api/#Evaluation","page":"API","text":"With rand one can draw samples from the prior distribution of a Model.\n\nOne can also evaluate the log prior, log likelihood, and log joint probability.","title":"Evaluation"},{"category":"section","location":"api/#LogDensityProblems.jl-interface","page":"API","text":"The LogDensityProblems.jl interface is also supported by wrapping a Model in a DynamicPPL.LogDensityFunction.\n\nInternally, this is accomplished using init!! on:\n\nYou can also draw vectorised samples from a LogDensityFunction via\n\n(although note that this is a limited interface as it only generates parameters; please see the documentation for more information on how to combine LogDensityFunction with init!! more generally.)","title":"LogDensityProblems.jl interface"},{"category":"section","location":"api/#Condition-and-decondition","page":"API","text":"A Model can be conditioned on a set of observations with AbstractPPL.condition or its alias |.\n\nSimilarly, one can specify with AbstractPPL.decondition that certain, or all, random variables are not observed.","title":"Condition and decondition"},{"category":"section","location":"api/#Fixing-and-unfixing","page":"API","text":"We can also fix a collection of variables in a Model to certain values using DynamicPPL.fix.\n\nThis is quite similar to the aforementioned condition and its siblings, but they are indeed different operations:\n\nconditioned variables are considered to be observations, and are thus included in the computation logjoint and loglikelihood, but not in logprior.\nfixed variables are considered to be constant, and are thus not included in any log-probability computations.\n\nThe differences are more clearly spelled out in the docstring of DynamicPPL.fix below.\n\nThe difference between DynamicPPL.fix and DynamicPPL.condition is described in the docstring of DynamicPPL.fix above.\n\nSimilarly, we can revert this with DynamicPPL.unfix, i.e. return the variables to their original meaning:","title":"Fixing and unfixing"},{"category":"section","location":"api/#Predicting","page":"API","text":"DynamicPPL provides functionality for generating samples from the posterior predictive distribution through the predict function. This allows you to use posterior parameter samples to generate predictions for unobserved data points.\n\nThe predict function has two main methods:\n\nFor AbstractVector{<:AbstractVarInfo} - useful when you have a collection of VarInfo objects representing posterior samples.\nFor MCMCChains.Chains (only available when MCMCChains.jl is loaded) - useful when you have posterior samples in the form of an MCMCChains.Chains object.","title":"Predicting"},{"category":"section","location":"api/#Basic-Usage","page":"API","text":"The typical workflow for posterior prediction involves:\n\nFitting a model to observed data to obtain posterior samples\nCreating a new model instance with some variables marked as missing (unobserved)\nUsing predict to generate samples for these missing variables based on the posterior parameter samples\n\nWhen using predict with MCMCChains.Chains, you can control which variables are included in the output with the include_all parameter:\n\ninclude_all=false (default): Include only newly predicted variables\ninclude_all=true: Include both parameters from the original chain and predicted variables","title":"Basic Usage"},{"category":"section","location":"api/#Marginalisation","page":"API","text":"DynamicPPL provides the marginalize function to marginalise out variables from a model. This requires MarginalLogDensities.jl to be loaded in your environment.\n\nA MarginalLogDensity object acts as a function which maps non-marginalised parameter values to a marginal log-probability. To retrieve a VarInfo object from it, you can use InitFromVector.","title":"Marginalisation"},{"category":"section","location":"api/#Models-within-models","page":"API","text":"One can include models and call another model inside the model function with left ~ to_submodel(model).\n\nNote that a [to_submodel](@ref) is only sampleable; one cannot compute logpdf for its realizations.\n\nIn the context of including models within models, it's also useful to prefix the variables in sub-models to avoid variable names clashing:","title":"Models within models"},{"category":"section","location":"api/#Utilities","page":"API","text":"It is possible to manually increase (or decrease) the accumulated log likelihood or prior from within a model function.\n\nReturn values of the model function can be obtained with returned(model, sample), where sample is either a MCMCChains.Chains object (which represents a collection of samples), or a single sample represented as a NamedTuple or a dictionary of VarNames.\n\nFor a chain of samples, one can compute the pointwise log-likelihoods of each observed random variable with pointwise_loglikelihoods. Similarly, the log-densities of the priors using pointwise_prior_logdensities or both, i.e. all variables, using pointwise_logdensities.\n\nSometimes it can be useful to extract the priors of a model. This is the possible using extract_priors.","title":"Utilities"},{"category":"section","location":"api/#Distribution-wrappers","page":"API","text":"","title":"Distribution wrappers"},{"category":"section","location":"api/#AD-testing-and-benchmarking-utilities","page":"API","text":"To test and/or benchmark the performance of an AD backend on a model, DynamicPPL provides the following utilities:\n\nThe default test setting is to compare against ForwardDiff. You can have more fine-grained control over how to test the AD backend using the following types:\n\nThese are returned / thrown by the run_ad function:","title":"AD testing and benchmarking utilities"},{"category":"section","location":"api/#Demo-models","page":"API","text":"DynamicPPL provides several demo models in the DynamicPPL.TestUtils submodule.\n\nFor every demo model, one can define the true log prior, log likelihood, and log joint probabilities.\n\nAnd in the case where the model includes constrained variables, it can also be useful to define\n\nFinally, the following methods can also be of use:","title":"Demo models"},{"category":"section","location":"api/#Debugging-Utilities","page":"API","text":"DynamicPPL provides a few methods for checking validity of a model-definition.\n\nAnd some which might be useful to determine certain properties of the model based on the debug trace.\n\nFor determining whether one might have type instabilities in the model, the following can be useful\n\nInterally, the type-checking methods make use of the following method for construction of the call with the argument types:","title":"Debugging Utilities"},{"category":"section","location":"api/#Advanced","page":"API","text":"","title":"Advanced"},{"category":"section","location":"api/#Variable-names","page":"API","text":"Names and possibly nested indices of variables are described with AbstractPPL.VarName. They can be defined with AbstractPPL.@varname. Please see the documentation of AbstractPPL.jl for further information.","title":"Variable names"},{"category":"section","location":"api/#Data-Structures-of-Variables","page":"API","text":"DynamicPPL provides a data structure for storing samples and accumulation of the log-probabilities, called VarInfo. The interface that VarInfo respects is described by the abstract type AbstractVarInfo. Internally DynamicPPL also uses a couple of other subtypes of AbstractVarInfo.\n\nOne main characteristic of VarInfo is that samples are transformed to unconstrained Euclidean space and stored in a linearized form, as described in the main Turing documentation. The Transformations section below describes the methods used for this. In the specific case of VarInfo, it keeps track of whether samples have been transformed by setting flags on them, using the following functions.","title":"Data Structures of Variables"},{"category":"section","location":"api/#VarNamedTuples","page":"API","text":"VarInfo is only a thin wrapper around VarNamedTuple, which stores arbitrary data keyed by VarNames. For more details on VarNamedTuple, see the Internals section of our documentation.\n\nVarNamedTuple provides a Dict-like interface, so you can iterate over keys(vnt), values(vnt), and pairs(vnt). You can also use getindex(vnt, key), but setindex! is not allowed: all changes to a VarNamedTuple must be done via setindex!! or templated_setindex!!. Please see the VarNamedTuple documentation for more details.\n\nYou can convert a VarNamedTuple to a NamedTuple in the case where all keys are VarNames with identity optics.","title":"VarNamedTuples"},{"category":"section","location":"api/#Accumulators","page":"API","text":"The subtypes of AbstractVarInfo store the cumulative log prior and log likelihood, and sometimes other variables that change during executing, in what are called accumulators.\n\nThe float type used for accumulation of log-probabilities is defined by a compile-time preference:\n\nTo manipulate the accumulators in a VarInfo, one can use:","title":"Accumulators"},{"category":"section","location":"api/#Common-API","page":"API","text":"","title":"Common API"},{"category":"section","location":"api/#Accumulation-of-log-probabilities","page":"API","text":"","title":"Accumulation of log-probabilities"},{"category":"section","location":"api/#Variables-and-their-realizations","page":"API","text":"","title":"Variables and their realizations"},{"category":"section","location":"api/#Transformations","page":"API","text":"","title":"Transformations"},{"category":"section","location":"api/#Utils","page":"API","text":"","title":"Utils"},{"category":"section","location":"api/#Evaluation-Contexts","page":"API","text":"Internally, model evaluation is performed with AbstractPPL.evaluate!!.\n\nThis method mutates the varinfo used for execution. By default, it does not perform any actual sampling: it only evaluates the model using the values of the variables that are already in the varinfo. If you wish to sample new values, see the section on VarInfo initialisation just below this.\n\nThe behaviour of a model execution can be changed with evaluation contexts, which are a field of the model.\n\nAll contexts are subtypes of AbstractPPL.AbstractContext.\n\nContexts are split into two kinds:\n\nLeaf contexts: These are the most important contexts as they ultimately decide how model evaluation proceeds. For example, DefaultContext evaluates the model using values stored inside a VarInfo's metadata, whereas InitContext obtains new values either by sampling or from a known set of parameters. DynamicPPL has more leaf contexts which are used for internal purposes, but these are the two that are exported.\n\nTo implement a leaf context, you need to subtype AbstractPPL.AbstractContext and implement the tilde_assume!! and tilde_observe!! methods for your context.\n\nParent contexts: These essentially act as 'modifiers' for leaf contexts. For example, PrefixContext adds a prefix to all variable names during evaluation, while CondFixContext marks certain variables as being either conditioned or fixed.\n\nTo implement a parent context, you have to subtype DynamicPPL.AbstractParentContext, and implement the childcontext and setchildcontext methods. If needed, you can also implement tilde_assume!! and tilde_observe!! for your context. This is optional; the default implementation is to simply delegate to the child context.\n\nSince contexts form a tree structure, these functions are automatically defined for manipulating context stacks. They are mainly useful for modifying the fundamental behaviour (i.e. the leaf context), without affecting any of the modifiers (i.e. parent contexts).\n\nSometimes it is necessary to handle all PrefixContexts in a context stack at one go:","title":"Evaluation Contexts"},{"category":"section","location":"api/#VarInfo-initialisation","page":"API","text":"The function init!! is used to initialise, or overwrite, values in a VarInfo. It is really a thin wrapper around using evaluate!! with an InitContext.\n\nTo accomplish this, an initialisation strategy is required, which defines how new values are to be obtained. There are several concrete strategies provided in DynamicPPL: see the initialisation strategies page for more information.\n\nIf you wish to write your own, you have to subtype DynamicPPL.AbstractInitStrategy and implement the init method. In very rare situations, you may also need to implement get_param_eltype, which defines the element type of the parameters generated by the strategy.\n\nThe function DynamicPPL.init should return a TransformedValue.\n\nThe interface for working with transformed values consists of:","title":"VarInfo initialisation"},{"category":"section","location":"api/#Converting-VarInfos-to/from-chains","page":"API","text":"It is a fairly common operation to want to convert a collection of VarInfo objects into a chains object for downstream analysis.\n\nThis can be accomplished by first converting each VarInfo into a ParamsWithStats object:\n\nOnce you have a matrix of these, you can convert them into a chains object using:\n\nIf you only have a vector you can use hcat to convert it into an N×1 matrix first.\n\nFurthermore, one can convert chains back into a collection of parameter dictionaries and/or stats with:\n\n(Note that the model argument is mandatory as it provides templating information for the variables in the chains.) With these, you can (for example) extract the parameter dictionaries and use InitFromParams to re-evaluate a model at each point in the chain.\n\n","title":"Converting VarInfos to/from chains"},{"category":"macro","location":"api/#DynamicPPL.@model","page":"API","text":"@model(expr[, warn = false])\n\nMacro to specify a probabilistic model.\n\nIf warn is true, a warning is displayed if internal variable names are used in the model definition.\n\nExamples\n\nModel definition:\n\n@model function model(x, y = 42)\n    ...\nend\n\nTo generate a Model, call model(xvalue) or model(xvalue, yvalue).\n\n\n\n\n\n","title":"DynamicPPL.@model"},{"category":"type","location":"api/#DynamicPPL.Model","page":"API","text":"struct Model{F,argnames,defaultnames,missings,Targs,Tdefaults,Ctx<:AbstractContext,Threaded}\n    f::F\n    args::NamedTuple{argnames,Targs}\n    defaults::NamedTuple{defaultnames,Tdefaults}\n    context::Ctx=DefaultContext()\nend\n\nA Model struct with model evaluation function of type F, arguments of names argnames types Targs, default arguments of names defaultnames with types Tdefaults, missing arguments missings, and evaluation context of type Ctx.\n\nHere argnames, defaultargnames, and missings are tuples of symbols, e.g. (:a, :b). context is by default DefaultContext().\n\nAn argument with a type of Missing will be in missings by default. However, in non-traditional use-cases missings can be defined differently. All variables in missings are treated as random variables rather than observations.\n\nThe Threaded type parameter indicates whether the model requires threadsafe evaluation (i.e., whether the model contains statements which modify the internal VarInfo that are executed in parallel). By default, this is set to false.\n\nThe default arguments are used internally when constructing instances of the same model with different arguments.\n\nExamples\n\njulia> Model(f, (x = 1.0, y = 2.0))\nModel{typeof(f),(:x, :y),(),(),Tuple{Float64,Float64},Tuple{}}(f, (x = 1.0, y = 2.0), NamedTuple())\n\njulia> Model(f, (x = 1.0, y = 2.0), (x = 42,))\nModel{typeof(f),(:x, :y),(:x,),(),Tuple{Float64,Float64},Tuple{Int64}}(f, (x = 1.0, y = 2.0), (x = 42,))\n\njulia> Model{(:y,)}(f, (x = 1.0, y = 2.0), (x = 42,)) # with special definition of missings\nModel{typeof(f),(:x, :y),(:x,),(:y,),Tuple{Float64,Float64},Tuple{Int64}}(f, (x = 1.0, y = 2.0), (x = 42,))\n\n\n\n\n\n","title":"DynamicPPL.Model"},{"category":"method","location":"api/#DynamicPPL.Model-Tuple{}","page":"API","text":"(model::Model)([rng, varinfo])\n\nSample from the prior of the model with random number generator rng.\n\nReturns the model's return value.\n\nNote that calling this with an existing varinfo object will mutate it.\n\n\n\n\n\n","title":"DynamicPPL.Model"},{"category":"function","location":"api/#DynamicPPL.contextualize","page":"API","text":"contextualize(model::Model, context::AbstractContext)\n\nReturn a new Model with the same evaluation function and other arguments, but with its underlying context set to context.\n\n\n\n\n\n","title":"DynamicPPL.contextualize"},{"category":"function","location":"api/#DynamicPPL.setthreadsafe","page":"API","text":"setthreadsafe(model::Model, threadsafe::Bool)\n\nReturns a new Model with its threadsafe flag set to threadsafe.\n\nThreadsafe evaluation ensures correctness when executing model statements that mutate the internal VarInfo object in parallel. For example, this is needed if tilde-statements are nested inside Threads.@threads or similar constructs.\n\nIt is not needed for generic multithreaded operations that don't involve VarInfo. For example, calculating a log-likelihood term in parallel and then calling @addlogprob! outside of the parallel region is safe without needing to set threadsafe=true.\n\nIt is also not needed for multithreaded sampling with AbstractMCMC's MCMCThreads().\n\nSetting threadsafe to true increases the overhead in evaluating the model. Please see the Turing.jl docs for more details.\n\n\n\n\n\n","title":"DynamicPPL.setthreadsafe"},{"category":"function","location":"api/#DynamicPPL.requires_threadsafe","page":"API","text":"requires_threadsafe(model::Model)\n\nReturn whether model has been marked as needing threadsafe evaluation (using setthreadsafe).\n\n\n\n\n\n","title":"DynamicPPL.requires_threadsafe"},{"category":"function","location":"api/#Base.rand","page":"API","text":"rand([rng=Random.default_rng()], model::Model)\n\nSample a VarNamedTuple of raw values from the prior of model.\n\n\n\n\n\nBase.rand(\n    [rng::AbstractRNG,]\n    ldf::LogDensityFunction,\n    init_strategy::AbstractInitStrategy=InitFromPrior(),\n)\n\nGenerate a random vector of parameters that is consistent with the given LogDensityFunction, using the provided initialisation strategy.\n\nNote that this function only generates parameters, and does not return the log density. If you also need the log density, instead of calling rand and then LogDensityProblems.logdensity separately (which will incur two separate model evaluations), you can directly call DynamicPPL.init!! with a VectorParamAccumulator plus the log-probability accumulators that you need, and extract both the parameters and the log density from the resulting OnlyAccsVarInfo. See the DynamicPPL documentation for an example of this.\n\n\n\n\n\n","title":"Base.rand"},{"category":"function","location":"api/#DynamicPPL.logprior","page":"API","text":"logprior(model::Model, params)\nlogprior(model::Model, varinfo::AbstractVarInfo)\n\nReturn the log prior probability of variables params for the probabilistic model, or the log prior of the data in varinfo if provided.\n\nNote that this probability always refers to the parameters in unlinked space, i.e., the return value of logprior does not depend on whether VarInfo has been linked or not.\n\nSee also logjoint and loglikelihood.\n\nExamples\n\njulia> @model function demo(x)\n           m ~ Normal()\n           for i in eachindex(x)\n               x[i] ~ Normal(m, 1.0)\n           end\n       end\ndemo (generic function with 2 methods)\n\njulia> # Using a `NamedTuple`.\n       logprior(demo([1.0]), (m = 100.0, ))\n-5000.918938533205\n\njulia> # Using a `OrderedDict`.\n       logprior(demo([1.0]), OrderedDict(@varname(m) => 100.0))\n-5000.918938533205\n\njulia> # Truth.\n       logpdf(Normal(), 100.0)\n-5000.918938533205\n\n\n\n\n\nlogprior(model::DynamicPPL.Model, chain::MCMCChains.Chains)\n\nReturn an array of log prior probabilities evaluated at each sample in an MCMC chain.\n\nExamples\n\njulia> using MCMCChains, Distributions\n\njulia> @model function demo_model(x)\n           s ~ InverseGamma(2, 3)\n           m ~ Normal(0, sqrt(s))\n           for i in eachindex(x)\n               x[i] ~ Normal(m, sqrt(s))\n           end\n       end;\n\njulia> # Construct a chain of samples using MCMCChains.\n       # This sets s = 0.5 and m = 1.0 for all three samples.\n       chain = Chains(repeat([0.5 1.0;;;], 3, 1, 1), [:s, :m]);\n\njulia> logprior(demo_model([1., 2.]), chain)\n3×1 Matrix{Float64}:\n -3.2956988239086447\n -3.2956988239086447\n -3.2956988239086447\n\n\n\n\n\n","title":"DynamicPPL.logprior"},{"category":"function","location":"api/#StatsAPI.loglikelihood","page":"API","text":"loglikelihood(model::Model, params)\nloglikelihood(model::Model, varinfo::AbstractVarInfo)\n\nReturn the log likelihood of variables params for the probabilistic model, or the log likelihood of the data in varinfo if provided.\n\nSee also logjoint and logprior.\n\nExamples\n\n```jldoctest; setup=:(using Distributions) julia> @model function demo(x)            m ~ Normal()            for i in eachindex(x)                x[i] ~ Normal(m, 1.0)            end        end demo (generic function with 2 methods)\n\njulia> # Using a NamedTuple.        loglikelihood(demo([1.0]), (m = 100.0, )) -4901.418938533205\n\njulia> # Using a OrderedDict.        loglikelihood(demo([1.0]), OrderedDict(@varname(m) => 100.0)) -4901.418938533205\n\njulia> # Truth.        logpdf(Normal(100.0, 1.0), 1.0) -4901.418938533205\n\n\n\n\n\nloglikelihood(model::DynamicPPL.Model, chain::MCMCChains.Chains)\n\nReturn an array of log likelihoods evaluated at each sample in an MCMC chain.\n\nExamples\n\njulia> using MCMCChains, Distributions\n\njulia> @model function demo_model(x)\n           s ~ InverseGamma(2, 3)\n           m ~ Normal(0, sqrt(s))\n           for i in eachindex(x)\n               x[i] ~ Normal(m, sqrt(s))\n           end\n       end;\n\njulia> # Construct a chain of samples using MCMCChains.\n       # This sets s = 0.5 and m = 1.0 for all three samples.\n       chain = Chains(repeat([0.5 1.0;;;], 3, 1, 1), [:s, :m]);\n\njulia> loglikelihood(demo_model([1., 2.]), chain)\n3×1 Matrix{Float64}:\n -2.1447298858494\n -2.1447298858494\n -2.1447298858494\n\n\n\n\n\n","title":"StatsAPI.loglikelihood"},{"category":"function","location":"api/#DynamicPPL.logjoint","page":"API","text":"logjoint(model::Model, params)\nlogjoint(model::Model, varinfo::AbstractVarInfo)\n\nReturn the log joint probability of variables params for the probabilistic model, or the log joint of the data in varinfo if provided.\n\nNote that this probability always refers to the parameters in unlinked space, i.e., the return value of logjoint does not depend on whether VarInfo has been linked or not.\n\nSee also logprior and loglikelihood.\n\nExamples\n\njulia> @model function demo(x)\n           m ~ Normal()\n           for i in eachindex(x)\n               x[i] ~ Normal(m, 1.0)\n           end\n       end\ndemo (generic function with 2 methods)\n\njulia> # Using a `NamedTuple`.\n       logjoint(demo([1.0]), (m = 100.0, ))\n-9902.33787706641\n\njulia> # Using a `OrderedDict`.\n       logjoint(demo([1.0]), OrderedDict(@varname(m) => 100.0))\n-9902.33787706641\n\njulia> # Truth.\n       logpdf(Normal(100.0, 1.0), 1.0) + logpdf(Normal(), 100.0)\n-9902.33787706641\n\n\n\n\n\nlogjoint(model::Model, chain::MCMCChains.Chains)\n\nReturn an array of log joint probabilities evaluated at each sample in an MCMC chain.\n\nExamples\n\njulia> using MCMCChains, Distributions\n\njulia> @model function demo_model(x)\n           s ~ InverseGamma(2, 3)\n           m ~ Normal(0, sqrt(s))\n           for i in eachindex(x)\n               x[i] ~ Normal(m, sqrt(s))\n           end\n       end;\n\njulia> # Construct a chain of samples using MCMCChains.\n       # This sets s = 0.5 and m = 1.0 for all three samples.\n       chain = Chains(repeat([0.5 1.0;;;], 3, 1, 1), [:s, :m]);\n\njulia> logjoint(demo_model([1., 2.]), chain)\n3×1 Matrix{Float64}:\n -5.440428709758045\n -5.440428709758045\n -5.440428709758045\n\n\n\n\n\n","title":"DynamicPPL.logjoint"},{"category":"method","location":"api/#DynamicPPL.LogDensityFunction-Tuple{Model, Any, VarNamedTuple}","page":"API","text":"DynamicPPL.LogDensityFunction(\n    model::Model,\n    getlogdensity::Any=getlogjoint_internal,\n    vecvals_or_strategy=UnlinkAll(),\n    accs::Union{NTuple{<:Any,AbstractAccumulator},AccumulatorTuple}=ldf_accs(getlogdensity);\n    adtype::Union{ADTypes.AbstractADType,Nothing}=nothing,\n    fix_transforms::Bool=false,\n)\n\nMost users of LogDensityFunction should use this constructor, which does not require passing a sample input vector x.\n\nThe first two arguments are the same as in the four-argument constructor.\n\nmodel is the DynamicPPL model for which we want to construct a LogDensityFunction.\ngetlogdensity is a callable which takes a single argument: an OnlyAccsVarInfo, and returns a Real corresponding to the log density of interest. Most of the time this is getlogjoint_internal.\n\nThe third argument can take many forms, but it essentially encodes all the necessary information to generate the RangeAndTransforms, as well as the sample input vector x that is required for the four-argument constructor.\n\nYou can pass either:\n\nvnt: a VarNamedTuple which contains vectorised representations of all the random variables in the model (i.e., it maps VarNames to TransformedValue{<:AbstractVector}s). This is useful if you already have one, either by creating a full VarInfo and accessing its values field, or by creating a OnlyAccsVarInfo with a VectorValueAccumulator and calling get_vector_values on it.\nvi: a VarInfo, in which case the vi.values field is used.\noavi: an OnlyAccsVarInfo, in which case the get_vector_values function is used to extract a VarNamedTuple of vector values from the VectorValueAccumulator inside it. If the OnlyAccsVarInfo does not contain a VectorValueAccumulator, then an error is thrown.\ntransform_strategy: by far the most convenient way. In this case, the transform strategy will be first used to generate a set of vectorised parameters, from which the relevant information will be extracted. This does come at the cost of doing one extra model evaluation. Whilst LogDensityFunction construction is unlikely to occur in performance-sensitive code paths, if you absolutely cannot pay this price, then you should generate the vectorised parameters yourself and pass them here instead.\n\nKeyword arguments\n\nThe adtype keyword argument allows you to specify an AD type for gradient preparation and calculation.\n\nThe fix_transforms keyword argument allows you to specify whether the transforms used in the LogDensityFunction should be cached at the time of construction. If so, the model is evaluated once using the provided transform strategy, and the transforms used for each variable are stored in the LogDensityFunction. This allows you to avoid the overhead of recalculating transforms during each log-density evaluation. See the documentation on fixed transforms for more information.\n\n\n\n\n\n","title":"DynamicPPL.LogDensityFunction"},{"category":"type","location":"api/#DynamicPPL.LogDensityFunction","page":"API","text":"DynamicPPL.LogDensityFunction(\n    model::Model,\n    getlogdensity::Any,\n    ranges_and_transforms::VarNamedTuple,\n    x::AbstractVector{<:Real},\n    accs::Union{NTuple{<:Any,AbstractAccumulator},AccumulatorTuple}=ldf_accs(getlogdensity);\n    adtype::Union{ADTypes.AbstractADType,Nothing}=nothing,\n)\n\nA struct which contains a model, along with all the information necessary to:\n\ncalculate its log density at a given point;\nand if adtype is provided, calculate the gradient of the log density at that point.\n\nThis information can be extracted using the LogDensityProblems.jl interface, specifically, using LogDensityProblems.logdensity and LogDensityProblems.logdensity_and_gradient. If adtype is nothing, then only logdensity is implemented. If adtype is a concrete AD backend type, then logdensity_and_gradient is also implemented.\n\nPositional arguments\n\nnote: Note\nYou should almost never need to call this particular constructor of LogDensityFunction. Instead you should prefer the more convenient constructors that do not need an input vector x.\n\nThe first argument is the DynamicPPL model.\n\nThe second argument, getlogdensity should be a callable which takes a single argument: an OnlyAccsVarInfo, and returns a Real corresponding to the log density of interest. There are several functions in DynamicPPL that are 'supported' out of the box:\n\ngetlogjoint_internal: calculate the log joint, including the log-Jacobian term for any variables that have been linked in the provided VarInfo.\ngetlogprior_internal: calculate the log prior, including the log-Jacobian term for any variables that have been linked in the provided VarInfo.\ngetlogjoint: calculate the log joint in the model space, ignoring any effects of linking\ngetlogprior: calculate the log prior in the model space, ignoring any effects of linking\ngetloglikelihood: calculate the log likelihood (this is unaffected by linking, since transforms are only applied to random variables)\n\nnote: Note\nBy default, LogDensityFunction uses getlogjoint_internal, i.e., the result of LogDensityProblems.logdensity(f, x) will depend on whether the LogDensityFunction was created with a linked or unlinked VarInfo. This is done primarily to ease interoperability with MCMC samplers.\n\nThe third argument is a VarNamedTuple which maps VarNames seen in the model to their corresponding RangeAndTransform. Each RangeAndTransform, as the name suggests, contains a range which says which indices in the vectorised parameters correspond to that variable, and a transform which says how to obtain the original (raw) value of that variable from the slice.\n\nThe fourth argument is a sample vector of parameters, which should be consistent with the ranges specified in the previous argument. This is used to determine the dimension and the expected element type of the vectorised parameters, and is also used in AD preparation. The values in the vector are not important.\n\nwarning: Compiled ReverseDiff\nFor compiled ReverseDiff, the values in the vector are used to compile the tape, and so if your model has control flow that depends on the values of the parameters, then you may find that the resulting LogDensityFunction only yields correct results for parameters that trigger the same control flow as the sample vector. In general, functions with parameter-dependent control flow should not be differentiated with compiled ReverseDiff.\n\nThe last positional argument, accs, allows you to specify an AccumulatorTuple or a tuple of AbstractAccumulators which will be used when evaluating the log density. (Note that any accumulators from the previous argument are discarded.) This argument is not mandatory: by default, this uses an internal function,DynamicPPL.ldf_accs`, which attempts to choose an appropriate set of accumulators based on which kind of log-density is being calculated.\n\nKeyword arguments\n\nIf the adtype keyword argument is provided, then this struct will also store the adtype along with other information for efficient calculation of the gradient of the log density. Note that preparing a LogDensityFunction with an AD type AutoBackend() requires the AD backend itself to have been loaded (e.g. with import Backend).\n\nFields\n\nNote that it is undefined behaviour to access any of a LogDensityFunction's fields, apart from:\n\nldf.model: The original model from which this LogDensityFunction was constructed.\nldf.adtype: The AD type used for gradient calculations, or nothing if no AD type was provided.\nldf.transform_strategy: The transform strategy that specifies the transforms for all variables in the model.\n\nFor all other fields, please use the corresponding getter functions provided in the API:\n\nget_logdensity_callable\nget_input_vector_type\nget_sample_input_vector\nget_range_and_transform\nget_all_ranges_and_transforms\n\nExtended help\n\nUp until DynamicPPL v0.38, there have been two ways of evaluating a DynamicPPL model at a given set of parameters:\n\nWith unflatten!! + evaluate!! with DefaultContext: this stores a vector of parameters inside a VarInfo's metadata, then reads parameter values from the VarInfo during evaluation.\nWith InitFromParams: this reads parameter values from a NamedTuple or a Dict, and stores them inside a VarInfo's metadata.\n\nIn general, both of these approaches work fine, but the fact that they modify the VarInfo's metadata can often be quite wasteful. In particular, it is very common that the only outputs we care about from model evaluation are those which are stored in accumulators, such as log probability densities, or raw values.\n\nTo avoid this issue, we use OnlyAccsVarInfo, which is a VarInfo that only contains accumulators. It implements enough of the AbstractVarInfo interface to not error during model evaluation.\n\nBecause OnlyAccsVarInfo does not store any parameter values, when evaluating a model with it, it is mandatory that parameters are provided from outside the VarInfo, namely via InitContext.\n\nThe main problem that we face is that it is not possible to directly implement DynamicPPL.init(rng, vn, dist, strategy) for strategy::InitFromParams{<:AbstractVector}. In particular, it is not clear:\n\nwhich parts of the vector correspond to which random variables, and\nwhether the variables are linked or unlinked.\n\nTraditionally, this problem has been solved by unflatten!!, because that function would place values into the VarInfo's metadata alongside the information about ranges and linking. That way, when we evaluate with DefaultContext, we can read this information out again. However, we want to avoid using a metadata. Thus, here, we extract this information from the VarInfo a single time when constructing a LogDensityFunction object. Inside the LogDensityFunction, we store a mapping from VarNames to ranges in that vector, along with link status.\n\nWhen evaluating the model, this allows us to combine the parameter vector together with those ranges to create an InitFromVector, which lets us very quickly read parameter values from the vector.\n\nNote that this assumes that the ranges and link status are static throughout the lifetime of the LogDensityFunction object. Therefore, a LogDensityFunction object cannot handle models which have variable numbers of parameters, or models which may visit random variables in different orders depending on stochastic control flow. Indeed, silent errors may occur with such models. This is a general limitation of vectorised parameters: the original unflatten!! + evaluate!! approach also fails with such models.\n\n\n\n\n\n","title":"DynamicPPL.LogDensityFunction"},{"category":"type","location":"api/#DynamicPPL.RangeAndTransform","page":"API","text":"RangeAndTransform\n\nSuppose we have a set of vectorised parameters params::AbstractVector{<:Real} for a Turing model. Each random variable in the model will in general correspond to a sub-vector of params. This struct stores information about that range, as well as the transform that was applied to that variable, which determines how the variable is interpreted.\n\nrange::UnitRange{Int64}\ntransform::Any\n\n\n\n\n\n","title":"DynamicPPL.RangeAndTransform"},{"category":"function","location":"api/#DynamicPPL.get_range_and_transform","page":"API","text":"DynamicPPL.get_range_and_transform(ldf::LogDensityFunction, vn::VarName)::RangeAndTransform\n\nA LogDensityFunction stores a mapping from VarNames to their corresponding ranges in the vectorised parameter representation, along with their transform status. This function retrieves that information for a single VarName.\n\n\n\n\n\n","title":"DynamicPPL.get_range_and_transform"},{"category":"function","location":"api/#DynamicPPL.get_all_ranges_and_transforms","page":"API","text":"DynamicPPL.get_all_ranges_and_transforms(ldf::LogDensityFunction)::VarNamedTuple\n\nA LogDensityFunction stores a mapping from VarNames to their corresponding ranges in the vectorised parameter representation, along with their transform status. This function retrieves the complete mapping.\n\n\n\n\n\n","title":"DynamicPPL.get_all_ranges_and_transforms"},{"category":"function","location":"api/#DynamicPPL.get_logdensity_callable","page":"API","text":"DynamicPPL.get_logdensity_callable(ldf::LogDensityFunction)\n\nA LogDensityFunction stores a callable that, given an OnlyAccsVarInfo, can be used to calculate the log density of the model at a given set of parameters. For example, most usecases in DynamicPPL use DynamicPPL.getlogjoint_internal for this purpose.\n\nThis function retrieves that callable.\n\n\n\n\n\n","title":"DynamicPPL.get_logdensity_callable"},{"category":"function","location":"api/#DynamicPPL.get_input_vector_type","page":"API","text":"DynamicPPL.get_input_vector_type(::LogDensityFunction)\n\nGet the type of the vector x that should be passed to LogDensityProblems.logdensity(ldf, x).\n\nNote that if you pass a vector of a different type, it will be converted to the correct type. This allows you however to determine upfront what kind of vector should be passed in. It is also useful for determining e.g. whether Float32 or Float64 parameters are expected.\n\n\n\n\n\n","title":"DynamicPPL.get_input_vector_type"},{"category":"function","location":"api/#DynamicPPL.get_sample_input_vector","page":"API","text":"DynamicPPL.get_sample_input_vector(::LogDensityFunction)::AbstractVector{<:Real}\n\nGet the sample input vector x used to construct the LogDensityFunction.\n\n\n\n\n\n","title":"DynamicPPL.get_sample_input_vector"},{"category":"type","location":"api/#DynamicPPL.OnlyAccsVarInfo","page":"API","text":"OnlyAccsVarInfo(accs...)\n\nOnlyAccsVarInfo is a wrapper around a tuple of accumulators.\n\nIts name stems from the fact that it implements the minimal AbstractVarInfo interface to work with the tilde_assume!! and tilde_observe!! functions for InitContext.\n\nNote that this does not implement almost every other AbstractVarInfo interface function, and so using this with a different leaf context such as DefaultContext will result in errors.\n\nFor more information about accumulators, please see the DynamicPPL documentation on accumulators.\n\n\n\n\n\n","title":"DynamicPPL.OnlyAccsVarInfo"},{"category":"function","location":"api/#DynamicPPL.to_vector_params","page":"API","text":"to_vector_params(\n    vector_values::VarNamedTuple,\n    ldf::LogDensityFunction\n)\n\nExtract vectorised values from a VarNamedTuple, and concatenate them into a single vector that is consistent with the ranges specified in the LogDensityFunction.\n\nThis is useful when you want to regenerate new vectorised parameters but using a different initialisation strategy.\n\nNote that the transform status of the variables in the VarNamedTuple must be consistent with the transform strategy stored in the LogDensityFunction. This function checks for that.\n\n\n\n\n\n","title":"DynamicPPL.to_vector_params"},{"category":"method","location":"api/#Base.rand-Tuple{AbstractRNG, LogDensityFunction, AbstractInitStrategy}","page":"API","text":"Base.rand(\n    [rng::AbstractRNG,]\n    ldf::LogDensityFunction,\n    init_strategy::AbstractInitStrategy=InitFromPrior(),\n)\n\nGenerate a random vector of parameters that is consistent with the given LogDensityFunction, using the provided initialisation strategy.\n\nNote that this function only generates parameters, and does not return the log density. If you also need the log density, instead of calling rand and then LogDensityProblems.logdensity separately (which will incur two separate model evaluations), you can directly call DynamicPPL.init!! with a VectorParamAccumulator plus the log-probability accumulators that you need, and extract both the parameters and the log density from the resulting OnlyAccsVarInfo. See the DynamicPPL documentation for an example of this.\n\n\n\n\n\n","title":"Base.rand"},{"category":"method","location":"api/#Base.:|-Tuple{Model, Union{Tuple, AbstractDict{<:VarName}, NamedTuple}}","page":"API","text":"model | (x = 1.0, ...)\n\nReturn a Model which now treats variables on the right-hand side as observations.\n\nSee condition for more information and examples.\n\n\n\n\n\n","title":"Base.:|"},{"category":"function","location":"api/#AbstractPPL.condition","page":"API","text":"condition(model::Model; values...)\ncondition(model::Model, values::NamedTuple)\n\nReturn a Model which now treats the variables in values as observations.\n\nSee also: decondition, conditioned\n\nLimitations\n\nThis does currently not work with variables that are provided to the model as arguments, e.g. @model function demo(x) ... end means that condition will not affect the variable x.\n\nTherefore if one wants to make use of condition and decondition one should not be specifying any random variables as arguments.\n\nThis is done for the sake of backwards compatibility.\n\nExamples\n\nSimple univariate model\n\njulia> using Distributions\n\njulia> @model function demo()\n           m ~ Normal()\n           x ~ Normal(m, 1)\n           return (; m=m, x=x)\n       end\ndemo (generic function with 2 methods)\n\njulia> model = demo();\n\njulia> m, x = model(); (m != 1.0 && x != 100.0)\ntrue\n\njulia> # Create a new instance which treats `x` as observed\n       # with value `100.0`, and similarly for `m=1.0`.\n       conditioned_model = condition(model, x=100.0, m=1.0);\n\njulia> m, x = conditioned_model(); (m == 1.0 && x == 100.0)\ntrue\n\njulia> # Let's only condition on `x = 100.0`.\n       conditioned_model = condition(model, x = 100.0);\n\njulia> m, x = conditioned_model(); (m != 1.0 && x == 100.0)\ntrue\n\njulia> # We can also use the nicer `|` syntax.\n       conditioned_model = model | (x = 100.0, );\n\njulia> m, x = conditioned_model(); (m != 1.0 && x == 100.0)\ntrue\n\nIn the above we have specified the conditioning variables via keyword arguments. You can also provide a NamedTuple, AbstractDict{<:VarName}, or a VarNamedTuple; internally these are all converted to a VarNamedTuple.\n\nFor example, here we use a Dict:\n\njulia> conditioned_model_dict = condition(model, Dict(@varname(x) => 100.0));\n\njulia> m, x = conditioned_model_dict(); (m != 1.0 && x == 100.0)\ntrue\n\njulia> # There's also an option using `|` by letting the right-hand side be a tuple\n       # with elements of type `Pair{<:VarName}`, i.e. `vn => value` with `vn isa VarName`.\n       conditioned_model_pairs = model | (@varname(x) => 100.0);\n\njulia> m, x = conditioned_model_pairs(); (m != 1.0 && x == 100.0)\ntrue\n\nCondition only a part of a multivariate variable\n\nWhen conditioning on multiple variables at a time, we can use missing to signal that a part of the variable should not be conditioned on.\n\nHowever, note that in this case each element of the multivariate random variable must be on its own tilde-statement. In other words, if we write m ~ MvNormal(...), then we cannot condition on only m[1]. (In principle, for some distributions this can be possible, specifically when the distribution can be factorised into independent components, like an MvNormal with a diagonal covariance matrix. However, this is not currently implemented.)\n\njulia> @model function demo_mv(::Type{TV}=Float64) where {TV}\n           m = Vector{TV}(undef, 2)\n           m[1] ~ Normal()\n           m[2] ~ Normal()\n           return m\n       end\ndemo_mv (generic function with 4 methods)\n\njulia> model = demo_mv();\n\njulia> conditioned_model = condition(model, m = [missing, 1.0]);\n\njulia> # (✓) `m[1]` sampled while `m[2]` is fixed\n       m = conditioned_model(); (m[1] != 1.0 && m[2] == 1.0)\ntrue\n\nIntuitively one might also expect to be able to write model | (m[2] = 1.0, ). You cannot do this with a NamedTuple because the VarName m[2] cannot be represented as a Symbol (i.e., Symbol(\"m[2]\") is not the same as @varname(m[2])).\n\njulia> # (×) `m[2]` is not set to 1.0.\n       m = condition(model, var\"m[2]\" = 1.0)(); m[2] == 1.0\nfalse\n\nBut you can do this if you use a Dict or a VarNamedTuple as the underlying storage instead:\n\njulia> vnt = @vnt begin\n           @template m = zeros(2)\n           m[2] := 1.0\n       end\nVarNamedTuple\n└─ m => PartialArray size=(2,) data::Vector{Float64}\n        └─ (2,) => 1.0\n\njulia> m = condition(model, vnt)(); (m[1] != 1.0 && m[2] == 1.0)\ntrue\n\nNested models\n\ncondition also supports the use of nested models through the use of to_submodel.\n\njulia> @model demo_inner() = m ~ Normal()\ndemo_inner (generic function with 2 methods)\n\njulia> @model function demo_outer()\n           # By default, `to_submodel` prefixes the variables using the left-hand side of `~`.\n           inner ~ to_submodel(demo_inner())\n           return inner\n       end\ndemo_outer (generic function with 2 methods)\n\njulia> model = demo_outer();\n\njulia> model() ≠ 1.0\ntrue\n\njulia> # To condition the variable inside `demo_inner` we need to refer to it as `inner.m`.\n       conditioned_model = model | (@varname(inner.m) => 1.0, );\n\njulia> conditioned_model()\n1.0\n\njulia> # If you attempt to condition on `inner` itself, it must refer to the prefixed\n       # latent variables, not the return value. For example, this will work:\n       conditioned_model2 = model | (inner = (m = 1.0,), );\n\njulia> conditioned_model2()\n1.0\n\njulia> # However, if `inner` does not contain `m` inside it as a field, this will not\n       # result in any conditioning:\n       conditioned_model_fail = model | (inner = \"something else\", );\n\njulia> conditioned_model_fail == 1.0\nfalse\n\n\n\n\n\n","title":"AbstractPPL.condition"},{"category":"function","location":"api/#DynamicPPL.conditioned","page":"API","text":"conditioned(context::AbstractContext)\n\nReturn VarNamedTuple of values that are conditioned on under context.\n\nNote that this will recursively traverse the context stack and return a merged version of the condition values.\n\n\n\n\n\nconditioned(model::Model)\n\nReturn the conditioned values in model.\n\nExamples\n\njulia> using Distributions\n\njulia> using DynamicPPL: conditioned, contextualize, PrefixContext, CondFixContext, Condition\n\njulia> @model function demo()\n           m ~ Normal()\n           x ~ Normal(m, 1)\n       end\ndemo (generic function with 2 methods)\n\njulia> m = demo();\n\njulia> # Returns all the variables we have conditioned on + their values.\n       conditioned(condition(m, x=100.0, m=1.0))\nVarNamedTuple\n├─ x => 100.0\n└─ m => 1.0\n\njulia> # Nested ones also work. (Note that `PrefixContext` also prefixes\n       # the variables of any `CondFixContext` that is _inside_ it.)\n       new_context = PrefixContext(@varname(a), CondFixContext{Condition}(VarNamedTuple(m=1.0,)));\n       cm = condition(contextualize(m, new_context), x=100.0);\n\njulia> conditioned(cm)\nVarNamedTuple\n├─ a => VarNamedTuple\n│       └─ m => 1.0\n└─ x => 100.0\n\njulia> # Since we conditioned on `a.m`, it is not treated as a random variable.\n       # However, `a.x` will still be a random variable.\n       keys(VarInfo(cm))\n1-element Vector{VarName}:\n a.x\n\njulia> # We can also condition on `a.m` _outside_ of the PrefixContext:\n       cm = condition(contextualize(m, PrefixContext(@varname(a))), (@varname(a.m) => 1.0));\n\njulia> conditioned(cm)\nVarNamedTuple\n└─ a => VarNamedTuple\n        └─ m => 1.0\n\njulia> # Now `a.x` will be sampled.\n       keys(VarInfo(cm))\n1-element Vector{VarName}:\n a.x\n\n\n\n\n\n","title":"DynamicPPL.conditioned"},{"category":"function","location":"api/#AbstractPPL.decondition","page":"API","text":"decondition(model::Model)\ndecondition(model::Model, variables...)\n\nReturn a Model for which variables... are not conditioned on. If no variables are provided, then all conditioned variables will be removed.\n\nNote that this function cannot decondition variables that are provided as arguments to the model function itself; it can only decondition variables that were provided via condition or |.\n\nThis is essentially the inverse of condition.\n\nExamples\n\njulia> using Distributions\n\njulia> @model function demo()\n           m ~ Normal()\n           x ~ Normal(m, 1)\n           return (; m=m, x=x)\n       end\ndemo (generic function with 2 methods)\n\njulia> conditioned_model = condition(demo(), m = 1.0, x = 10.0);\n\njulia> conditioned_model()\n(m = 1.0, x = 10.0)\n\njulia> # By specifying the `VarName` to `decondition`.\n       model = decondition(conditioned_model, @varname(m));\n\njulia> (m, x) = model(); (m ≠ 1.0 && x == 10.0)\ntrue\n\njulia> # `decondition` also accepts symbols, although VarNames are preferable for\n       # type stability reasons.\n       model = decondition(conditioned_model, :m);\n\njulia> (m, x) = model(); (m ≠ 1.0 && x == 10.0)\ntrue\n\njulia> # `decondition` multiple at once:\n       (m, x) = decondition(model, :m, :x)(); (m ≠ 1.0 && x ≠ 10.0)\ntrue\n\njulia> # `decondition` without any symbols will `decondition` all variables.\n       (m, x) = decondition(model)(); (m ≠ 1.0 && x ≠ 10.0)\ntrue\n\nNote that decondition is only guaranteed to work when you decondition variables that were explicitly provided to condition earlier. In this example we condition on @varname(m) but decondition on @varname(m[1]), which fails because m[1] was not explicitly conditioned on:\n\njulia> @model function demo_mv(::Type{TV}=Float64) where {TV}\n           m = Vector{TV}(undef, 2)\n           m[1] ~ Normal()\n           m[2] ~ Normal()\n           return m\n       end\ndemo_mv (generic function with 4 methods)\n\njulia> model = demo_mv();\n\njulia> conditioned_model = condition(model, @varname(m) => [1.0, 2.0]);\n\njulia> conditioned_model()\n2-element Vector{Float64}:\n 1.0\n 2.0\n\njulia> deconditioned_model = decondition(conditioned_model, @varname(m[1]));\n\njulia> deconditioned_model()  # (×) `m[1]` is still conditioned\n2-element Vector{Float64}:\n 1.0\n 2.0\n\n\n\n\n\n","title":"AbstractPPL.decondition"},{"category":"function","location":"api/#DynamicPPL.fix","page":"API","text":"fix(model::Model; values...)\nfix(model::Model, values::NamedTuple)\n\nReturn a Model which now treats the variables in values as fixed.\n\nSee also: unfix, fixed\n\nExamples\n\nSimple univariate model\n\njulia> using Distributions\n\njulia> @model function demo()\n           m ~ Normal()\n           x ~ Normal(m, 1)\n           return (; m=m, x=x)\n       end\ndemo (generic function with 2 methods)\n\njulia> model = demo();\n\njulia> m, x = model(); (m ≠ 1.0 && x ≠ 100.0)\ntrue\n\njulia> # Create a new instance which treats `x` as observed\n       # with value `100.0`, and similarly for `m=1.0`.\n       fixed_model = fix(model, x=100.0, m=1.0);\n\njulia> m, x = fixed_model(); (m == 1.0 && x == 100.0)\ntrue\n\njulia> # Let's only fix on `x = 100.0`.\n       fixed_model = fix(model, x = 100.0);\n\njulia> m, x = fixed_model(); (m != 1.0 && x == 100.0)\ntrue\n\nOther ways of specifying fixed values\n\nSpecifying fixed values can be done exactly in the same way as for condition; please see its docstring for more examples.\n\nDifference from condition\n\nThe only difference between fixing and conditioning is as follows:\n\nConditioned variables are considered to be observations, and are thus included in the computation log-joint and log-likelihood, but not the log-prior.\nFixed variables are considered to be constant, and are thus not included in any log-probability computations.\n\njulia> @model function demo()\n           m ~ Normal()\n           x ~ Normal(m, 1)\n           return (; m=m, x=x)\n       end\ndemo (generic function with 2 methods)\n\njulia> model = demo();\n\njulia> model_fixed = fix(model, m = 1.0);\n\njulia> model_conditioned = condition(model, m = 1.0);\n\njulia> logjoint(model_fixed, (x=1.0,))\n-0.9189385332046728\n\njulia> logjoint(model_conditioned, (x=1.0,))\n-2.3378770664093453\n\njulia> # The difference is the missing log-probability of `m`:\n       logpdf(Normal(), 1.0)\n-1.4189385332046727\n\n\n\n\n\n","title":"DynamicPPL.fix"},{"category":"function","location":"api/#DynamicPPL.fixed","page":"API","text":"fixed(context::AbstractContext)\n\nReturn a VarNamedTuple containing the values that are fixed under context.\n\nNote that this will recursively traverse the context stack and return a merged version of the fix values.\n\n\n\n\n\nfixed(model::Model)\n\nReturn the fixed values in model.\n\nExamples\n\njulia> using Distributions\n\njulia> using DynamicPPL: fixed, contextualize, PrefixContext, CondFixContext, Fix\n\njulia> @model function demo()\n           m ~ Normal()\n           x ~ Normal(m, 1)\n       end\ndemo (generic function with 2 methods)\n\njulia> m = demo();\n\njulia> # Returns all the variables we have fixed on + their values.\n       fixed(fix(m, x=100.0, m=1.0))\nVarNamedTuple\n├─ x => 100.0\n└─ m => 1.0\n\njulia> # The rest of this is the same as the `condition` example above.\n       fm = fix(contextualize(m, PrefixContext(@varname(a), CondFixContext{Fix}(VarNamedTuple(m=1.0)))), x=100.0);\n\njulia> Set(keys(fixed(fm))) == Set([@varname(a.m), @varname(x)])\ntrue\n\njulia> keys(VarInfo(fm))\n1-element Vector{VarName}:\n a.x\n\njulia> # We can also fix `a.m` _outside_ of the PrefixContext:\n       fm = fix(contextualize(m, PrefixContext(@varname(a))), (@varname(a.m) => 1.0));\n\njulia> fixed(fm)\nVarNamedTuple\n└─ a => VarNamedTuple\n        └─ m => 1.0\n\njulia> # Now `a.x` will be sampled.\n       keys(VarInfo(fm))\n1-element Vector{VarName}:\n a.x\n\n\n\n\n\n","title":"DynamicPPL.fixed"},{"category":"function","location":"api/#DynamicPPL.unfix","page":"API","text":"unfix(model::Model)\nunfix(model::Model, variables...)\n\nReturn a Model for which variables... are not considered fixed. If no variables are provided, then all fixed variables will be removed.\n\nThis is essentially the inverse of fix.\n\nConceptually this is very similar to decondition and thus the same limitations apply; please see its docstring for more details.\n\nExamples\n\njulia> using Distributions\n\njulia> @model function demo()\n           m ~ Normal()\n           x ~ Normal(m, 1)\n           return (; m=m, x=x)\n       end\ndemo (generic function with 2 methods)\n\njulia> fixed_model = fix(demo(), m = 1.0, x = 10.0);\n\njulia> fixed_model()\n(m = 1.0, x = 10.0)\n\njulia> # By specifying the `VarName` to `unfix`.\n       model = unfix(fixed_model, @varname(m));\n\njulia> (m, x) = model(); (m != 1.0 && x == 10.0)\ntrue\n\njulia> # When `NamedTuple` is used as the underlying, you can also provide\n       # the symbol directly (though the `@varname` approach is preferable if\n       # if the variable is known at compile-time).\n       model = unfix(fixed_model, :m);\n\njulia> (m, x) = model(); (m != 1.0 && x == 10.0)\ntrue\n\njulia> # `unfix` multiple at once:\n       (m, x) = unfix(model, :m, :x)(); (m != 1.0 && x != 10.0)\ntrue\n\njulia> # `unfix` without any symbols will `unfix` all variables.\n       (m, x) = unfix(model)(); (m != 1.0 && x != 10.0)\ntrue\n\n\n\n\n\n","title":"DynamicPPL.unfix"},{"category":"function","location":"api/#StatsAPI.predict","page":"API","text":"predict([rng::AbstractRNG,] model::Model, chain::MCMCChains.Chains; include_all=false)\n\nSample from the posterior predictive distribution by executing model with parameters fixed to each sample in chain, and return the resulting Chains.\n\nThe model passed to predict is often different from the one used to generate chain. Typically, the model from which chain originated treats certain variables as observed (i.e., data points), while the model you pass to predict may mark these same variables as missing or unobserved. Calling predict then leverages the previously inferred parameter values to simulate what new, unobserved data might look like, given your posterior beliefs.\n\nFor each parameter configuration in chain:\n\nAll random variables present in chain are fixed to their sampled values.\nAny variables not included in chain are sampled from their prior distributions.\n\nIf include_all is false, the returned Chains will contain only those variables that were not fixed by the samples in chain. This is useful when you want to sample only new variables from the posterior predictive distribution.\n\nExamples\n\nusing AbstractMCMC, Distributions, DynamicPPL, Random\n\n@model function linear_reg(x, y, σ = 0.1)\n    β ~ Normal(0, 1)\n    for i in eachindex(y)\n        y[i] ~ Normal(β * x[i], σ)\n    end\nend\n\n# Generate synthetic chain using known ground truth parameter\nground_truth_β = 2.0\n\n# Create chain of samples from a normal distribution centered on ground truth\nβ_chain = MCMCChains.Chains(\n    rand(Normal(ground_truth_β, 0.002), 1000), [:β,]\n)\n\n# Generate predictions for two test points\nxs_test = [10.1, 10.2]\n\nm_train = linear_reg(xs_test, fill(missing, length(xs_test)))\n\npredictions = DynamicPPL.AbstractPPL.predict(\n    Random.default_rng(), m_train, β_chain\n)\n\nys_pred = vec(mean(Array(predictions); dims=1))\n\n# Check if predictions match expected values within tolerance\n(\n    isapprox(ys_pred[1], ground_truth_β * xs_test[1], atol = 0.01),\n    isapprox(ys_pred[2], ground_truth_β * xs_test[2], atol = 0.01)\n)\n\n# output\n\n(true, true)\n\n\n\n\n\n","title":"StatsAPI.predict"},{"category":"function","location":"api/#DynamicPPL.marginalize","page":"API","text":"DynamicPPL.marginalize(\n    model::DynamicPPL.Model,\n    marginalized_varnames::AbstractVector{<:VarName};\n    transform_strategy::DynamicPPL.AbstractTransformStrategy=DynamicPPL.LinkAll(),\n    getlogprob=DynamicPPL.getlogjoint,\n    method::MarginalLogDensities.AbstractMarginalizer=MarginalLogDensities.LaplaceApprox();\n    kwargs...,\n)\n\nConstruct a MarginalLogDensities.MarginalLogDensity object that represents the marginal log-density of the given model, after marginalizing out the variables specified in varnames.\n\nThe resulting object can be called with a vector of parameter values to compute the marginal log-density.\n\nKeyword arguments\n\ntransform_strategy: The transform strategy to use for the model, which determines  whether the marginalisation is performed in the original (possibly constrained) space  or in a transformed (unconstrained) space. By default, this is DynamicPPL.LinkAll(),  which transforms all variables to unconstrained space.\nTo avoid this transformation and perform the marginalisation in the original space, use  DynamicPPL.UnlinkAll(). You can also use fixed transforms which can in specific  circumstances improve performance: see the DynamicPPL documentation for more details.\ngetlogprob: A function which specifies which kind of marginal log-density to compute.  Its default value is DynamicPPL.getlogjoint which returns the marginal log-joint  probability in the original space (i.e., log-Jacobians from the transformation to  unconstrained space are ignored).\nmethod: The marginalization method; defaults to a Laplace approximation. Please see the  MarginalLogDensities.jl package  for other options.\nOther keyword arguments are passed to the MarginalLogDensities.MarginalLogDensity constructor.\n\nExample\n\njulia> using DynamicPPL, Distributions, MarginalLogDensities\n\njulia> @model function demo()\n           x ~ Normal(1.0)\n           y ~ Normal(2.0)\n       end\ndemo (generic function with 2 methods)\n\njulia> marginalized = marginalize(demo(), [@varname(x)]);\n\njulia> # The resulting callable computes the marginal log-density of `y`.\n       marginalized([1.0])\n-1.4189385332046727\n\njulia> logpdf(Normal(2.0), 1.0)\n-1.4189385332046727\n\nwarning: Warning\nThe default usage of DynamicPPL.LinkAll() means that, for example, optimization of the marginal log-density can be performed in unconstrained space. However, care must be taken if the model contains variables where the link transformation depends on a marginalized variable. For example:@model function f()\n    x ~ Normal()\n    y ~ truncated(Normal(); lower=x)\nendHere, the support of y, and hence the link transformation used, depends on the value of x. If we now marginalize over x, we obtain a function mapping linked values of y to log-probabilities. However, it will not be possible to use DynamicPPL to correctly retrieve unlinked values of y.\n\n\n\n\n\n","title":"DynamicPPL.marginalize"},{"category":"function","location":"api/#DynamicPPL.to_submodel","page":"API","text":"to_submodel(model::Model[, auto_prefix::Bool])\n\nReturn a model wrapper indicating that it is a sampleable model over the return-values.\n\nThis is mainly meant to be used on the right-hand side of a ~ operator to indicate that the model can be sampled from but not necessarily evaluated for its log density.\n\nwarning: Warning\nNote that some other operations that one typically associate with expressions of the form left ~ right such as condition, will also not work with to_submodel.\n\nwarning: Warning\nTo avoid variable names clashing between models, it is recommended to leave the argument auto_prefix equal to true. If one does not use automatic prefixing, then it's recommended to use prefix(::Model, input) explicitly, i.e. to_submodel(prefix(model, @varname(my_prefix)))\n\nArguments\n\nmodel::Model: the model to wrap.\nauto_prefix::Bool: whether to automatically prefix the variables in the model using the left-hand   side of the ~ statement. Default: true.\n\nExamples\n\njulia> using DynamicPPL, Distributions\n\njulia> @model function demo1(x)\n           x ~ Normal()\n           return 1 + abs(x)\n       end;\n\njulia> @model function demo2(x, y)\n            a ~ to_submodel(demo1(x))\n            return y ~ Uniform(0, a)\n       end;\n\nWhen we sample from the model demo2(missing, 0.4) the random variable x will be sampled, but it will be prefixed with a (the left-hand side of the tilde):\n\njulia> model = demo2(missing, 0.4);\n\njulia> haskey(rand(model), @varname(a.x))\ntrue\n\nThe variable a will be assigned the return value of demo1, and can be used in subsequent lines of the model, e.g. in the definition of y above.\n\nWe can verify that the log joint probability of the model accumulated in vi is correct:\n\njulia> accs = setacc!!(OnlyAccsVarInfo(), RawValueAccumulator(false));\n\njulia> _, accs = init!!(model, accs, InitFromPrior(), UnlinkAll());\n\njulia> x = get_raw_values(accs)[@varname(a.x)];\n\njulia> getlogjoint(accs) ≈ logpdf(Normal(), x) + logpdf(Uniform(0, 1 + abs(x)), 0.4)\ntrue\n\nWithout automatic prefixing\n\nAs mentioned earlier, by default, the auto_prefix argument specifies whether to automatically prefix the variables in the submodel. If auto_prefix=false, then the variables in the submodel will not be prefixed.\n\njulia> @model function demo1(x)\n           x ~ Normal()\n           return 1 + abs(x)\n       end;\n\njulia> @model function demo2_no_prefix(x, z)\n            a ~ to_submodel(demo1(x), false)\n            return z ~ Uniform(-a, 1)\n       end;\n\njulia> model = demo2_no_prefix(missing, 0.4);\n\njulia> haskey(rand(model), @varname(x))  # here we just use `x` instead of `a.x`\ntrue\n\nHowever, not using prefixing is generally not recommended as it can lead to variable name clashes unless one is careful. For example, if the same submodel is used multiple times in a model, not using prefixing will lead to variable name clashes.\n\nOne can manually specify a prefix using prefix(::Model, prefix_varname):\n\njulia> @model function demo2(x, y, z)\n            a ~ to_submodel(prefix(demo1(x), @varname(sub1)), false)\n            b ~ to_submodel(prefix(demo1(y), @varname(sub2)), false)\n            return z ~ Uniform(-a, b)\n       end;\n\njulia> model = demo2(missing, missing, 0.4);\n\njulia> haskey(rand(model), @varname(sub1.x))\ntrue\n\njulia> haskey(rand(model), @varname(sub2.x))\ntrue\n\n\n\n\n\n","title":"DynamicPPL.to_submodel"},{"category":"function","location":"api/#DynamicPPL.prefix","page":"API","text":"prefix(ctx::AbstractContext, vn::VarName)\n\nApply the prefixes in the context ctx to the variable name vn.\n\n\n\n\n\nprefix(model::Model, x::VarName)\nprefix(model::Model, x::Val{sym})\nprefix(model::Model, x::Any)\n\nReturn model but with all random variables prefixed by x, where x is either:\n\na VarName (e.g. @varname(a)),\na Val{sym} (e.g. Val(:a)), or\nfor any other type, x is converted to a Symbol and then to a VarName. Note that this will introduce runtime overheads so is not recommended unless absolutely necessary.\n\nExamples\n\njulia> using DynamicPPL: prefix\n\njulia> @model demo() = x ~ Dirac(1)\ndemo (generic function with 2 methods)\n\njulia> rand(prefix(demo(), @varname(my_prefix)))\nVarNamedTuple\n└─ my_prefix => VarNamedTuple\n                └─ x => 1\n\njulia> rand(prefix(demo(), Val(:my_prefix)))\nVarNamedTuple\n└─ my_prefix => VarNamedTuple\n                └─ x => 1\n\n\n\n\n\n","title":"DynamicPPL.prefix"},{"category":"macro","location":"api/#DynamicPPL.@addlogprob!","page":"API","text":"@addlogprob!(ex)\n\nAdd a term to the log joint.\n\nIf ex evaluates to a NamedTuple with keys :loglikelihood and/or :logprior, the values are added to the log likelihood and log prior respectively.\n\nIf ex evaluates to a number it is added to the log likelihood.\n\nExamples\n\njulia> mylogjoint(x, μ) = (; loglikelihood=loglikelihood(Normal(μ, 1), x), logprior=1.0);\n\njulia> @model function demo(x)\n           μ ~ Normal()\n           @addlogprob! mylogjoint(x, μ)\n       end;\n\njulia> x = [1.3, -2.1];\n\njulia> loglikelihood(demo(x), (μ=0.2,)) ≈ mylogjoint(x, 0.2).loglikelihood\ntrue\n\njulia> logprior(demo(x), (μ=0.2,)) ≈ logpdf(Normal(), 0.2) + mylogjoint(x, 0.2).logprior\ntrue\n\nand to reject samples:\n\njulia> @model function demo(x)\n           m ~ MvNormal(zero(x), I)\n           if dot(m, x) < 0\n               @addlogprob! (; loglikelihood=-Inf)\n               # Exit the model evaluation early\n               return\n           end\n           x ~ MvNormal(m, I)\n           return\n       end;\n\njulia> logjoint(demo([-2.1]), (m=[0.2],)) == -Inf\ntrue\n\n\n\n\n\n","title":"DynamicPPL.@addlogprob!"},{"category":"method","location":"api/#DynamicPPL.returned-Tuple{Model, Chains}","page":"API","text":"returned(model::Model, chain::MCMCChains.Chains)\n\nExecute model for each of the samples in chain and return an array of the values returned by the model for each sample.\n\nExamples\n\nGeneral\n\nOften you might have additional quantities computed inside the model that you want to inspect, e.g.\n\n@model function demo(x)\n    # sample and observe\n    θ ~ Prior()\n    x ~ Likelihood()\n    return interesting_quantity(θ, x)\nend\nm = demo(data)\nchain = sample(m, alg, n)\n# To inspect the `interesting_quantity(θ, x)` where `θ` is replaced by samples\n# from the posterior/`chain`:\nreturned(m, chain) # <= results in a `Vector` of returned values\n                               #    from `interesting_quantity(θ, x)`\n\nConcrete (and simple)\n\njulia> using Turing\n\njulia> @model function demo(xs)\n           s ~ InverseGamma(2, 3)\n           m_shifted ~ Normal(10, √s)\n           m = m_shifted - 10\n\n           for i in eachindex(xs)\n               xs[i] ~ Normal(m, √s)\n           end\n\n           return (m, )\n       end\ndemo (generic function with 1 method)\n\njulia> model = demo(randn(10));\n\njulia> chain = sample(model, MH(), 10);\n\njulia> returned(model, chain)\n10×1 Array{Tuple{Float64},2}:\n (2.1964758025119338,)\n (2.1964758025119338,)\n (0.09270081916291417,)\n (0.09270081916291417,)\n (0.09270081916291417,)\n (0.09270081916291417,)\n (0.09270081916291417,)\n (0.043088571494005024,)\n (-0.16489786710222099,)\n (-0.16489786710222099,)\n\n\n\n\n\n","title":"DynamicPPL.returned"},{"category":"method","location":"api/#DynamicPPL.returned-Tuple{Model, Union{AbstractDict{<:VarName}, NamedTuple}}","page":"API","text":"returned(model::Model, parameters...)\n\nInitialise a model using the given parameters and return the model's return value. The parameters must be provided in a format that can be wrapped in an InitFromParams, i.e., InitFromParams(parameters..., nothing) must be a valid AbstractInitStrategy (where nothing is the fallback strategy to use if parameters are not provided).\n\nAs far as DynamicPPL is concerned, parameters can be either a singular NamedTuple or an AbstractDict{<:VarName}; however this method is left flexible to allow for other packages that wish to extend InitFromParams.\n\nExample\n\njulia> using DynamicPPL, Distributions\n\njulia> @model function demo()\n           m ~ Normal()\n           return (mp1 = m + 1,)\n       end\ndemo (generic function with 2 methods)\n\njulia> model = demo();\n\njulia> returned(model, (; m = 1.0))\n(mp1 = 2.0,)\n\njulia> returned(model, Dict{VarName,Float64}(@varname(m) => 2.0))\n(mp1 = 3.0,)\n\n\n\n\n\n","title":"DynamicPPL.returned"},{"category":"function","location":"api/#DynamicPPL.pointwise_logdensities","page":"API","text":"DynamicPPL.pointwise_logdensities(\n    model::Model,\n    init_strat::AbstractInitStrategy\n)\n\nCalculate the pointwise log-densities for the parameters obtained by evaluating the model with the given initialisation strategy. The resulting VarNamedTuple will contain both log-prior probabilities (for random variables) and log-likelihoods (for observed variables).\n\n\n\n\n\nDynamicPPL.pointwise_logdensities(\n    model::DynamicPPL.Model,\n    chain::MCMCChains.Chains,\n)\n\nRuns model on each sample in chain, returning a new MCMCChains.Chains object where the log-density of each variable at each sample is stored (rather than its value).\n\nSee also: DynamicPPL.pointwise_loglikelihoods, DynamicPPL.pointwise_prior_logdensities.\n\nExamples\n\njulia> using MCMCChains\n\njulia> @model function demo(xs, y)\n           s ~ InverseGamma(2, 3)\n           m ~ Normal(0, √s)\n           for i in eachindex(xs)\n               xs[i] ~ Normal(m, √s)\n           end\n           y ~ Normal(m, √s)\n       end\ndemo (generic function with 2 methods)\n\njulia> # Example observations.\n       model = demo([1.0, 2.0, 3.0], [4.0]);\n\njulia> # A chain with 3 iterations.\n       chain = Chains(\n           reshape(1.:6., 3, 2),\n           [:s, :m];\n           info=(varname_to_symbol=Dict(\n               @varname(s) => :s,\n               @varname(m) => :m,\n           ),),\n       );\n\njulia> plds = pointwise_logdensities(model, chain)\nChains MCMC chain (3×6×1 Array{Float64, 3}):\n\nIterations        = 1:1:3\nNumber of chains  = 1\nSamples per chain = 3\nparameters        = s, m, xs[1], xs[2], xs[3], y\n[...]\n\njulia> plds[:s]\n2-dimensional AxisArray{Float64,2,...} with axes:\n    :iter, 1:1:3\n    :chain, 1:1\nAnd data, a 3×1 Matrix{Float64}:\n -0.8027754226637804\n -1.3822169643436162\n -2.0986122886681096\n\njulia> # The above is the same as:\n       logpdf.(InverseGamma(2, 3), chain[:s])\n3×1 Matrix{Float64}:\n -0.8027754226637804\n -1.3822169643436162\n -2.0986122886681096\n\n\n\n\n\n","title":"DynamicPPL.pointwise_logdensities"},{"category":"function","location":"api/#DynamicPPL.pointwise_loglikelihoods","page":"API","text":"DynamicPPL.pointwise_loglikelihoods(\n    model::Model,\n    init_strat::AbstractInitStrategy\n)\n\nSame as pointwise_logdensities, but only returns the log-likelihoods for observed variables.\n\n\n\n\n\nDynamicPPL.pointwise_loglikelihoods(\n    model::DynamicPPL.Model,\n    chain::MCMCChains.Chains,\n)\n\nCompute the pointwise log-likelihoods of the model given the chain. This is the same as pointwise_logdensities(model, chain), but only including the likelihood terms.\n\nSee also: DynamicPPL.pointwise_logdensities, DynamicPPL.pointwise_prior_logdensities.\n\n\n\n\n\n","title":"DynamicPPL.pointwise_loglikelihoods"},{"category":"function","location":"api/#DynamicPPL.pointwise_prior_logdensities","page":"API","text":"DynamicPPL.pointwise_prior_logdensities(\n    model::Model,\n    init_strat::AbstractInitStrategy\n)\n\nSame as pointwise_logdensities, but only returns the log-densities for random variables (i.e. the priors).\n\n\n\n\n\nDynamicPPL.pointwise_prior_logdensities(\n    model::DynamicPPL.Model,\n    chain::MCMCChains.Chains\n)\n\nCompute the pointwise log-prior-densities of the model given the chain. This is the same as pointwise_logdensities(model, chain), but only including the prior terms.\n\nSee also: DynamicPPL.pointwise_logdensities, DynamicPPL.pointwise_loglikelihoods.\n\n\n\n\n\n","title":"DynamicPPL.pointwise_prior_logdensities"},{"category":"function","location":"api/#DynamicPPL.extract_priors","page":"API","text":"extract_priors([rng::Random.AbstractRNG, ]model::Model)\n\nExtract the priors from a model. This is done by sampling from the model and recording the distributions that are used to generate the samples.\n\nwarning: Warning\nBecause the extraction is done by execution of the model, there are several caveats:If the distribution itself is not a constant (e.g. if it depends on another random variable, then the extracted prior will have different parameters in every extraction!\nIf the model does not have static support, say, n ~ Categorical(1:10); x ~ MvNormal(zeros(n), I), then the extracted priors themselves will be different between extractions, not just their parameters.Both of these caveats are demonstrated below.\n\nExamples\n\nChanging parameters\n\njulia> using Distributions, StableRNGs\n\njulia> rng = StableRNG(42);\n\njulia> @model function model_dynamic_parameters()\n           x ~ Normal(0, 1)\n           y ~ Normal(x, 1)\n       end;\n\njulia> model = model_dynamic_parameters();\n\njulia> extract_priors(rng, model)[@varname(y)]\nNormal{Float64}(μ=-0.6702516921145671, σ=1.0)\n\njulia> extract_priors(rng, model)[@varname(y)]\nNormal{Float64}(μ=1.3736306979834252, σ=1.0)\n\nChanging support\n\njulia> using LinearAlgebra, Distributions, StableRNGs\n\njulia> rng = StableRNG(42);\n\njulia> @model function model_dynamic_support()\n           n ~ Categorical(ones(10) ./ 10)\n           x ~ MvNormal(zeros(n), I)\n       end;\n\njulia> model = model_dynamic_support();\n\njulia> length(extract_priors(rng, model)[@varname(x)])\n6\n\njulia> length(extract_priors(rng, model)[@varname(x)])\n9\n\n\n\n\n\nextract_priors(model::Model, varinfo::AbstractVarInfo)\n\nExtract the priors that were used to generate a VarInfo.\n\nThis is done by evaluating the model at the values present in varinfo and recording the distributions that are present at each tilde statement.\n\n\n\n\n\n","title":"DynamicPPL.extract_priors"},{"category":"function","location":"api/#DynamicPPL.filldist","page":"API","text":"filldist(d::Distribution, ns...)\n\nCreate a product distribution from a single distribution and a list of dimension sizes. If size(d) is (d1, d2, ...) and ns is (n1, n2, ...), then the resulting distribution will have size (d1, d2, ..., n1, n2, ...).\n\nWhen sampling from the resulting distribution, the output will be an array where each element is sampled from the original distribution d.\n\nThis is a convenient wrapper around product_distribution(FillArrays.Fill(d, ns...)).\n\nnote: Note\nfilldist used to be defined in DistributionsAD.jl. The definition here is semantically equivalent, but removes a lot of custom code that is no longer needed nowadays.\n\n\n\n\n\n","title":"DynamicPPL.filldist"},{"category":"function","location":"api/#DynamicPPL.arraydist","page":"API","text":"arraydist(dists::AbstractArray{<:Distribution})\n\nCreate a product distribution from an array of sub-distributions. Each element of dists should have the same size. If the size of each element is (d1, d2, ...), and size(dists) is (n1, n2, ...), then the resulting distribution will have size (d1, d2, ..., n1, n2, ...).\n\nThis is equivalent to product_distribution(dists), but can be more performant in some instances (specifically when dists is a vector of Normals).\n\nnote: Note\narraydist used to be defined in DistributionsAD.jl. The definition here is semantically equivalent, but removes a lot of custom code that is no longer needed nowadays.\n\n\n\n\n\n","title":"DynamicPPL.arraydist"},{"category":"type","location":"api/#DynamicPPL.NamedDist","page":"API","text":"NamedDist(dist::Distribution, name::Symbol)\n\nA named distribution that carries the name of the random variable with it.\n\nFields\n\ndist::Distribution{variate, support} where {variate, support}: the underlying distribution\nname::VarName: the name of the random variable\n\n\n\n\n\n","title":"DynamicPPL.NamedDist"},{"category":"function","location":"api/#DynamicPPL.TestUtils.AD.run_ad","page":"API","text":"run_ad(\n    model::Model,\n    adtype::ADTypes.AbstractADType;\n    test::Union{AbstractADCorrectnessTestSetting,Bool}=WithBackend(),\n    benchmark=false,\n    atol::AbstractFloat=1e-8,\n    rtol::AbstractFloat=sqrt(eps()),\n    getlogdensity::Function=getlogjoint_internal,\n    rng::Random.AbstractRNG=Random.default_rng(),\n    transform_strategy::AbstractTransformStrategy=LinkAll(),\n    params::Union{Nothing,Vector{<:AbstractFloat}}=nothing,\n    verbose=true,\n)::ADResult\n\nDescription\n\nTest the correctness and/or benchmark the AD backend adtype for the model model.\n\nWhether to test and benchmark is controlled by the test and benchmark keyword arguments. By default, test is true and benchmark is false.\n\nNote that to run AD successfully you will need to import the AD backend itself. For example, to test with AutoReverseDiff() you will need to run import ReverseDiff.\n\nArguments\n\nThere are two positional arguments, which absolutely must be provided:\n\nmodel - The model being tested.\nadtype - The AD backend being tested.\n\nEverything else is optional, and can be categorised into several groups:\n\nWhether to evaluate in linked space or not.\nIf requested, DynamicPPL internally transforms the model parameters to unconstrained Euclidean space before evaluating the log density in that transformed space.\nYou can control whether this transformation happens or not by passing the transform_strategy keyword argument. The default is LinkAll(), which means that all parameters will be transformed to unconstrained space. This is the most relevant setting for testing AD.\nHowever, if you want to evaluate in the original space, you can use UnlinkAll(); you can also specify mixed linking strategies if desired (see the DynamicPPL documentation for more information). \nHow to specify the parameters to be used for evaluation.\nFor maximum control over this, generate a vector of parameters yourself and pass this as the params argument. If you don't specify this, it will be generated randomly from the prior of the model. If you want to seed the parameter generation, you can pass the rng keyword argument, which will then be used to generate the parameters.\nWhich type of logp is being calculated.\nBy default, run_ad evaluates the 'internal log joint density' of the model, i.e., the log joint density in the unconstrained space. Thus, for example, in\n@model f() = x ~ LogNormal()\nthe internal log joint density is logpdf(Normal(), log(x)). This is the relevant log density for e.g. Hamiltonian Monte Carlo samplers and is therefore the most useful to test.\nIf you want the log joint density in the original model parameterisation, you can use getlogjoint. Likewise, if you want only the prior or likelihood, you can use getlogprior or getloglikelihood, respectively.\nHow to specify the results to compare against.\nOnce logp and its gradient has been calculated with the specified adtype, it can optionally be tested for correctness. The exact way this is tested is specified in the test parameter.\nThere are several options for this:\nYou can explicitly specify the correct value using WithExpectedResult().\nYou can compare against the result obtained with a different AD backend using WithBackend(adtype).\nYou can disable testing by passing NoTest().\nThe default is to compare against the result obtained with ForwardDiff, i.e. WithBackend(AutoForwardDiff()).\ntest=false and test=true are synonyms for NoTest() and WithBackend(AutoForwardDiff()), respectively.\nHow to specify the tolerances. (Only if testing is enabled.)\nBoth absolute and relative tolerances can be specified using the atol and rtol keyword arguments respectively. The behaviour of these is similar to isapprox(), i.e. the value and gradient are considered correct if either atol or rtol is satisfied. The default values are 100*eps() for atol and sqrt(eps()) for rtol.\nFor the most part, it is the rtol check that is more meaningful, because we cannot know the magnitude of logp and its gradient a priori. The atol value is supplied to handle the case where gradients are equal to zero.\nWhether to benchmark.\nBy default, benchmarking is disabled. To enable it, set benchmark=true. When enabled, the time taken to evaluate logp as well as its gradient is measured using Chairmarks.jl, and the ADResult object returned will contain grad_time and primal_time fields with the median times (in seconds).\nWhether to output extra logging information.\nBy default, this function prints messages when it runs. To silence it, set verbose=false.\n\nReturns / Throws\n\nReturns an ADResult object, which contains the results of the test and/or benchmark.\n\nIf test is true and the AD backend returns an incorrect value or gradient, an ADIncorrectException is thrown. If a different error occurs, it will be thrown as-is.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.AD.run_ad"},{"category":"type","location":"api/#DynamicPPL.TestUtils.AD.AbstractADCorrectnessTestSetting","page":"API","text":"AbstractADCorrectnessTestSetting\n\nDifferent ways of testing the correctness of an AD backend.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.AD.AbstractADCorrectnessTestSetting"},{"category":"type","location":"api/#DynamicPPL.TestUtils.AD.WithBackend","page":"API","text":"WithBackend(adtype::AbstractADType=AutoForwardDiff()) <: AbstractADCorrectnessTestSetting\n\nTest correctness by comparing it against the result obtained with adtype.\n\nadtype defaults to ForwardDiff.jl, since it's the default AD backend used in Turing.jl.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.AD.WithBackend"},{"category":"type","location":"api/#DynamicPPL.TestUtils.AD.WithExpectedResult","page":"API","text":"WithExpectedResult(\n    value::T,\n    grad::AbstractVector{T}\n) where {T <: AbstractFloat}\n<: AbstractADCorrectnessTestSetting\n\nTest correctness by comparing it against a known result (e.g. one obtained analytically, or one obtained with a different backend previously). Both the value of the primal (i.e. the log-density) as well as its gradient must be supplied.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.AD.WithExpectedResult"},{"category":"type","location":"api/#DynamicPPL.TestUtils.AD.NoTest","page":"API","text":"NoTest() <: AbstractADCorrectnessTestSetting\n\nDisable correctness testing.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.AD.NoTest"},{"category":"type","location":"api/#DynamicPPL.TestUtils.AD.ADResult","page":"API","text":"ADResult{Tparams<:AbstractFloat,Tresult<:AbstractFloat,Ttol<:AbstractFloat}\n\nData structure to store the results of the AD correctness test.\n\nThe type parameter Tparams is the numeric type of the parameters passed in; Tresult is the type of the value and the gradient; and Ttol is the type of the absolute and relative tolerances used for correctness testing.\n\nFields\n\nmodel::Model: The DynamicPPL model that was tested\ngetlogdensity::Function: The function used to extract the log density from the model\nldf::LogDensityFunction: The LogDensityFunction that was used\nparams::Vector{Tparams} where Tparams<:AbstractFloat: The values at which the model was evaluated\nadtype::AbstractADType: The AD backend that was tested\natol::AbstractFloat: Absolute tolerance used for correctness test\nrtol::AbstractFloat: Relative tolerance used for correctness test\nvalue_expected::Union{Nothing, Tresult} where Tresult<:AbstractFloat: The expected value of logp\ngrad_expected::Union{Nothing, Vector{Tresult}} where Tresult<:AbstractFloat: The expected gradient of logp\nvalue_actual::AbstractFloat: The value of logp (calculated using adtype)\ngrad_actual::Vector{Tresult} where Tresult<:AbstractFloat: The gradient of logp (calculated using adtype)\ngrad_time::Union{Nothing, Tresult} where Tresult<:AbstractFloat: If benchmarking was requested, the time taken by the AD backend to evaluate the gradient     of logp (in seconds)\nprimal_time::Union{Nothing, Tresult} where Tresult<:AbstractFloat: If benchmarking was requested, the time taken by the AD backend to evaluate logp (in     seconds)\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.AD.ADResult"},{"category":"type","location":"api/#DynamicPPL.TestUtils.AD.ADIncorrectException","page":"API","text":"ADIncorrectException{T<:AbstractFloat}\n\nException thrown when an AD backend returns an incorrect value or gradient.\n\nThe type parameter T is the numeric type of the value and gradient.\n\nFields\n\nvalue_expected::AbstractFloat\nvalue_actual::AbstractFloat\ngrad_expected::Vector{T} where T<:AbstractFloat\ngrad_actual::Vector{T} where T<:AbstractFloat\natol::AbstractFloat\nrtol::AbstractFloat\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.AD.ADIncorrectException"},{"category":"constant","location":"api/#DynamicPPL.TestUtils.DEMO_MODELS","page":"API","text":"A collection of models corresponding to the posterior distribution defined by the generative process\n\ns ~ InverseGamma(2, 3)\nm ~ Normal(0, √s)\n1.5 ~ Normal(m, √s)\n2.0 ~ Normal(m, √s)\n\nor by\n\ns[1] ~ InverseGamma(2, 3)\ns[2] ~ InverseGamma(2, 3)\nm[1] ~ Normal(0, √s)\nm[2] ~ Normal(0, √s)\n1.5 ~ Normal(m[1], √s[1])\n2.0 ~ Normal(m[2], √s[2])\n\nThese are examples of a Normal-InverseGamma conjugate prior with Normal likelihood, for which the posterior is known in closed form.\n\nIn particular, for the univariate model (the former one):\n\nmean(s) == 49 / 24\nmean(m) == 7 / 6\n\nAnd for the multivariate one (the latter one):\n\nmean(s[1]) == 19 / 8\nmean(m[1]) == 3 / 4\nmean(s[2]) == 8 / 3\nmean(m[2]) == 1\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.DEMO_MODELS"},{"category":"constant","location":"api/#DynamicPPL.TestUtils.ALL_MODELS","page":"API","text":"A tuple of all models defined in DynamicPPL.TestUtils.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.ALL_MODELS"},{"category":"function","location":"api/#DynamicPPL.TestUtils.logprior_true","page":"API","text":"logprior_true(model, args...)\n\nReturn the logprior of model for args.\n\nThis should generally be implemented by hand for every specific model.\n\nSee also: logjoint_true, loglikelihood_true.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.logprior_true"},{"category":"function","location":"api/#DynamicPPL.TestUtils.loglikelihood_true","page":"API","text":"loglikelihood_true(model, args...)\n\nReturn the loglikelihood of model for args.\n\nThis should generally be implemented by hand for every specific model.\n\nSee also: logjoint_true, logprior_true.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.loglikelihood_true"},{"category":"function","location":"api/#DynamicPPL.TestUtils.logjoint_true","page":"API","text":"logjoint_true(model, args...)\n\nReturn the logjoint of model for args.\n\nDefaults to logprior_true(model, args...) + loglikelihood_true(model, args..).\n\nThis should generally be implemented by hand for every specific model so that the returned value can be used as a ground-truth for testing things like:\n\nValidity of evaluation of model using a particular implementation of AbstractVarInfo.\nValidity of a sampler when combined with DynamicPPL by running the sampler twice: once targeting ground-truth functions, e.g. logjoint_true, and once targeting model.\n\nAnd more.\n\nSee also: logprior_true, loglikelihood_true.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.logjoint_true"},{"category":"function","location":"api/#DynamicPPL.TestUtils.logprior_true_with_logabsdet_jacobian","page":"API","text":"logprior_true_with_logabsdet_jacobian(model::Model, args...)\n\nReturn a tuple (args_unconstrained, logprior_unconstrained) of model for args....\n\nUnlike logprior_true, the returned logprior computation includes the log-absdet-jacobian adjustment, thus computing logprior for the unconstrained variables.\n\nNote that args are assumed be in the support of model, while args_unconstrained will be unconstrained.\n\nSee also: logprior_true.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.logprior_true_with_logabsdet_jacobian"},{"category":"function","location":"api/#DynamicPPL.TestUtils.logjoint_true_with_logabsdet_jacobian","page":"API","text":"logjoint_true_with_logabsdet_jacobian(model::Model, args...)\n\nReturn a tuple (args_unconstrained, logjoint) of model for args.\n\nUnlike logjoint_true, the returned logjoint computation includes the log-absdet-jacobian adjustment, thus computing logjoint for the unconstrained variables.\n\nNote that args are assumed be in the support of model, while args_unconstrained will be unconstrained.\n\nThis should generally not be implemented directly, instead one should implement logprior_true_with_logabsdet_jacobian for a given model.\n\nSee also: logjoint_true, logprior_true_with_logabsdet_jacobian.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.logjoint_true_with_logabsdet_jacobian"},{"category":"function","location":"api/#DynamicPPL.TestUtils.varnames","page":"API","text":"varnames(model::Model)\n\nReturn the VarNames defined in model, as a Vector.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.varnames"},{"category":"function","location":"api/#DynamicPPL.TestUtils.posterior_mean","page":"API","text":"posterior_mean(model::Model)\n\nReturn a NamedTuple compatible with varnames(model) where the values represent the posterior mean under model.\n\n\"Compatible\" means that a varname from varnames(model) can be used to extract the corresponding value using e.g. AbstractPPL.getvalue(posterior_mean(model), varname).\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.posterior_mean"},{"category":"function","location":"api/#DynamicPPL.TestUtils.setup_varinfos","page":"API","text":"setup_varinfos(model::Model, example_values::NamedTuple; include_threadsafe::Bool=false)\n\nReturn a tuple of VarInfo (and ThreadSafeVarInfo, if include_threadsafe=true) where the values in vi have been obtained by evaluating the model with example_values.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.setup_varinfos"},{"category":"function","location":"api/#DynamicPPL.TestUtils.test_values","page":"API","text":"test_values(vnt::VarNamedTuple, vals::NamedTuple, vns)\n\nTest that vnt[vn] corresponds to the correct value in vals for every vn in vns.\n\n\n\n\n\n","title":"DynamicPPL.TestUtils.test_values"},{"category":"function","location":"api/#DynamicPPL.DebugUtils.check_model","page":"API","text":"DynamicPPL.DebugUtils.check_model(\n    [rng::Random.AbstractRNG,]\n    model::Model;\n    error_on_failure=false,\n    fail_if_discrete=false\n)\n\nCheck model for potential issues. Returns true if the model check succeeded, false otherwise.\n\nThe model is only evaluated a single time, so if the model contains any indeterminism, results may differ across runs. The rng argument can be used to control reproducibility if needed.\n\nIssues that this function checks for\n\nRepeated usage of the same or overlapping VarNames\nNaN on the left-hand side of observe statements\n(if fail_if_discrete is set) Usage of discrete distributions\nEmpty models emit a warning, but do not fail (since they are not incorrect per se)\n\nKeyword arguments\n\nerror_on_failure::Bool: Whether to throw an error (instead of just returning false) if the model check fails.\nfail_if_discrete::Bool: Whether to fail (i.e., return false or throw an error,  depending on error_on_failure) when the model contains discrete distributions. Discrete  distributions do not have a differentiable log-density and are incompatible with  gradient-based approaches such as HMC / NUTS or optimisation.\n\nExamples\n\nCorrect model\n\njulia> using DynamicPPL.DebugUtils: check_model; using Distributions\n\njulia> @model demo_correct() = x ~ Normal()\ndemo_correct (generic function with 2 methods)\n\njulia> model = demo_correct();\n\njulia> check_model(model)\ntrue\n\njulia> cond_model = model | (x = 1.0,);\n\njulia> # Empty models will issue a warning, but not a failure\n       check_model(cond_model)\n┌ Warning: The model does not contain any parameters.\n└ @ DynamicPPL.DebugUtils DynamicPPL.jl/src/debug_utils.jl:215\ntrue\n\nIncorrect model\n\n```jldoctest; setup=:(using Distributions) julia> using DynamicPPL.DebugUtils: check_model; using Distributions\n\njulia> @model function demoincorrect()            # Sampling x twice.            x ~ Normal()            x ~ Exponential()        end demoincorrect (generic function with 2 methods)\n\njulia> # Notice that VarInfo(modelincorrect) evaluates the model, but doesn't actually        # alert us to the issue of x being sampled twice.        model = demoincorrect(); varinfo = VarInfo(model);\n\njulia> checkmodel(model; erroron_failure=true) ERROR: varname x used multiple times in model\n\n\n\n\n\n","title":"DynamicPPL.DebugUtils.check_model"},{"category":"function","location":"api/#DynamicPPL.DebugUtils.has_static_constraints","page":"API","text":"has_static_constraints([rng, ]model::Model; num_evals=5)\n\nAttempts to detect whether model has static constraints (i.e., the support of all variables is the same regardless of what their values are). Returns true if the model has static constraints, false otherwise.\n\nNote that this is a heuristic check based on sampling from the model multiple times and checking if the model is consistent across runs.\n\nArguments\n\nrng::Random.AbstractRNG: The random number generator to use when evaluating the model.\nmodel::Model: The model to check.\n\nKeyword Arguments\n\nnum_evals::Int: The number of evaluations to perform. Default: 5.\n\n\n\n\n\n","title":"DynamicPPL.DebugUtils.has_static_constraints"},{"category":"function","location":"api/#DynamicPPL.DebugUtils.model_warntype","page":"API","text":"model_warntype(model[, varinfo]; optimize=true)\n\nCheck the type stability of the model's evaluator, warning about any potential issues.\n\nThis simply calls @code_warntype on the model's evaluator, filling in internal arguments where needed.\n\nArguments\n\nmodel::Model: The model to check.\nvarinfo::AbstractVarInfo: The varinfo to use when evaluating the model. Default: VarInfo(model).\n\nKeyword Arguments\n\noptimize::Bool: Whether to generate optimized code. Default: false.\n\n\n\n\n\n","title":"DynamicPPL.DebugUtils.model_warntype"},{"category":"function","location":"api/#DynamicPPL.DebugUtils.model_typed","page":"API","text":"model_typed(model[, varinfo]; optimize=true)\n\nReturn the type inference for the model's evaluator.\n\nThis simply calls @code_typed on the model's evaluator, filling in internal arguments where needed.\n\nArguments\n\nmodel::Model: The model to check.\nvarinfo::AbstractVarInfo: The varinfo to use when evaluating the model. Default: VarInfo(model).\n\nKeyword Arguments\n\noptimize::Bool: Whether to generate optimized code. Default: true.\n\n\n\n\n\n","title":"DynamicPPL.DebugUtils.model_typed"},{"category":"function","location":"api/#DynamicPPL.DebugUtils.gen_evaluator_call_with_types","page":"API","text":"gen_evaluator_call_with_types(model[, varinfo])\n\nGenerate the evaluator call and the types of the arguments.\n\nArguments\n\nmodel::Model: The model whose evaluator is of interest.\nvarinfo::AbstractVarInfo: The varinfo to use when evaluating the model. Default: VarInfo(model).\n\nReturns\n\nA 2-tuple with the following elements:\n\nf: This is either model.f or Core.kwcall, depending on whether   the model has keyword arguments.\nargtypes::Type{<:Tuple}: The types of the arguments for the evaluator.\n\n\n\n\n\n","title":"DynamicPPL.DebugUtils.gen_evaluator_call_with_types"},{"category":"type","location":"api/#DynamicPPL.AbstractVarInfo","page":"API","text":"AbstractVarInfo\n\nAbstract supertype for data structures that capture random variables when executing a probabilistic model and accumulate log densities such as the log likelihood or the log joint probability of the model.\n\nSee also: VarInfo, OnlyAccsVarInfo.\n\n\n\n\n\n","title":"DynamicPPL.AbstractVarInfo"},{"category":"type","location":"api/#DynamicPPL.VarInfo","page":"API","text":"VarInfo{\n    Tfm<:AbstractTransformStrategy,\n    T<:VarNamedTuple,\n    Accs<:AccumulatorTuple\n} <: AbstractVarInfo\n\nThe default implementation of AbstractVarInfo, storing variable values and accumulators.\n\nA VarInfo, vi, is quite a thin wrapper around\n\nvi.values: a VarNamedTuple storing the variable values, and\nvi.accs: a tuple of accumulators.\n\nThe only really noteworthy thing about it is that vi.values specifically stores the values of variables as [TransformedValue{<:AbstractVector}](@ref TransformedValue).\n\nThat is, regardless of what the value of a variable is in the original distribution, the VarInfo stores a vectorised version of the value. It is not particularly concerned about whether the variable is linked or not: you can mix unlinked variables with linked variables in a VarInfo.\n\nnote: Note\nThis functionality is identical to that in VectorValueAccumulator, and going forward we recommend using that instead of VarInfo.\n\nOn top of that, VarInfo also stores a transform strategy, which reflects the linked status of variables inside the VarInfo. For example, a VarInfo{LinkAll} should contain only TransformedValue{T,DynamicLink}s in its values field. This unfortunately leads to redundancy of information, but is necessary for type stability, since that allows us to have compile-time knowledge of what transformations are applied.\n\nBecause the job of VarInfo is to store transformed values, there is no generic setindex!! implementation on VarInfo itself. Instead, all storage must go via setindex_with_dist!!, which takes care of storing the value in the correct transformed form. This in turn means that the distribution on the right-hand side of a tilde-statement must be available when modifying a VarInfo.\n\nFurthermore, since no untransformed (raw) values are stored in VarInfo, there is no generic getindex implementation that returns raw values. If you need this functionality, you should make sure that vi.accs contains a RawValueAccumulator and use that to get the raw values. To directly get access to the internal vectorised values in vi.values, you can use getindex_internal, setindex_internal!!, and unflatten!!.\n\nFor more details on the internal storage, see documentation of TransformedValue and VarNamedTuple.\n\nFields\n\ntransform_strategy::AbstractTransformStrategy\nvalues::VarNamedTuple\naccs::DynamicPPL.AccumulatorTuple\n\n\n\n\n\n","title":"DynamicPPL.VarInfo"},{"category":"function","location":"api/#DynamicPPL.get_values","page":"API","text":"get_values(vi::AbstractVarInfo)\n\nReturn the VarNamedTuple in vi that stores the variables' transformed values.\n\nThis should be implemented by each subtype of AbstractVarInfo.\n\n\n\n\n\n","title":"DynamicPPL.get_values"},{"category":"function","location":"api/#DynamicPPL.setindex_with_dist!!","page":"API","text":"setindex_with_dist!!(\n    vi::VarInfo,\n    tval::TransformedValue{<:AbstractVector{<:Real},<:Any},\n    dist::Distribution,\n    vn::VarName,\n    template::Any,\n)\n\nStore a transformed value that has already been vectorised. This might include dynamically transformed variables (which have tval.transform as a DynamicLink or Unlink), or statically transformed variables (which have tval.transform as a FixedTransform). However, in either case, it is mandatory that tval.value is a vector.\n\nNote that this will cause the linked status of vi to update according to what tval is. That means that whether or not a variable is considered to be 'linked' is determined by tval rather than the previous status of vi.\n\n\n\n\n\nsetindex_with_dist!!(\n    vi::VarInfo,\n    utval::TransformedValue{<:Any,NoTransform},\n    dist::Distribution,\n    vn::VarName,\n    template::Any\n)\n\nVectorise utval and store it. (Note that if setindex_with_dist!! receives an untransformed value, the variable is always considered unlinked, since if it were to be linked, apply_transform_strategy will already have done so.)\n\n\n\n\n\n","title":"DynamicPPL.setindex_with_dist!!"},{"category":"function","location":"api/#DynamicPPL.is_transformed","page":"API","text":"is_transformed(vi::AbstractVarInfo[, vns::Union{VarName, AbstractVector{<:Varname}}])\n\nReturn true if all variables in vi have a transform of DynamicLink, and false     otherwise.\n\nIf vns is provided, then only check if this/these varname(s) are transformed.\n\nwarning: Warning\nNot all implementations of AbstractVarInfo support transforming only a subset of the variables.\n\n\n\n\n\n","title":"DynamicPPL.is_transformed"},{"category":"type","location":"api/#DynamicPPL.VarNamedTuples.VarNamedTuple","page":"API","text":"VarNamedTuple{names,Values}\n\nA NamedTuple-like structure with VarName keys.\n\nVarNamedTuple is a data structure for storing arbitrary data, keyed by VarNames, in an efficient and type stable manner. It is mainly used through getindex, setindex!!, templated_setindex!!, and haskey, all of which only accept VarNames as keys. Other notable methods are merge and subset.\n\nVarNamedTuple has an ordering to its elements, and two VarNamedTuples with the same keys and values but in different orders are considered different for equality and hashing. Iterations such as keys and values respect this ordering. The ordering is dependent on the order in which elements were inserted into the VarNamedTuple, though isn't always equal to it. More specifically\n\nAny new keys that have a joint parent VarName with an existing key are inserted after that key. For instance, if one first inserts, in order, @varname(a.x), @varname(b), and @varname(a.y), the resulting order will be (@varname(a.x), @varname(a.y), @varname(b)).\nIndex keys, like@varname(a[3])or@varname(b[2,3,4:5]), are always iterated in the same order anArraywith the same indices would be iterated. For instance, if one first inserts, in order,@varname(a[2]),@varname(b), and@varname(a[1]), the resulting order will be(@varname(a[1]), @varname(a[2]), @varname(b))`.\n\nOtherwise insertion order is respected.\n\nsetindex!! and getindex on VarNamedTuple are type stable as long as one does not store heterogeneous data under different indices of the same symbol. That is, if either\n\none sets a[1] and a[2] to be of different types, or\nif a[1] and a[2] both exist, one sets a[1].b without setting a[2].b,\n\nthen getting values for a[1] or a[2] will not be type stable.\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.VarNamedTuple"},{"category":"macro","location":"api/#DynamicPPL.VarNamedTuples.@vnt","page":"API","text":"@vnt begin ... end\n\nConstruct a VarNamedTuple from a block of assignments. Each assignment should be of the form var := value, where var is a variable name. This is best illustrated by example:\n\njulia> using DynamicPPL\n\njulia> @vnt begin\n           a := 1\n           b := 2\n       end\nVarNamedTuple\n├─ a => 1\n└─ b => 2\n\nYou can set entirely arbitrary variables:\n\njulia> @vnt begin\n           a.b.c.d.e := \"hello\"\n       end\nVarNamedTuple\n└─ a => VarNamedTuple\n        └─ b => VarNamedTuple\n                └─ c => VarNamedTuple\n                        └─ d => VarNamedTuple\n                                └─ e => \"hello\"\n\nFor variables that have indexing, it is often necessary to provide a template, so that the VNT 'knows' what kind of array is being used to store the values, and can set the values in the appropriate places (consider e.g. OffsetArrays where x[1] may not mean what it usually does).\n\nThis is done by inserting a @template macro call in the block. The @template macro accepts whitespace-separated arguments, which must either be\n\na single symbol (e.g. @template x), in which case the template is the value of x (and x must already be defined in the current scope); or\nan assignment of the form y = expr, in which case the template for y is the value of expr. In this case y does not need to be defined in the current scope, but any symbols referenced in expr must be.\n\nFor example:\n\njulia> x = zeros(5); outside_y = zeros(3, 3);\n\njulia> @vnt begin\n            @template x y=outside_y\n            x[1] := 1.0\n            y[1, 1] := 2.0\n       end\nVarNamedTuple\n├─ x => PartialArray size=(5,) data::Vector{Float64}\n│       └─ (1,) => 1.0\n└─ y => PartialArray size=(3, 3) data::Matrix{Float64}\n        └─ (1, 1) => 2.0\n\nnote: Note\nYou can use any expression in @template y=expr, even a function call that is completely contained within the macro (e.g. @template y=zeros(3, 3)). The macro makes sure that expr is only evaluated once, so there is no performance penalty to doing this.\n\nIf no template is provided, the VNT will use a GrowableArray. This can produce correct results in simple cases, but is not recommended for general use. Please see the VarNamedTuple documentation for more details.\n\njulia> @vnt begin\n            # No template provided.\n            x[1] := 1.0\n            y[1, 1] := 2.0\n       end\n┌ Warning: Creating a growable `Base.Array` of dimension 1 to store values. This may not match the actual type or size of the actual `AbstractArray` that will be used inside the DynamicPPL model.\n│\n│  If this is not the type or size that you expect, please see: https://turinglang.org/docs/uri/growablearray\n└ @ DynamicPPL.VarNamedTuples /path/to/DynamicPPL.jl/src/varnamedtuple/partial_array.jl:823\n┌ Warning: Creating a growable `Base.Array` of dimension 2 to store values. This may not match the actual type or size of the actual `AbstractArray` that will be used inside the DynamicPPL model.\n│\n│  If this is not the type or size that you expect, please see: https://turinglang.org/docs/uri/growablearray\n└ @ DynamicPPL.VarNamedTuples /path/to/DynamicPPL.jl/src/varnamedtuple/partial_array.jl:823\nVarNamedTuple\n├─ x => PartialArray size=(1,) data::DynamicPPL.VarNamedTuples.GrowableArray{Float64, 1}\n│       └─ (1,) => 1.0\n└─ y => PartialArray size=(1, 1) data::DynamicPPL.VarNamedTuples.GrowableArray{Float64, 2}\n        └─ (1, 1) => 2.0\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.@vnt"},{"category":"function","location":"api/#DynamicPPL.VarNamedTuples.apply!!","page":"API","text":"apply!!(func, vnt::VarNamedTuple, name::VarName)\n\nApply func to the subdata at name in vnt, and set the result back at name.\n\nLike map_values!!, but only for a single VarName.\n\njulia> using DynamicPPL: VarNamedTuple, setindex!!\n\njulia> using DynamicPPL.VarNamedTuples: apply!!\n\njulia> vnt = VarNamedTuple()\nVarNamedTuple()\n\njulia> vnt = setindex!!(vnt, [1, 2, 3], @varname(a))\nVarNamedTuple\n└─ a => [1, 2, 3]\n\njulia> apply!!(x -> x .+ 1, vnt, @varname(a))\nVarNamedTuple\n└─ a => [2, 3, 4]\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.apply!!"},{"category":"function","location":"api/#DynamicPPL.VarNamedTuples.densify!!","page":"API","text":"densify!!(vnt::VarNamedTuple)\n\nConvert any PartialArrays in vnt where all elements are filled to just normal arrays.\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.densify!!"},{"category":"function","location":"api/#DynamicPPL.VarNamedTuples.skeleton","page":"API","text":"DynamicPPL.skeleton(vnt::VarNamedTuple)\n\nCreate a skeleton of the given VarNamedTuple, which holds enough template information to reconstruct the original vnt from an ordered set of (VarName, value) pairs.\n\nSpecifically, we require that for any input orig_vnt that the following holds true:\n\nskel = skeleton(orig_vnt)\n# Check roundtrip reconstruction\nnew_vnt = VarNamedTuple()\nfor (vn, val) in pairs(orig_vnt)\n    top_sym = AbstractPPL.getsym(vn)\n    template = get(skel.data, top_sym, DynamicPPL.NoTemplate())\n    new_vnt = DynamicPPL.templated_setindex!!(new_vnt, val, vn, template)\nend\n@assert new_vnt == orig_vnt\n\nThis is accomplished by recursively traversing vnt and dropping any elements that do not contain PartialArrays. PartialArrays on the other hand are replaced with arrays of the same shape and array type, but with the elements replaced with nothing (since that is space-efficient). This does not cause any issues with reconstruction type stability, since templated_setindex!! does not use the array's element type when creating a new entry (it infers the element type to use from the value that is being set).\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.skeleton"},{"category":"function","location":"api/#DynamicPPL.VarNamedTuples.map_pairs!!","page":"API","text":"map_pairs!!(func, vnt::VarNamedTuple)\n\nApply func to all key => value pairs of vnt, in place if possible.\n\nfunc should accept a pair of VarName and value, and return the new value to be set.\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.map_pairs!!"},{"category":"function","location":"api/#DynamicPPL.VarNamedTuples.map_values!!","page":"API","text":"map_values!!(func, vnt::VarNamedTuple)\n\nApply func to elements of vnt, in place if possible.\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.map_values!!"},{"category":"type","location":"api/#DynamicPPL.VarNamedTuples.PartialArray","page":"API","text":"PartialArray{\n    ElType,\n    num_dims,\n    D<:AbstractArray{ElType,num_dims},\n    M<:AbstractArray{Bool,num_dims}\n}\n\nAn array-like like structure that may only have some of its elements defined.\n\nOne can set values in a PartialArray either element-by-element, or with ranges like arr[1:3,2] = [5,10,15]. \n\nWhen setting values over a range of indices, the value being set can be an AbstractArray whose size matches the range (in which case the values are set elementwise). If the value is some other object, it can still be stored as an ArrayLikeBlock. Retrieving such a block-value must be done with the exact same range of indices, otherwise an error is thrown. Please see the DynamicPPL documentation for more information on this.\n\nIf the element type of a PartialArray is not concrete, any call to setindex!! will check if, after the new value has been set, the element type can be made more concrete. If so, a new PartialArray with a more concrete element type is returned. Thus the element type of any PartialArray should always be as concrete as is allowed by the elements in it.\n\nThe internal implementation of an PartialArray consists of two arrays: one holding the data and the other one being a boolean mask indicating which elements are defined. These internal arrays may need resizing when new elements are set that have index ranges larger than the current internal arrays. To avoid resizing too often, the internal arrays are resized in exponentially increasing steps. This means that most setindex!! calls are very fast, but some may incur substantial overhead due to resizing and copying data. It also means that the largest index set so far determines the memory usage of the PartialArray. PartialArrays are thus well-suited when most values in it will eventually be set. If only a few scattered values are set, a structure like SparseArray may be more appropriate.\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.PartialArray"},{"category":"function","location":"api/#DynamicPPL.VarNamedTuples.templated_setindex!!","page":"API","text":"DynamicPPL.VarNamedTuples.templated_setindex!!(vnt, value, vn, template; allow_new=Val(true))\n\nAssign value to the location in vnt specified by vn.\n\nThe argument template must be provided in order to guide the creation of PartialArrays, as well as to concretise any dynamic indices in vn. It must be an object that has the shape of the top-level symbol in vn. For example:\n\nvnt = VarNamedTuple()\ntemplated_setindex!!(vnt, 10, @varname(x[1]), rand(2, 2))\n\nHere, rand(2, 2) is the template for the top-level symbol x, which tells setindex!! that x should be a PartialArray that is backed by a matrix.\n\nThe actual data inside template is not needed, and template is never mutated by this call.\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.templated_setindex!!"},{"category":"type","location":"api/#DynamicPPL.VarNamedTuples.NoTemplate","page":"API","text":"NoTemplate\n\nA singleton struct representing the fact that there is no template for a top-level variable. When NoTemplate is used, several things happen:\n\nIf you attempt to call make_leaf with an Index optic, this will error.\nWhen recursing into substructures, NoTemplate will be propagated.\n\nCollectively this means that you can only set values for variables that only have Property optics, unless a template is provided.\n\nIt might seem more idiomatic to use nothing or missing for this. However, this causes a bug with BangBang.setindex!!: https://github.com/JuliaFolds2/BangBang.jl/issues/43 so we use a dedicated struct instead.\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.NoTemplate"},{"category":"type","location":"api/#DynamicPPL.VarNamedTuples.SkipTemplate","page":"API","text":"SkipTemplate{N}(value)\n\nA struct representing the fact that value is the template for the variable N levels down from the top-level variable. In other words, SkipTemplate{0}(value) is equivalent to just value, and SkipTemplate{1}(value) means that value is the template for a when setting the variable @varname(x.a).\n\n\n\n\n\n","title":"DynamicPPL.VarNamedTuples.SkipTemplate"},{"category":"method","location":"api/#Core.NamedTuple-Tuple{VarNamedTuple}","page":"API","text":"NamedTuple(vnt::VarNamedTuple)\n\nConvert a VarNamedTuple to a standard NamedTuple, provided all keys in the VarNamedTuple are VarNames with top-level symbols. If any key is a VarName with a non-identity optic (e.g., @varname(x.a) or @varname(x[1])), this will throw an ArgumentError.\n\nExamples\n\njulia> using DynamicPPL, BangBang\n\njulia> vnt = VarNamedTuple(); vnt = setindex!!(vnt, 10, @varname(x))\nVarNamedTuple\n└─ x => 10\n\njulia> NamedTuple(vnt)\n(x = 10,)\n\njulia> vnt2 = setindex!!(vnt, 20, @varname(y.a))\nVarNamedTuple\n├─ x => 10\n└─ y => VarNamedTuple\n        └─ a => 20\n\njulia> NamedTuple(vnt2)\nERROR: ArgumentError: Cannot convert VarNamedTuple containing non-identity VarNames to NamedTuple. To create a NamedTuple, all keys in the VarNamedTuple must be top-level symbols.\n[...]\n\n\n\n\n\n","title":"Core.NamedTuple"},{"category":"type","location":"api/#DynamicPPL.AbstractAccumulator","page":"API","text":"AbstractAccumulator\n\nAn abstract type for accumulators.\n\nAn accumulator is an object that may change its value at every tilde_assume!! or tilde_observe!! call based on the random variable in question. The obvious examples of accumulators are the log prior and log likelihood. Other examples might be a variable that counts the number of observations in a trace, or a list of the names of random variables seen so far.\n\nAn accumulator type T <: AbstractAccumulator must implement the following methods:\n\naccumulator_name(acc::T) or accumulator_name(::Type{T})\naccumulate_observe!!(acc::T, dist, val, vn, template)\naccumulate_assume!!(acc::T, val, tval, logjac, vn, dist, template)\nreset(acc::T)\nBase.copy(acc::T)\n\nIn these functions:\n\nval is the new value of the random variable sampled from a distribution (always in the original unlinked space), or the value on the left-hand side of an observe statement.\ntval is the original TransformedValue that was obtained from the initialisation strategy. This is passed through unchanged to accumulate_assume!! since it can be reused for some accumulators (e.g. when storing linked values, if the linked value was already provided, it is faster to reuse it than to re-link val).\ndist is the distribution on the RHS of the tilde statement.\nvn is the VarName that is on the left-hand side of the tilde-statement. If the tilde-statement is a literal observation like 0.0 ~ Normal(), then vn is nothing.\nlogjac is the log determinant of the Jacobian of the link transformation, if the variable is stored as a linked value in the VarInfo. If the variable is stored in its original, unlinked form, then logjac is zero.\ntemplate is a value that conveys the shape of the top-level symbol in vn, and is used specifically for accumulators that carry VarNamedTuples.\n\nTo be able to work with multi-threading, it should also implement:\n\nsplit(acc::T)\ncombine(acc::T, acc2::T)\n\nSee the documentation for each of these functions for more details.\n\n\n\n\n\n","title":"DynamicPPL.AbstractAccumulator"},{"category":"function","location":"api/#DynamicPPL.accumulate_assume!!","page":"API","text":"accumulate_assume!!(acc::AbstractAccumulator, val, tval, logjac, vn, right, template)\n\nUpdate acc in a tilde_assume!! call. Returns the updated acc.\n\nSee AbstractAccumulator for the meaning of the arguments.\n\naccumulate_assume!! may mutate acc, but not any of the other arguments.\n\nSee also: accumulate_observe!!\n\n\n\n\n\n","title":"DynamicPPL.accumulate_assume!!"},{"category":"function","location":"api/#DynamicPPL.accumulate_observe!!","page":"API","text":"accumulate_observe!!(acc::AbstractAccumulator, right, left, vn, template)\n\nUpdate acc in a tilde_observe!! call. Returns the updated acc.\n\nSee AbstractAccumulator for the meaning of the arguments.\n\naccumulate_observe!! may mutate acc, but not any of the other arguments.\n\nSee also: accumulate_assume!!\n\n\n\n\n\n","title":"DynamicPPL.accumulate_observe!!"},{"category":"function","location":"api/#DynamicPPL.accumulator_name","page":"API","text":"accumulator_name(acc::AbstractAccumulator)\n\nReturn a Symbol which can be used as a name for acc.\n\nThe name has to be unique in the sense that a VarInfo can only have one accumulator for each name. The most typical case, and the default implementation, is that the name only depends on the type of acc, not on its value.\n\n\n\n\n\n","title":"DynamicPPL.accumulator_name"},{"category":"function","location":"api/#DynamicPPL.reset","page":"API","text":"reset(acc::AbstractAccumulator)\n\nReturn a new accumulator like acc, but with its contents reset to the state that they should be at the beginning of model evaluation.\n\nNote that this may in general have very similar behaviour to split, and may share the same implementation, but the difference is that split may in principle happen at any stage during model evaluation, whereas reset is only called at the beginning of model evaluation.\n\n\n\n\n\n","title":"DynamicPPL.reset"},{"category":"function","location":"api/#DynamicPPL.split","page":"API","text":"split(acc::AbstractAccumulator)\n\nReturn a new accumulator like acc suitable for use in a forked thread.\n\nThe returned value should be such that combine(acc, split(acc)) is equal to acc. This is used in the context of multi-threading where different threads may accumulate independently and the results are then combined.\n\nNote that this may in general have very similar behaviour to reset, but is semantically different. See reset for more details.\n\nSee also: combine\n\n\n\n\n\n","title":"DynamicPPL.split"},{"category":"function","location":"api/#DynamicPPL.combine","page":"API","text":"combine(acc::AbstractAccumulator, acc2::AbstractAccumulator)\n\nCombine two accumulators which have the same type (but may, in general, have different type parameters). Returns a new accumulator of the same type.\n\nSee also: split\n\n\n\n\n\n","title":"DynamicPPL.combine"},{"category":"type","location":"api/#DynamicPPL.LogProbType","page":"API","text":"DynamicPPL.LogProbType\n\nThe default type used for log-probabilities in DynamicPPL.jl. This is a compile-time constant that can be set via set_logprob_type!, which under the hood uses Preferences.jl.\n\nNote that this does not prevent computations within the model from promoting the log-probability to a different type. In essence, LogProbType specifies the lowest possible type that log-probabilities can be, and DynamicPPL promises to not insert any extra operations that would cause this to be promoted to a higher type. However, DynamicPPL cannot guard against user code inside models.\n\nFor example, in:\n\n@model f() = x ~ Normal(0.0, 1.0)\n\nthe log-probability of the model will always be promoted to Float64, regardless of the value of LogProbType, because the logpdf of Normal(0.0, 1.0) is a Float64. On the other hand, in:\n\n@model f() = x ~ Normal(0.0f0, 1.0f0)\n\nthe log-probability of the model will be Float32 if LogProbType is Float32 or lower.\n\n\n\n\n\n","title":"DynamicPPL.LogProbType"},{"category":"function","location":"api/#DynamicPPL.set_logprob_type!","page":"API","text":"set_logprob_type!(::Type{T}) where {T}\n\nSet the log probability type for DynamicPPL.jl, DynamicPPL.LogProbType, to T. Permitted values are Float64, Float32, Float16, and NoLogProb. The default in DynamicPPL is Float64.\n\nNoLogProb is a special type that is the \"lowest\" possible float type. This means that the log probability will be promoted to whatever type the model dictates. This is a totally unintrusive option, which can be useful if you do not know in advance what log probability type you are targeting, or want to troubleshoot a model to see what type the log probability is being promoted to. However, this can also cause type stability issues and performance degradations, so we generally recommend setting a specific log probability type if you know what type you want to target.\n\nThis function uses Preferences.jl to set a compile-time constant, so you will need to restart your Julia session for the change to take effect.\n\n\n\n\n\n","title":"DynamicPPL.set_logprob_type!"},{"category":"type","location":"api/#DynamicPPL.NoLogProb","page":"API","text":"DynamicPPL.NoLogProb <: Real\n\nSingleton type that represents the absence of a log probability value. This is used as the default type parameter for LogProbAccumulator when no log probability value is needed, to avoid defining a concrete type such as Float64 that would cause unwanted type promotion when accumulating log probabilities of other types (e.g., Float32).\n\nAdding anything to NoLogProb() returns the other thing. In other words, NoLogProb is a true additive identity which additionally preserves types.\n\n\n\n\n\n","title":"DynamicPPL.NoLogProb"},{"category":"type","location":"api/#DynamicPPL.VNTAccumulator","page":"API","text":"VNTAccumulator{AccName}(f::F, values::VarNamedTuple=VarNamedTuple()) where {AccName,F}\n\nA generic accumulator that applies a function f to values seen during model execution and stores the results in a VarNamedTuple.\n\nAccName is the name of the accumulator, and is exposed to allow users to define and use multiple forms of VNTAccumulator within the same set of accumulators. In theory, each VNTAccumulator with the same function f should use the same accumulator name. This is not enforced.\n\nThe function f should have the signature:\n\nf(val, tval, logjac, vn, dist) -> value_to_store\n\nwhere val, tval, logjac, vn, and dist have their usual meanings in accumulate_assume!! (see its docstring for more details). If a value does not need to be accumulated, this can be signalled by returning DoNotAccumulate() from f.\n\nIf f is a struct (and not a function), you also need to define Base.copy on f.\n\n\n\n\n\n","title":"DynamicPPL.VNTAccumulator"},{"category":"type","location":"api/#DynamicPPL.DoNotAccumulate","page":"API","text":"DoNotAccumulate()\n\nSentinel value indicating that no accumulation should be performed for a given variable.\n\n\n\n\n\n","title":"DynamicPPL.DoNotAccumulate"},{"category":"function","location":"api/#DynamicPPL.getacc","page":"API","text":"getacc(at::AccumulatorTuple, ::Val{accname})\n\nGet the accumulator with name accname from at.\n\n\n\n\n\ngetacc(vi::AbstractVarInfo, ::Val{accname})\n\nReturn the AbstractAccumulator of vi with name accname.\n\n\n\n\n\n","title":"DynamicPPL.getacc"},{"category":"function","location":"api/#DynamicPPL.setacc!!","page":"API","text":"setacc!!(at::AccumulatorTuple, acc::AbstractAccumulator)\n\nAdd acc to at. Returns a new AccumulatorTuple.\n\nIf an AbstractAccumulator with the same accumulator_name already exists in at it is replaced. at will never be mutated, but the name has the !! for consistency with the corresponding function for AbstractVarInfo.\n\n\n\n\n\nsetacc!!(vi::AbstractVarInfo, acc::AbstractAccumulator)\n\nAdd acc to the AccumulatorTuple of vi, mutating if it makes sense.\n\nIf an accumulator with the same accumulator_name already exists, it will be replaced.\n\n\n\n\n\n","title":"DynamicPPL.setacc!!"},{"category":"function","location":"api/#DynamicPPL.setaccs!!","page":"API","text":"setaccs!!(vi::AbstractVarInfo, accs::AccumulatorTuple)\nsetaccs!!(vi::AbstractVarInfo, accs::NTuple{N,AbstractAccumulator} where {N})\n\nUpdate the AccumulatorTuple of vi to accs, mutating if it makes sense.\n\nsetaccs!!(vi:AbstractVarInfo, accs::AccumulatorTuple) should be implemented by each subtype ofAbstractVarInfo`.\n\n\n\n\n\n","title":"DynamicPPL.setaccs!!"},{"category":"function","location":"api/#DynamicPPL.deleteacc!!","page":"API","text":"deleteacc!!(at::AccumulatorTuple, ::Val{accname})\n\nDelete the accumulator with name accname from at. Returns a new AccumulatorTuple.\n\n\n\n\n\n","title":"DynamicPPL.deleteacc!!"},{"category":"function","location":"api/#DynamicPPL.getlogp","page":"API","text":"getlogp(vi::AbstractVarInfo)\n\nReturn a NamedTuple of the log prior, log Jacobian, and log likelihood probabilities.\n\nThe keys are called logprior, logjac, and loglikelihood. If any of them are not present in vi an error will be thrown.\n\n\n\n\n\n","title":"DynamicPPL.getlogp"},{"category":"function","location":"api/#DynamicPPL.setlogp!!","page":"API","text":"setlogp!!(vi::AbstractVarInfo, logp::NamedTuple)\n\nSet both the log prior and the log likelihood probabilities in vi.\n\nlogp should have fields logprior and loglikelihood and no other fields.\n\nSee also: setlogprior!!, setloglikelihood!!, getlogp.\n\n\n\n\n\n","title":"DynamicPPL.setlogp!!"},{"category":"function","location":"api/#DynamicPPL.acclogp!!","page":"API","text":"acclogp!!(vi::AbstractVarInfo, logp::NamedTuple; ignore_missing_accumulator::Bool=false)\n\nAdd to both the log prior and the log likelihood probabilities in vi.\n\nlogp should have fields logprior and/or loglikelihood, and no other fields.\n\nBy default if the necessary accumulators are not in vi an error is thrown. If ignore_missing_accumulator is set to true then this is silently ignored instead.\n\n\n\n\n\n","title":"DynamicPPL.acclogp!!"},{"category":"function","location":"api/#DynamicPPL.getlogjoint","page":"API","text":"getlogjoint(vi::AbstractVarInfo)\n\nReturn the log of the joint probability of the observed data and parameters in vi.\n\nSee also: getlogprior, getloglikelihood.\n\n\n\n\n\n","title":"DynamicPPL.getlogjoint"},{"category":"function","location":"api/#DynamicPPL.getlogjoint_internal","page":"API","text":"getlogjoint_internal(vi::AbstractVarInfo)\n\nReturn the log of the joint probability of the observed data and parameters as they are stored internally in vi, including the log-Jacobian for any linked parameters.\n\nIn general, we have that:\n\ngetlogjoint_internal(vi) == getlogjoint(vi) - getlogjac(vi)\n\n\n\n\n\n","title":"DynamicPPL.getlogjoint_internal"},{"category":"function","location":"api/#DynamicPPL.getlogjac","page":"API","text":"getlogjac(vi::AbstractVarInfo)\n\nReturn the accumulated log-Jacobian term for any linked parameters in vi. The Jacobian here is taken with respect to the forward (link) transform.\n\nSee also: setlogjac!!.\n\n\n\n\n\n","title":"DynamicPPL.getlogjac"},{"category":"function","location":"api/#DynamicPPL.setlogjac!!","page":"API","text":"setlogjac!!(vi::AbstractVarInfo, logjac)\n\nSet the accumulated log-Jacobian term for any linked parameters in vi. The Jacobian here is taken with respect to the forward (link) transform.\n\nSee also: getlogjac, acclogjac!!.\n\n\n\n\n\n","title":"DynamicPPL.setlogjac!!"},{"category":"function","location":"api/#DynamicPPL.acclogjac!!","page":"API","text":"acclogjac!!(vi::AbstractVarInfo, logjac; ignore_missing_accumulator::Bool=false)\n\nAdd logjac to the value of the log Jacobian in vi.\n\nErrors if vi does not have a LogJacobianAccumulator, unless ignore_missing_accumulator is set to true, in which case this is silently ignored instead.\n\nSee also: getlogjac, setlogjac!!.\n\n\n\n\n\n","title":"DynamicPPL.acclogjac!!"},{"category":"function","location":"api/#DynamicPPL.getlogprior","page":"API","text":"getlogprior(vi::AbstractVarInfo)\n\nReturn the log of the prior probability of the parameters in vi.\n\nSee also: getlogjoint, getloglikelihood, setlogprior!!.\n\n\n\n\n\n","title":"DynamicPPL.getlogprior"},{"category":"function","location":"api/#DynamicPPL.getlogprior_internal","page":"API","text":"getlogprior_internal(vi::AbstractVarInfo)\n\nReturn the log of the prior probability of the parameters as stored internally in vi. This includes the log-Jacobian for any linked parameters.\n\nIn general, we have that:\n\ngetlogprior_internal(vi) == getlogprior(vi) - getlogjac(vi)\n\n\n\n\n\n","title":"DynamicPPL.getlogprior_internal"},{"category":"function","location":"api/#DynamicPPL.setlogprior!!","page":"API","text":"setlogprior!!(vi::AbstractVarInfo, logp)\n\nSet the log of the prior probability of the parameters sampled in vi to logp.\n\nSee also: setloglikelihood!!, setlogp!!, getlogprior.\n\n\n\n\n\n","title":"DynamicPPL.setlogprior!!"},{"category":"function","location":"api/#DynamicPPL.acclogprior!!","page":"API","text":"acclogprior!!(vi::AbstractVarInfo, logp)\n\nAdd logp to the value of the log of the prior probability in vi.\n\nErrors if vi does not have a LogPriorAccumulator, unless ignore_missing_accumulator is set to true, in which case this is silently ignored instead.\n\nSee also: accloglikelihood!!, acclogp!!, getlogprior, setlogprior!!.\n\n\n\n\n\n","title":"DynamicPPL.acclogprior!!"},{"category":"function","location":"api/#DynamicPPL.getloglikelihood","page":"API","text":"getloglikelihood(vi::AbstractVarInfo)\n\nReturn the log of the likelihood probability of the observed data in vi.\n\nSee also: getlogjoint, getlogprior, setloglikelihood!!.\n\n\n\n\n\n","title":"DynamicPPL.getloglikelihood"},{"category":"function","location":"api/#DynamicPPL.setloglikelihood!!","page":"API","text":"setloglikelihood!!(vi::AbstractVarInfo, logp)\n\nSet the log of the likelihood probability of the observed data sampled in vi to logp.\n\nSee also: setlogprior!!, setlogp!!, getloglikelihood.\n\n\n\n\n\n","title":"DynamicPPL.setloglikelihood!!"},{"category":"function","location":"api/#DynamicPPL.accloglikelihood!!","page":"API","text":"accloglikelihood!!(vi::AbstractVarInfo, logp)\n\nAdd logp to the value of the log of the likelihood in vi.\n\nErrors if vi does not have a LogLikelihoodAccumulator, unless ignore_missing_accumulator is set to true, in which case this is silently ignored instead.\n\nSee also: accloglikelihood!!, acclogp!!, getloglikelihood, setloglikelihood!!.\n\n\n\n\n\n","title":"DynamicPPL.accloglikelihood!!"},{"category":"function","location":"api/#Base.keys","page":"API","text":"keys(vi::AbstractVarInfo)\n\nReturn an iterator over all vns in vi.\n\n\n\n\n\n","title":"Base.keys"},{"category":"function","location":"api/#BangBang.empty!!","page":"API","text":"empty!!(vi::AbstractVarInfo)\n\nEmpty vi of variables and reset all accumulators.\n\nThis is useful when using a sampling algorithm that assumes an empty vi, e.g. SMC.\n\n\n\n\n\n","title":"BangBang.empty!!"},{"category":"function","location":"api/#Base.isempty","page":"API","text":"isempty(vi::AbstractVarInfo)\n\nReturn true if vi is empty and false otherwise.\n\n\n\n\n\n","title":"Base.isempty"},{"category":"function","location":"api/#DynamicPPL.getindex_internal","page":"API","text":"getindex_internal(vi::AbstractVarInfo, vn::VarName)\ngetindex_internal(vi::AbstractVarInfo, vns::Vector{<:VarName})\ngetindex_internal(vi::AbstractVarInfo, ::Colon)\n\nReturn the internal value of the varname vn, varnames vns, or all varnames in vi respectively. The internal value is the value of the variables that is stored in the varinfo object; this may be the actual realisation of the random variable (i.e. the value sampled from the distribution), or it may have been transformed to Euclidean space, depending on whether the varinfo was linked.\n\nSee https://turinglang.org/docs/developers/transforms/dynamicppl/ for more information on how transformed variables are stored in DynamicPPL.\n\n\n\n\n\n","title":"DynamicPPL.getindex_internal"},{"category":"function","location":"api/#DynamicPPL.setindex_internal!!","page":"API","text":"setindex_internal!!(vi::VarInfo, val, vn::VarName)\n\nSet the internal (vectorised) value of variable vn in vi to val.\n\nThis does not change the transformation or linked status of the variable.\n\n\n\n\n\n","title":"DynamicPPL.setindex_internal!!"},{"category":"function","location":"api/#Bijectors.link","page":"API","text":"link(vi::AbstractVarInfo, model::Model)\nlink(vi::AbstractVarInfo, vns::NTuple{N,VarName}, model::Model)\n\nTransform all variables in vi to their linked space without mutating vi (i.e., replace all the TransformedValues in vi.values with the corresponding TransformedValue(linked_value, DynamicLink()). If vns is provided, then only transform the variables in vns.\n\nNote that if vi contains variables that have fixed transforms, the fixed transforms will be overwritten.\n\nSee also: invlink.\n\n\n\n\n\n","title":"Bijectors.link"},{"category":"function","location":"api/#Bijectors.invlink","page":"API","text":"invlink(vi::AbstractVarInfo, model::Model)\ninvlink(vi::AbstractVarInfo, vns::NTuple{N,VarName}, model::Model)\n\nTransform all variables in vi to the original space without mutating vi (i.e., replace all the TransformedValues in vi.values with the corresponding TransformedValue(unlinked_value, Unlink()). Note that the unlinked values are still vectorised (that is a requirement of vi.values). If vns is provided, then only transform the variables in vns.\n\nNote that if vi contains variables that have fixed transforms, the fixed transforms will be overwritten.\n\nSee also: link.\n\n\n\n\n\n","title":"Bijectors.invlink"},{"category":"function","location":"api/#DynamicPPL.link!!","page":"API","text":"link!!(vi::AbstractVarInfo, model::Model)\nlink!!(vi::AbstractVarInfo, vns::NTuple{N,VarName}, model::Model)\n\nLike link, but might mutate vi in-place if it is possible to do so.\n\n\n\n\n\n","title":"DynamicPPL.link!!"},{"category":"function","location":"api/#DynamicPPL.invlink!!","page":"API","text":"invlink!!(vi::AbstractVarInfo, model::Model)\ninvlink!!(vi::AbstractVarInfo, vns::NTuple{N,VarName}, model::Model)\n\nLike invlink, but might mutate vi in-place if it is possible to do so.\n\n\n\n\n\n","title":"DynamicPPL.invlink!!"},{"category":"function","location":"api/#DynamicPPL.update_transform_status!!","page":"API","text":"DynamicPPL.update_transform_status!!(\n    orig_vi::VarInfo, transform_strategy::AbstractTransformStrategy, model::Model\n)\n\nGiven an original VarInfo orig_vi, update the link status of its variables according to the new transform_strategy.\n\n\n\n\n\n","title":"DynamicPPL.update_transform_status!!"},{"category":"type","location":"api/#DynamicPPL.AbstractTransformStrategy","page":"API","text":"abstract type AbstractTransformStrategy end\n\nAn abstract type for strategies that determine how each variable should be transformed.\n\nFor subtypes of AbstractTransformStrategy, the only method that needs to be overloaded is DynamicPPL.target_transform(::AbstractTransformStrategy, vn::VarName), which returns an AbstractTransform that specifies how the variable with name vn should be transformed.\n\nThe transform strategy dictates how the log-Jacobian is accumulated during model evaluation. Regardless of what initialisation strategy is used (and what kind of transformed value init() returns, the log-Jacobian that is accumulated is always the log-Jacobian for the forward transform specified by target_transform(strategy, vn).\n\nThat is, even if init() returns an unlinked or untransformed value, if the transform strategy is LinkAll() (which returns DynamicLink for all variables), then the log-Jacobian for linking will be accumulated during model evaluation.\n\nSubtypes in DynamicPPL are LinkAll, UnlinkAll, LinkSome, and UnlinkSome.\n\n\n\n\n\n","title":"DynamicPPL.AbstractTransformStrategy"},{"category":"type","location":"api/#DynamicPPL.LinkAll","page":"API","text":"LinkAll() <: AbstractTransformStrategy\n\nIndicate that all variables should be linked.\n\n\n\n\n\n","title":"DynamicPPL.LinkAll"},{"category":"type","location":"api/#DynamicPPL.UnlinkAll","page":"API","text":"UnlinkAll() <: AbstractTransformStrategy\n\nIndicate that all variables should be unlinked.\n\n\n\n\n\n","title":"DynamicPPL.UnlinkAll"},{"category":"type","location":"api/#DynamicPPL.LinkSome","page":"API","text":"LinkSome(vns::Set{<:VarName}, fallback) <: AbstractTransformStrategy\n\nIndicate that the variables in vns must be linked. The link statuses of other variables are determined by the fallback strategy.\n\n\n\n\n\n","title":"DynamicPPL.LinkSome"},{"category":"type","location":"api/#DynamicPPL.UnlinkSome","page":"API","text":"UnlinkSome(vns::Set{<:VarName}, fallback) <: AbstractTransformStrategy\n\nIndicate that the variables in vns must not be linked. The link statuses of other variables are determined by the fallback strategy.\n\n\n\n\n\n","title":"DynamicPPL.UnlinkSome"},{"category":"type","location":"api/#DynamicPPL.WithTransforms","page":"API","text":"WithTransforms(transforms::VarNamedTuple, fallback) <: AbstractTransformStrategy\n\nIndicate that the variables in transforms should be transformed according to their corresponding values in transforms, which should be subtypes of AbstractTransform. The link statuses of other variables are determined by the fallback strategy.\n\n\n\n\n\n","title":"DynamicPPL.WithTransforms"},{"category":"type","location":"api/#DynamicPPL.AbstractTransform","page":"API","text":"abstract type AbstractTransform end\n\nAn abstract type to represent the intended transformation for a variable.\n\n\n\n\n\n","title":"DynamicPPL.AbstractTransform"},{"category":"type","location":"api/#DynamicPPL.DynamicLink","page":"API","text":"DynamicLink <: AbstractTransform\n\nA type indicating that a target transformation should be derived by recomputing Bijectors.VectorBijectors.from_linked_vec(dist), where dist is the distribution on the right-hand side of the tilde.\n\n\n\n\n\n","title":"DynamicPPL.DynamicLink"},{"category":"type","location":"api/#DynamicPPL.Unlink","page":"API","text":"Unlink <: AbstractTransform\n\nA type indicating that a target transformation should be derived by recomputing Bijectors.VectorBijectors.from_vec(dist), where dist is the distribution on the right-hand side of the tilde.\n\n\n\n\n\n","title":"DynamicPPL.Unlink"},{"category":"type","location":"api/#DynamicPPL.FixedTransform","page":"API","text":"FixedTransform{F} <: AbstractTransform\n\nA type to represent a fixed (static) transformation of type F.\n\n\n\n\n\n","title":"DynamicPPL.FixedTransform"},{"category":"type","location":"api/#DynamicPPL.NoTransform","page":"API","text":"NoTransform <: AbstractTransform\n\nA type indicating that the value is not transformed.\n\n\n\n\n\n","title":"DynamicPPL.NoTransform"},{"category":"function","location":"api/#DynamicPPL.target_transform","page":"API","text":"target_transform(linker::AbstractTransformStrategy, vn::VarName)::AbstractTransform\n\nDetermine whether a variable with name vn should be linked according to the linker strategy. Returns a subtype of AbstractTransform that indicates the intended transformation for the variable.\n\n\n\n\n\n","title":"DynamicPPL.target_transform"},{"category":"function","location":"api/#DynamicPPL.apply_transform_strategy","page":"API","text":"DynamicPPL.apply_transform_strategy(\n    strategy::AbstractTransformStrategy,\n    tv::TransformedValue,\n    vn::VarName,\n    dist::Distribution,\n)\n\nApply the given strategy to the transformed value tv for a tilde-statement vn ~ dist.\n\nSpecifically, this function does a number of things:\n\nCalculates the raw value associated with tv.\nChecks whether the strategy expects the VarName vn to be linked or unlinked. If the current link status of tv matches the expected link status, tv is returned unchanged. Otherwise, either linking or unlinking is applied as necessary. Note that this function does not perform vectorisation unless it is needed.\nA table summarising the possible transformations is as follows:\ntv.transform isa ... target_transform(...) isa DynamicLink target_transform(...) isa Unlink\nDynamicLink -> DynamicLink -> NoTransform\nUnlink -> DynamicLink -> Unlink\nNoTransform -> DynamicLink -> NoTransform\nFixedTransform errors errors\nNote that, for the last row, when using FixedTransform we require that target_transform exactly matches the fixed transform, otherwise an error is thrown.\nIf vn is supposed to be linked, calculates the associated log-Jacobian adjustment for the forward linking transformation (i.e., from unlinked to linked).\n\nThis function returns a tuple of (raw_value, new_tv, logjac).\n\nnote: Note\nThis function is therefore the single source of truth for whether logjac should be incremented during model evaluation.\n\n\n\n\n\n","title":"DynamicPPL.apply_transform_strategy"},{"category":"method","location":"api/#Base.merge-Tuple{AbstractVarInfo}","page":"API","text":"merge(varinfo, other_varinfos...)\n\nMerge varinfos into one, giving precedence to the right-most varinfo when sensible.\n\nThis is particularly useful when combined with subset(varinfo, vns).\n\nSee docstring of subset(varinfo, vns) for examples.\n\n\n\n\n\n","title":"Base.merge"},{"category":"function","location":"api/#DynamicPPL.subset","page":"API","text":"subset(varinfo::AbstractVarInfo, vns::AbstractVector{<:VarName})\n\nSubset a varinfo to only contain the variables vns.\n\nThe ordering of variables in the return value will be the same as in varinfo.\n\nExamples\n\njulia> using DynamicPPL, Distributions\n\njulia> @model function demo()            s ~ InverseGamma(2, 3)            m ~ Normal(0, sqrt(s))            x = Vector{Float64}(undef, 2)            x[1] ~ Normal(m, sqrt(s))            x[2] ~ Normal(m, sqrt(s))        end;\n\njulia> model = demo();\n\njulia> params = @vnt begin            s := 1.0            m := 2.0            x := [3.0, 4.0]        end VarNamedTuple ├─ s => 1.0 ├─ m => 2.0 └─ x => [3.0, 4.0]\n\njulia> vi = last(init!!(model, VarInfo(), InitFromParams(params), UnlinkAll()));\n\njulia> vi.values 4-element Vector{VarName}:  s  m  x[1]  x[2]\n\njulia> # Extract one with only m.        vi_subset1 = subset(vi, [@varname(m),]);\n\njulia> keys(vi_subset1) 1-element Vector{VarName}:  m\n\njulia> DynamicPPL.getindexinternal(visubset1, @varname(m)) 1-element Vector{Float64}:  2.0\n\njulia> # Extract one with both s and x[2].        vi_subset2 = subset(vi, [@varname(s), @varname(x[2])]);\n\njulia> keys(vi_subset2) 2-element Vector{VarName}:  s  x[2]\n\njulia> DynamicPPL.getindexinternal(visubset2, @varname(s)) 1-element Vector{Float64}:  1.0\n\njulia> DynamicPPL.getindexinternal(visubset2, @varname(x[2])) 1-element Vector{Float64}:  4.0\n\n\n`subset` is particularly useful when combined with [`merge(vi::AbstractVarInfo)`](@ref)\n\n\njldoctest varinfo-subset julia> # Merge the two.        visubsetmerged = merge(visubset1, visubset2);\n\njulia> keys(visubsetmerged) 3-element Vector{VarName}:  m  s  x[2]\n\njulia> # Merge the two with the original.        vimerged = merge(vi, visubset_merged);\n\njulia> keys(vi_merged) 4-element Vector{VarName}:  s  m  x[1]  x[2] ```\n\n\n\n\n\n","title":"DynamicPPL.subset"},{"category":"function","location":"api/#DynamicPPL.unflatten!!","page":"API","text":"unflatten!!(vi::AbstractVarInfo, x::AbstractVector)\n\nReturn a new instance of vi where the internal values stored in vi.values have been overwritten with the values in x.\n\nThis is the inverse operation of internal_values_as_vector.\n\nwarning: The VarInfo will be in an invalid state\nNote that this does not re-evaluate the model (indeed it cannot!) so the contents of any accumulators in the VarInfo will almost certainly be inconsistent with the new values.Because of these issues, we strongly recommend against using this function, unless absolutely necessary. In many cases, usage of unflatten!!(vi, x) can be replaced with InitFromVector(x, ldf::LogDensityFunction): please see the DynamicPPL documentation for more information. If you are unsure how to adapt your code to avoid using unflatten!!, please open an issue.\n\n\n\n\n\n","title":"DynamicPPL.unflatten!!"},{"category":"function","location":"api/#DynamicPPL.internal_values_as_vector","page":"API","text":"internal_values_as_vector(vi::AbstractVarInfo)\n\nReturn all internal values stored in vi.values as a flattened Vector.\n\n\n\n\n\n","title":"DynamicPPL.internal_values_as_vector"},{"category":"function","location":"api/#AbstractPPL.evaluate!!","page":"API","text":"evaluate!!(model::Model, varinfo)\n\nEvaluate the model with the given varinfo, wrapping it in a ThreadSafeVarInfo if the model is marked as needing threadsafe evaluation.\n\nwarning: Warning\nThe semantics of this method are complicated. We strongly recommend that users do not use this method unless absolutely necessary. In the future this method will be deprecated and removed. As far as possible (and it should always be possible – please open an issue if you do not know how to adapt your code!) you should use the five-argument init!!([rng,] model, ::OnlyAccsVarInfo, init_strategy, transform_strategy) method, which has more explicit semantics and allows you to have more control over each part of the evaluation process.\n\nThe exact semantics depend on the model's context. Fundamentally, this method executes the model evaluation function (i.e., the function used to define the model) using the given varinfo as an argument. At each tilde-statement, tilde_assume!! or tilde_observe!! is called, whose behaviour depends on the model's context.\n\nBroadly speaking, if the leaf context is an InitContext, then this function:\n\nuses the initialisation strategy inside the InitContext;\nuses the transform strategy inside the InitContext;\nuses the accumulators inside varinfo (resetting them before evaluation);\noverwrites the values in varinfo with the new values obtained from the initialisation strategy.\n\nIf the leaf context is a DefaultContext, then this function:\n\nuses the values inside the varinfo as the initialisation strategy;\nderives a transform strategy from the varinfo's stored variables (if a linked variable is stored, then the transform strategy will treat that variable as linked; likewise for unlinked)\nuses the accumulators inside varinfo (resetting them before evaluation);\ndoes not overwrite the values in the varinfo (that is unnecessary since the values used for evaluation are already stored in varinfo).\n\nThe long-term plan for this method is to:\n\nReplace DefaultContext with InitContext by splitting up the functionality of DefaultContext into its constituent components\nRemove the VarInfo argument, and instead use only an AccumulatorTuple\nSeparate the initialisation and transform strategies into separate arguments, instead of storing them inside the model's context.\n\n\n\n\n\n","title":"AbstractPPL.evaluate!!"},{"category":"type","location":"api/#DynamicPPL.DefaultContext","page":"API","text":"struct DefaultContext <: AbstractContext end\n\nDefaultContext, as the name suggests, is the default context used when instantiating a model.\n\njulia> @model f() = x ~ Normal();\n\njulia> model = f(); model.context\nDefaultContext()\n\nAs an evaluation context, the behaviour of DefaultContext is to require all variables to be present in the AbstractVarInfo used for evaluation. Thus, semantically, evaluating a model with DefaultContext means 'calculating the log-probability associated with the variables in the AbstractVarInfo'.\n\n\n\n\n\n","title":"DynamicPPL.DefaultContext"},{"category":"type","location":"api/#DynamicPPL.InitContext","page":"API","text":"InitContext(\n    [rng::Random.AbstractRNG=Random.default_rng()],\n    strategy::AbstractInitStrategy,\n    transform_strategy::AbstractTransformStrategy,\n)\n\nA leaf context that indicates that new values for random variables are currently being obtained through sampling. Used e.g. when initialising a fresh VarInfo.\n\nThe strategy argument specifies how new values are to be obtained (see AbstractInitStrategy for details), while the transform_strategy argument specifies whether values should be treated as being in linked or unlinked space. That also means that transform_strategy determines whether the log-Jacobian of the link transform is included when evaluating the model.\n\nnote: Note\nIf leafcontext(model.context) isa InitContext, then evaluate!!(model, varinfo) will override all values in the VarInfo.\n\n\n\n\n\n","title":"DynamicPPL.InitContext"},{"category":"function","location":"api/#DynamicPPL.tilde_assume!!","page":"API","text":"DynamicPPL.tilde_assume!!(\n    context::AbstractContext,\n    right::Distribution,\n    vn::VarName,\n    template::Any,\n    vi::AbstractVarInfo\n)::Tuple{Any,AbstractVarInfo}\n\nHandle assumed variables, i.e. anything which is not observed (see tilde_observe!!). Accumulate the associated log probability, and return the sampled value and updated vi.\n\nvn is the VarName on the left-hand side of the tilde statement.\n\ntemplate is the value of the top-level symbol in vn.\n\nThis function should return a tuple (x, vi), where x is the sampled value (which must be untransformed, i.e., insupport(right, x) must be true!) and vi is the updated VarInfo.\n\n\n\n\n\nDynamicPPL.tilde_assume!!(\n    ::DefaultContext,\n    right::Distribution,\n    vn::VarName,\n    template::Any,\n    vi::AbstractVarInfo\n)\n\nHandle assumed variables. For DefaultContext, this function extracts the value associated with vn from vi, If vi does not contain an appropriate value then this will error.\n\n\n\n\n\nDynamicPPL.tilde_assume!!(\n    context::AbstractContext,\n    right::DynamicPPL.Submodel,\n    vn::VarName,\n    ::Any,\n    vi::AbstractVarInfo\n)\n\nEvaluate the submodel with the given context.\n\n\n\n\n\n","title":"DynamicPPL.tilde_assume!!"},{"category":"function","location":"api/#DynamicPPL.tilde_observe!!","page":"API","text":"DynamicPPL.tilde_observe!!(\n    context::AbstractContext,\n    right::Distribution,\n    left,\n    vn::Union{VarName, Nothing},\n    template::Any,\n    vi::AbstractVarInfo\n)::Tuple{Any,AbstractVarInfo}\n\nThis function handles observed variables, which may be:\n\nliterals on the left-hand side, e.g., 3.0 ~ Normal()\na model input, e.g. x ~ Normal() in a model @model f(x) ... end\na conditioned or fixed variable, e.g. x ~ Normal() in a model model | (; x = 3.0).\n\nThe relevant log-probability associated with the observation is computed and accumulated in the VarInfo object vi (except for fixed variables, which do not contribute to the log-probability).\n\nleft is the actual value that the left-hand side evaluates to. vn is the VarName on the left-hand side, or nothing if the left-hand side is a literal value. template is the value of the top-level symbol in vn; if vn is nothing, then template will be NoTemplate().\n\nThis function should return a tuple (left, vi), where left is the same as the input, and vi is the updated VarInfo.\n\n\n\n\n\nDynamicPPL.tilde_observe!!(\n    ::DefaultContext,\n    right::Distribution,\n    left,\n    vn::Union{VarName,Nothing},\n    template::Any,\n    vi::AbstractVarInfo,\n)\n\nHandle observed variables. This just accumulates the log-likelihood for left.\n\n\n\n\n\n","title":"DynamicPPL.tilde_observe!!"},{"category":"type","location":"api/#DynamicPPL.AbstractParentContext","page":"API","text":"AbstractParentContext\n\nAn abstract context that has a child context.\n\nSubtypes of AbstractParentContext must implement the following interface:\n\nDynamicPPL.childcontext(context::AbstractParentContext): Return the child context.\nDynamicPPL.setchildcontext(parent::AbstractParentContext, child::AbstractContext): Reconstruct parent but now using child as its child context.\n\n\n\n\n\n","title":"DynamicPPL.AbstractParentContext"},{"category":"function","location":"api/#DynamicPPL.childcontext","page":"API","text":"childcontext(context::AbstractParentContext)\n\nReturn the descendant context of context.\n\n\n\n\n\n","title":"DynamicPPL.childcontext"},{"category":"function","location":"api/#DynamicPPL.setchildcontext","page":"API","text":"setchildcontext(parent::AbstractParentContext, child::AbstractContext)\n\nReconstruct parent but now using child is its childcontext, effectively updating the child context.\n\nExamples\n\njulia> using DynamicPPL: InitContext, CondFixContext, Condition\n\njulia> ctx = CondFixContext{Condition}(VarNamedTuple(; a = 1));\n\njulia> DynamicPPL.childcontext(ctx)\nDefaultContext()\n\njulia> ctx_prior = DynamicPPL.setchildcontext(ctx, InitContext(MersenneTwister(23), InitFromPrior(), UnlinkAll()));\n\njulia> DynamicPPL.childcontext(ctx_prior)\nInitContext{MersenneTwister, InitFromPrior, UnlinkAll}(MersenneTwister(23), InitFromPrior(), UnlinkAll())\n\n\n\n\n\n","title":"DynamicPPL.setchildcontext"},{"category":"function","location":"api/#DynamicPPL.leafcontext","page":"API","text":"leafcontext(context::AbstractContext)\n\nReturn the leaf of context, i.e. the first descendant context that is not an AbstractParentContext.\n\n\n\n\n\n","title":"DynamicPPL.leafcontext"},{"category":"function","location":"api/#DynamicPPL.setleafcontext","page":"API","text":"setleafcontext(left::AbstractContext, right::AbstractContext)\n\nReturn left but now with its leaf context replaced by right.\n\nNote that this also works even if right is not a leaf context, in which case effectively append right to left, dropping the original leaf context of left.\n\nExamples\n\njulia> using DynamicPPL: leafcontext, setleafcontext, childcontext, setchildcontext, AbstractContext, InitContext\n\njulia> struct ParentContext{C} <: AbstractParentContext\n           context::C\n       end\n\njulia> DynamicPPL.childcontext(context::ParentContext) = context.context\n\njulia> DynamicPPL.setchildcontext(::ParentContext, child) = ParentContext(child)\n\njulia> Base.show(io::IO, c::ParentContext) = print(io, \"ParentContext(\", childcontext(c), \")\")\n\njulia> ctx = ParentContext(ParentContext(DefaultContext()))\nParentContext(ParentContext(DefaultContext()))\n\njulia> # Replace the leaf context with another leaf.\n       leafcontext(setleafcontext(ctx, InitContext(MersenneTwister(23), InitFromPrior(), UnlinkAll())))\nInitContext{MersenneTwister, InitFromPrior, UnlinkAll}(MersenneTwister(23), InitFromPrior(), UnlinkAll())\n\njulia> # Append another parent context.\n       setleafcontext(ctx, ParentContext(DefaultContext()))\nParentContext(ParentContext(ParentContext(DefaultContext())))\n\n\n\n\n\nsetleafcontext(model::Model, context::AbstractContext)\n\nReturn a new Model with its leaf context set to context. This is a convenience shortcut for contextualize(model, setleafcontext(model.context, context)).\n\n\n\n\n\n","title":"DynamicPPL.setleafcontext"},{"category":"function","location":"api/#DynamicPPL.extract_prefixes","page":"API","text":"DynamicPPL.extract_prefixes(ctx::AbstractContext)\n\nRecursively process the context stack ctx to remove all levels of PrefixContext, and aggregate their prefixes into a single VarName which can be used to apply all the prefixes at once.\n\nReturns a tuple of:\n\nctx_without_prefixes: the context stack with all PrefixContexts removed\nvn: the VarName that contains all the aggregated prefixes that were removed, or nothing if there were no PrefixContexts in the stack.\n\nExample\n\njulia> using DynamicPPL: extract_prefixes, PrefixContext, AbstractContext, @varname\n\njulia> struct MyContext <: AbstractContext end\n\njulia> ctx = PrefixContext(@varname(b), PrefixContext(@varname(a), MyContext()));\n\njulia> extract_prefixes(ctx)\n(MyContext(), b.a)\n\nwarning: Warning\nThis does not modify any variables in any CondFixContexts that may be present in the context stack. This is because this function is only used in functions such as  tilde_assume!!, which are lower in the tilde-pipeline than contextual_isassumption and contextual_isfixed (the functions which actually use the CondFixContext's values). Thus, by this time, any CondFixContexts present have already served their purpose.If you call this function, you must therefore be careful to ensure that you do not need to modify any inner CondFixContexts. If you do need to modify them, then you may need to use prefix_cond_and_fixed_variables instead.\n\n\n\n\n\n","title":"DynamicPPL.extract_prefixes"},{"category":"function","location":"api/#DynamicPPL.init!!","page":"API","text":"init!!(\n    [rng::Random.AbstractRNG,]\n    model::Model,\n    varinfo::AbstractVarInfo,\n    init_strategy::AbstractInitStrategy,\n    [transform_strategy::AbstractTransformStrategy=get_transform_strategy(varinfo),]\n)\n\nEvaluate the model and replace the values of the model's random variables in the given varinfo with new values, using a specified initialisation strategy. If the values in varinfo are not set, they will be added using a specified initialisation strategy.\n\ntransform_strategy tells the model evaluation whether variables should be interpreted as linked or unlinked. Right now, it is slightly complicated because the default behaviour depends on the varinfo provided. If varinfo isa VarInfo, then the transform strategy is inferred from the VarInfo, i.e., linked variables in the VarInfo are treated as linked during evaluation. Conversely, if varinfo isa OnlyAccsVarInfo, then you must specify the transform strategy explicitly, since an OnlyAccsVarInfo does not contain any information about which variables are transformed.\n\nReturns a tuple of the model's return value, plus the updated varinfo object.\n\n\n\n\n\n","title":"DynamicPPL.init!!"},{"category":"type","location":"api/#DynamicPPL.AbstractInitStrategy","page":"API","text":"AbstractInitStrategy\n\nAbstract type representing the possible ways of initialising new values for the random variables in a model (e.g., when creating a new VarInfo).\n\nAny subtype of AbstractInitStrategy must implement the DynamicPPL.init method, and in some cases, DynamicPPL.get_param_eltype (see its docstring for details).\n\n\n\n\n\n","title":"DynamicPPL.AbstractInitStrategy"},{"category":"function","location":"api/#DynamicPPL.init","page":"API","text":"init(rng::Random.AbstractRNG, vn::VarName, dist::Distribution, strategy::AbstractInitStrategy)\n\nGenerate a new value for a random variable with the given distribution.\n\nThis function must return a TransformedValue.\n\nIf strategy provides values that are already untransformed (e.g., a Float64 within (0, 1) for dist::Beta, then you should return a TransformedValue with a NoTransform().\n\nOtherwise, often there are cases where this will return a transformed value; for example, if the strategy is reading from an existing VarInfo.\n\n\n\n\n\n","title":"DynamicPPL.init"},{"category":"function","location":"api/#DynamicPPL.get_param_eltype","page":"API","text":"DynamicPPL.get_param_eltype(strategy::AbstractInitStrategy)\n\nReturn the element type of the parameters generated from the given initialisation strategy.\n\nThe default implementation returns Any. However, for InitFromParams which provides known parameters for evaluating the model, methods are sometimes implemented in order to return more specific types.\n\nIn general, if you are implementing a custom AbstractInitStrategy, correct behaviour can only be guaranteed if you implement this method as well. However, quite often, the default return value of Any will actually suffice. The cases where this does not suffice, and where you do have to manually implement get_param_eltype, are explained in the extended help (see ??DynamicPPL.get_param_eltype in the REPL).\n\nExtended help\n\nThere are a few edge cases in DynamicPPL where the element type is needed. These largely relate to determining the element type of accumulators ahead of time (before evaluation), as well as promoting type parameters in model arguments. The classic case is when evaluating a model with ForwardDiff: the log-probability accumulators must be promoted to contain Duals, and any Vector{Float64} arguments must be promoted to Vector{Dual}. Other tracer types, for example those in SparseConnectivityTracer.jl, also require similar treatment.\n\nIf the AbstractInitStrategy is never used in combination with tracer types, then it is perfectly safe to return Any. This does not lead to type instability downstream because the actual accumulators will still be created with concrete Float types (the Any is just used to determine whether the float type needs to be modified).\n\nIn case that wasn't enough: in fact, even the above is not always true. Firstly, the accumulator argument is only true when evaluating with ThreadSafeVarInfo. See the comments in DynamicPPL.unflatten!! for more details. For non-threadsafe evaluation, Julia is capable of automatically promoting the types on its own. Secondly, the promotion only matters if you are trying to directly assign into a Vector{Float64} with a ForwardDiff.Dual or similar tracer type, for example using xs[i] = MyDual. This doesn't actually apply to tilde-statements like xs[i] ~ ... because those use Accessors.set under the hood, which also does the promotion for you. For the gory details, see the following issues:\n\nhttps://github.com/TuringLang/DynamicPPL.jl/issues/906 for accumulator types\nhttps://github.com/TuringLang/DynamicPPL.jl/issues/823 for type argument promotion\n\n\n\n\n\nget_param_eltype(varinfo::AbstractVarInfo, context::AbstractContext)\n\nGet the element type of the parameters being used to evaluate a model, using a varinfo under the given context. For example, when evaluating a model with ForwardDiff AD, this should return ForwardDiff.Dual.\n\nBy default, this uses eltype(varinfo) which is slightly cursed. This relies on the fact that typically, before evaluation, the parameters will have been inserted into the VarInfo's metadata field.\n\nFor InitContext, it's quite different: because InitContext is responsible for supplying the parameters, we can avoid using eltype(varinfo) and instead query the parameters inside it. See the docstring of get_param_eltype(strategy::AbstractInitStrategy) for more explanation.\n\n\n\n\n\n","title":"DynamicPPL.get_param_eltype"},{"category":"type","location":"api/#DynamicPPL.TransformedValue","page":"API","text":"TransformedValue{V,T<:AbstractTransform}\n\nA struct to represent a value that has undergone some transformation.\n\nThe transformed value is stored in the value field, and the inverse transformation is stored in the transform field. \n\nThat means that get_transform(tv)(get_internal_value(tv)) should return the raw, untransformed, value associated with tv.\n\n\n\n\n\n","title":"DynamicPPL.TransformedValue"},{"category":"function","location":"api/#DynamicPPL.get_transform","page":"API","text":"DynamicPPL.get_transform(tv::TransformedValue)\n\nGet the subtype of AbstractTransform that is stored inside tv. Note that this is not always a function that can be used to obtain the raw, untransformed value. If you need the raw value, please use DynamicPPL.get_raw_value.\n\n\n\n\n\n","title":"DynamicPPL.get_transform"},{"category":"function","location":"api/#DynamicPPL.get_internal_value","page":"API","text":"DynamicPPL.get_internal_value(tv::TransformedValue)\n\nGet the internal value stored in tv.\n\n\n\n\n\n","title":"DynamicPPL.get_internal_value"},{"category":"function","location":"api/#DynamicPPL.get_raw_value","page":"API","text":"DynamicPPL.get_raw_value(tv::TransformedValue)\nDynamicPPL.get_raw_value(tv::TransformedValue, dist::Distribution)\n\nGet the raw (untransformed) value from a TransformedValue.\n\nThe two-argument version, with a dist::Distribution argument, is required when the TransformedValue holds a dynamic transform (i.e., tv.transform is either DynamicLink or Unlink.\n\nFor FixedTransform or NoTransform, the dist argument is not needed (and if supplied, will be ignored).\n\n\n\n\n\n","title":"DynamicPPL.get_raw_value"},{"category":"function","location":"api/#DynamicPPL.set_internal_value","page":"API","text":"DynamicPPL.set_internal_value(tv::TransformedValue, new_val)\n\nCreate a new TransformedValue with the same transformation as tv, but with internal value new_val.\n\n\n\n\n\n","title":"DynamicPPL.set_internal_value"},{"category":"type","location":"api/#DynamicPPL.ParamsWithStats","page":"API","text":"ParamsWithStats\n\nA struct which contains parameter values extracted from a VarInfo, along with any statistics associated with the VarInfo. The statistics are provided as a NamedTuple and are optional.\n\n\n\n\n\n","title":"DynamicPPL.ParamsWithStats"},{"category":"method","location":"api/#DynamicPPL.ParamsWithStats-Tuple{AbstractInitStrategy, Model}","page":"API","text":"ParamsWithStats(\n    init_strategy::AbstractInitStrategy,\n    model::Model,\n    stats::NamedTuple=NamedTuple();\n    include_colon_eq::Bool=true,\n    include_log_probs::Bool=true,\n)\n\nGenerate a ParamsWithStats by re-evaluating the given model with the provided init_strategy. Re-evaluation of the model is often necessary to obtain correct parameter values as well as log probabilities. This is especially true when using linked VarInfos, i.e., when variables have been transformed to unconstrained space, and if this is not done, subtle correctness bugs may arise: see, e.g., https://github.com/TuringLang/Turing.jl/issues/2195.\n\ninclude_colon_eq controls whether variables on the left-hand side of := are included in the resulting parameters.\n\ninclude_log_probs controls whether log probabilities (log prior, log likelihood, and log joint) are added to the resulting statistics NamedTuple.\n\n\n\n\n\n","title":"DynamicPPL.ParamsWithStats"},{"category":"method","location":"api/#DynamicPPL.ParamsWithStats-Tuple{AbstractVarInfo}","page":"API","text":"ParamsWithStats(\n    varinfo::AbstractVarInfo,\n    stats::NamedTuple=NamedTuple();\n    include_log_probs::Bool=true,\n)\n\nThere is one case where re-evaluation is not necessary, which is when the VarInfos all already contain DynamicPPL.RawValueAccumulator. This accumulator stores values as seen during the model evaluation, so the values can be simply read off. In this case, the model argument can be omitted, and no re-evaluation will be performed. However, it is the caller's responsibility to ensure that RawValueAccumulator is indeed present inside varinfo.\n\ninclude_log_probs controls whether log probabilities (log prior, log likelihood, and log joint) are added to the resulting statistics NamedTuple.\n\n\n\n\n\n","title":"DynamicPPL.ParamsWithStats"},{"category":"method","location":"api/#DynamicPPL.ParamsWithStats-Tuple{AbstractVector, LogDensityFunction}","page":"API","text":"ParamsWithStats(\n    param_vector::AbstractVector,\n    ldf::DynamicPPL.LogDensityFunction,\n    stats::NamedTuple=NamedTuple();\n    include_colon_eq::Bool=true,\n    include_log_probs::Bool=true,\n)\n\nGenerate a ParamsWithStats by re-evaluating the given ldf with the provided param_vector.\n\nThis method is intended to replace the old method of obtaining parameters and statistics via unflatten!! plus re-evaluation. It is faster for two reasons:\n\nIt does not rely on deepcopy-ing the VarInfo object (this used to be mandatory as otherwise re-evaluation would mutate the VarInfo, rendering it unusable for subsequent MCMC iterations).\nThe re-evaluation is faster as it uses OnlyAccsVarInfo.\n\nFurthermore, if the LogDensityFunction has all fixed transforms (i.e., was constructed with fix_transforms=true), and neither include_log_probs nor include_colon_eq is set, then model re-evaluation is skipped entirely and the raw parameter values are extracted directly from the parameter vector using the cached transforms.\n\n\n\n\n\n","title":"DynamicPPL.ParamsWithStats"},{"category":"method","location":"api/#AbstractMCMC.from_samples-Tuple{Type{Chains}, AbstractMatrix{<:ParamsWithStats}}","page":"API","text":"AbstractMCMC.from_samples(\n    ::Type{MCMCChains.Chains},\n    params_and_stats::AbstractMatrix{<:DynamicPPL.ParamsWithStats}\n)\n\nConvert an array of DynamicPPL.ParamsWithStats to an MCMCChains.Chains object.\n\n\n\n\n\n","title":"AbstractMCMC.from_samples"},{"category":"method","location":"api/#AbstractMCMC.to_samples-Tuple{Type{ParamsWithStats}, Chains, Model}","page":"API","text":"AbstractMCMC.to_samples(\n    ::Type{DynamicPPL.ParamsWithStats},\n    chain::MCMCChains.Chains,\n    model::DynamicPPL.Model\n)\n\nConvert an MCMCChains.Chains object to an array of DynamicPPL.ParamsWithStats.\n\nFor this to work, chain must contain the varname_to_symbol mapping in its info field.\n\n\n\n\n\n","title":"AbstractMCMC.to_samples"},{"category":"section","location":"vnt/manipulation/#Manipulation-of-VNTs","page":"Manipulation of VNTs","text":"Once you have created a VNT (or obtained it from somewhere in DynamicPPL), there are a number of functions which can be used to modify its contents. These are faithfully documented in the API docs. Many of them are intended as iteration tools, such as mapreduce, and don't really require thorough explanation.\n\nHowever, here we will briefly discuss two of the more 'special' functions, which modify the representation of a VNT and are used by DynamicPPL in fairly subtle ways. In particular, they are used extensively in the construction of chain objects from MCMC sampling.","title":"Manipulation of VNTs"},{"category":"section","location":"vnt/manipulation/#Densification","page":"Manipulation of VNTs","text":"The function DynamicPPL.densify!! converts VNTs to 'dense' representations: essentially, this means converting any PartialArrays whose masks are completely true to regular arrays.\n\nFor example, this VNT contains a PartialArray which is really the same thing as a regular array:\n\nusing DynamicPPL\n\nvnt = @vnt begin\n    @template x = zeros(2)\n    x[1] := 1.0\n    x[2] := 2.0\nend\n\nCalling densify!! on it leads to:\n\ndense_vnt = densify!!(vnt)\n\nNote that vnt and dense_vnt contain the same values, and you can index into them in the same way:\n\nvnt[@varname(x)], dense_vnt[@varname(x)]\n\nHowever, they differ in the keys that they return.\n\nkeys(vnt), keys(dense_vnt)\n\nIn other words, vnt retains the notion that x[1] and x[2] are separate, whereas dense_vnt treats x as a single entity. Often the latter is more convenient: consider e.g. sampling from the following model\n\nusing Distributions\n\n@model function my_model()\n    x = zeros(2)\n    return x .~ Normal()\nend\n\n# We aren't running this, but in principle, this is what you would do.\n# using Turing, FlexiChains\n# chain = sample(my_model(), NUTS(), 100; chain_type=VNChain)\n\nIn the resulting chain, if x[1] and x[2] were stored separately as different matrices, this would be quite annoying for post-processing. To get a sample of the vector x, you would have to manually perform some kind of concatenation, e.g.\n\n# Again this isn't run.\n# cat(chain[@varname(x[1])], chain[@varname(x[2])]; dims=:xindex)\n\nNow, notice that when we evaluate the model, we actually get non-dense VNTs:\n\nrand(my_model())\n\nHowever, if we were to actually use Turing and FlexiChains to sample from this model, we will find that FlexiChains stores the samples in a dense format. This is because inside the constructor of DynamicPPL.ParamsWithStats, DynamicPPL calls densify!! on the parameter VNTs before handing them off to FlexiChains.\n\nnote: Note\nIn principle, MCMCChains is also given densified VNTs; unfortunately, the first thing that MCMCChains does is to just split them back up into separate scalar-valued parameters, so you don't actually get to see the benefits of densification when using MCMCChains.\n\nNote that densification, and the resulting improvements in chain structure, is something that can only really be done with VNTs as a base data structure. If we had plain old OrderedDicts mapping x[1] => 1.0 and x[2] => 2.0, there would be no way to know that x only contained these two entries and nothing else. This goes back to our arguments about 'constructiveness' in the VNT motivation page.","title":"Densification"},{"category":"section","location":"vnt/manipulation/#Skeleton-VNTs","page":"Manipulation of VNTs","text":"Another function, DynamicPPL.skeleton, is used to generate 'skeletons' of VNTs. Specifically, a skeleton of a VNT is one that contains enough template information to reconstruct the VNT from its key-value pairs.\n\nThis is best illustrated by example:\n\nvnt = @vnt begin\n    # We set x to be length-3 to avoid it ever being\n    # densified, which would render this example moot.\n    @template x = zeros(3)\n    x[1] := 1.0\n    x[2] := 2.0\n    # Stick in another scalar for illustration.\n    y := 3.0\nend\n\nSuppose we convert this into an OrderedDict. This is a lossy operation, since we lose the information that x is a length-3 vector.\n\nod = OrderedDict(pairs(vnt)...)\n\nWe can't go back from this OrderedDict to the original VNT: if we attempt to naively do so, although y will be faithfully reconstructed, we will get a GrowableArray for x.\n\nnew_vnt = VarNamedTuple(od)\n\nClearly, what we are lacking is the template information for x. This is where the skeleton comes in:\n\nskel = skeleton(vnt)\n\nNotice that in the skeleton, the key y has been entirely dropped, since we don't need any template information for it. Furthermore, the PartialArray x has been replaced with a normal array with the same size and array type (i.e. Base.Array), but with all values set to nothing.\n\nHere, nothing is chosen because storing it in an array is extremely space-efficient:\n\nBase.sizeof(fill(nothing, 100))\n\nFurthermore, the element type of the array is not needed when reconstructing data. For example, this is how we would reconstruct the original VNT:\n\nusing AbstractPPL: getsym\n\n# In a real setting you wouldn't need `begin .. end` and `local`.\nbegin\n    local new_vnt = VarNamedTuple()\n    for (vn, val) in pairs(od)\n        top_sym = getsym(vn)\n        template = get(skel.data, top_sym, DynamicPPL.NoTemplate())\n        new_vnt = DynamicPPL.templated_setindex!!(new_vnt, val, vn, template)\n    end\n    new_vnt\nend\n\nIn the call to templated_setindex!! for x, the template is fill(nothing, 3). However, the element type of this template array is not important: when DynamicPPL instantiates the new PartialArray, it will use typeof(val) rather than eltype(template) to determine the element type it should use. This allows us to freely choose any element type we like in the skeleton.\n\nSkeleton VNTs are not currently used in DynamicPPL. However, it is likely that they will find some kind of use in the future as a minimalistic representation of the structure of a VNT, without any of the actual values. For example, consider model conditioning:\n\n@model function f()\n    x = zeros(2)\n    return x .~ Normal()\nend\n\ncond_model = f() | Dict(@varname(x[1]) => 1.0, @varname(x[2]) => 2.0)\n\nRight now, conditioning with a Dict will lead to the conditioning values being stored with GrowableArrays, which is not ideal. However, if the model carried with itself a skeleton VNT, then at the point where we condition the model, we could use that to reconstruct a VNT of conditioned values. This is not yet implemented, but is one of the use cases that we have in mind.\n\nFlexiChains does currently make use of skeleton VNTs. If we reevaluate a model with a VNChain, we need some way of reconstructing VNTs to feed into the model. However, FlexiChains stores parameters in (more or less) an OrderedDict{VarName,Matrix}, from which one cannot reconstruct a VNT (you can only reconstruct an OrderedDict{VarName}). To get around this, FlexiChains additionally stores the skeleton VNT which bridges this gap and allows e.g. FlexiChains.parameters_at to return a VNT. Please see the FlexiChains documentation for more details on this.\n\n","title":"Skeleton VNTs"},{"category":"section","location":"transforms/#transform-strategies","page":"Transform strategies","text":"Often it is useful to evaluate the log-probability of a model in a different space to the original one that it is defined in.\n\nnote: Note\nThe main Turing documentation site has a more detailed introduction to variable transformations in MCMC sampling. This page only describes the implementation in DynamicPPL.\n\nConsider the following model:\n\nusing DynamicPPL, Distributions\n\n@model function f()\n    x ~ LogNormal()\n    y ~ LogNormal()\n    return (x, y)\nend\n\nThere are several ways in which we might want to evaluate this model:\n\nIn the original ('untransformed') space: we provide values for x and y which are both positive, and we evaluate the log-probability directly. This corresponds directly to\nx, y = 1.5, 2.0\nlogp = logpdf(LogNormal(), x) + logpdf(LogNormal(), y)\nIn unconstrained ('transformed') space: we provide values for x and y in unconstrained space (i.e. they are real numbers), and evaluate the log-probability in transformed space. This corresponds to something like:\nx, y = 1.5, 2.0\n\n# To calculate the correct log-probability, we need to transform back to the\n# original space, but also account for the log-absolute-determinant of the\n# transformation Jacobian.\ntrf_x, trf_y = log(x), log(y)\nlogdetJx = -trf_x\nlogdetJy = -trf_y\n\nlogp = logpdf(LogNormal(), x) + logpdf(LogNormal(), y) - logdetJx - logdetJy\nWe might also want to have a mix of transformed and untransformed variables, for example, x in untransformed space and y in transformed space. This could be useful for example when using Gibbs sampling, or Metropolis–Hastings with different proposal distributions for different variables.","title":"Transform strategies"},{"category":"section","location":"transforms/#AbstractTransformStrategy","page":"Transform strategies","text":"DynamicPPL allows you to specify which variables you want to evaluate in transformed space using transform strategies. All transform strategies are subtypes of AbstractTransformStrategy. Currently, DynamicPPL provides the transform strategies LinkAll, UnlinkAll, LinkSome, and UnlinkSome. Their meanings should be fairly self-explanatory; here is a brief demonstration:\n\nparams = @vnt begin\n    x := 1.5\n    y := 2.0\nend\n_, vi_unlinked = init!!(f(), OnlyAccsVarInfo(), InitFromParams(params), UnlinkAll())\nvi_unlinked.accs\n\n_, vi_linked = init!!(f(), OnlyAccsVarInfo(), InitFromParams(params), LinkAll())\nvi_linked.accs\n\nnote: Initialisation strategy does not determine log-Jacobian\nIn the above examples, we used InitFromParams to provide variable values. InitFromParams is an initialisation strategy and when given a VarNamedTuple of values as we did above, it always interprets those values as being in untransformed space.This does not however mean that the log-Jacobian is disregarded! As we see in the second example above, when using LinkAll(), the log-Jacobian is still applied even though the values were provided in untransformed space. The transform strategy is what determines whether the log-Jacobian is applied or not when evaluating the log-probability. One could think of the transform strategy as being a re-interpretation of the value provided by the initialisation strategy.This frees up the initialisation strategy to return whatever kind of TransformedValue is most convenient for it.","title":"AbstractTransformStrategy"},{"category":"section","location":"transforms/#custom-transform-strategy","page":"Transform strategies","text":"The only requirement for a subtype of an AbstractTransformStrategy is that it must implement target_transform(::AbstractTransformStrategy, vn::VarName), where vn is the variable on the left-hand side of a tilde-statement.\n\ntarget_transform must in turn return an AbstractTransform specifying whether the variable should be transformed or not.\n\nFor example, the following would cause x to be transformed but not y.\n\nstruct LookupTransformsInVNT{V<:VarNamedTuple} <: AbstractTransformStrategy\n    transforms::V\nend\n\nfunction DynamicPPL.target_transform(l::LookupTransformsInVNT, vn::VarName)\n    return l.transforms[vn]\nend\n\nlink_x_only = LookupTransformsInVNT(@vnt begin\n    x := DynamicLink()\n    y := Unlink()\nend)\n\n_, vi_link_x_only = init!!(f(), OnlyAccsVarInfo(), InitFromParams(params), link_x_only)\nvi_link_x_only.accs","title":"Making your own transform strategy"},{"category":"section","location":"transforms/#DynamicLink-in-more-detail","page":"Transform strategies","text":"Looking at the two transform types used above, Unlink() is probably more intuitive: it just means 'do not interpret this variable as being in transformed space'. However, DynamicLink() is a bit more subtle.\n\nIn particular, for DynamicLink(), the actual transformation used is obtained at runtime from the distribution on the right-hand side of the tilde-statement, using:\n\nusing Bijectors: Bijectors\n\nBijectors.VectorBijectors.from_linked_vec(LogNormal())\n\nThis means that the transformation is recalculated on every model evaluation.\n\nOne might question why this is necessary: for example, in this simple model, we know that x and y are always LogNormal, so why not just store use the log-transform directly?\n\nThe answer is to do with distributions whose support (and hence transformation) can vary. For example, consider:\n\n@model function g()\n    x ~ Normal()\n    return y ~ truncated(Normal(); lower=x)\nend\n\nIn this example, the support of y depends on what value x takes, and that can vary from one evaluation to the next. Consequently, the transformation used for y must be determined at runtime; if we cache a fixed transformation, it is possible that this transformation will be invalid (e.g. by mapping unconstrained values to values outside the support of y).\n\nFor correctness, DynamicPPL therefore always prefers to determine the transformation at runtime when using DynamicLink(). This behaviour is encoded in (for example) Turing's HMC samplers, which use LinkAll() as the default transform strategy, and hence every VarName will have a target_transform of DynamicLink().","title":"DynamicLink in more detail"},{"category":"section","location":"transforms/#Why-not-let-init()-determine-the-transform?","page":"Transform strategies","text":"warning: Warning\nThis section is mainly for developers and advanced users interested in the design decisions behind DynamicPPL; it has no real implications for everyday usage.\n\nAn alternative to having an explicit link strategy would be to simply allow the initialisation strategy to determine whether variables are transformed or not. In this world, if init(rng, vn, dist, strategy) returned a transformed value, then we could treat it as being in transformed space, and vice versa.\n\nThe reason why we do this is to allow more flexibility in how models are evaluated, which can in turn save us from having to rerun the model multiple times.\n\nConsider, for example, DynamicPPL.InitFromUniform. This is an initialisation strategy which samples uniformly from [-2, 2] in transformed space, and is used by Turing's HMC samplers to generate initial values. This used to be a standard workflow in Turing:\n\nusing Random\n_, vi = init!!(Xoshiro(468), f(), VarInfo(), InitFromUniform())\n\n# We'll run this later.\n# vi_linked = link!!(vi, f())\n\nIn the first line, we are populating an empty VarInfo with values. This initialisation always causes the VarInfo to be unlinked, because the behaviour of init!! on a VarInfo is to always retain the transform state of the VarInfo (which is empty at this point, and hence unlinked).\n\nIf we did not have a separate transform strategy, then we would have to make sure that init() always returned untransformed values when using InitFromUniform (otherwise we would be filling the VarInfo with transformed values, which is not what we want here). So we have this extra step where InitFromUniform has to generate transformed values, untransform them, and then pass them on to the VarInfo so that it can store untransformed values. (This was indeed the way InitFromUniform was implemented in DynamicPPL <= v0.39).\n\nIf we then wanted to link the VarInfo again\n\nvi_linked = link!!(vi, f())\n\nthen we would have to recompute the forward transform, which frustratingly enough is exactly the same as what InitFromUniform had to undo. So we are calculating the same transformation twice, and evaluating the model twice, to make sure that our VarInfo ends up in the right state.\n\nInstead, now with a separate transform strategy, we can immediately do:\n\n_, vi_linked = init!!(Xoshiro(468), f(), VarInfo(), InitFromUniform(), LinkAll())\nvi_linked\n\nWe can see that we have gotten exactly the same result, but with only running the model once, and only calculating the transformation once. Furthermore, this allows us to remove the inverse transform step inside InitFromUniform: it can simply return a linked value directly, and the transform strategy is then responsible for performing the inverse transform a single time.\n\nEssentially, having a separate transform strategy allows us to:\n\nFree up the initialisation strategy to return whatever kind of TransformedValue is most convenient for it, without worrying about whether it needs to perform some transform.\nConsolidate all the actual transformation in a single function (DynamicPPL.apply_transform_strategy), which allows us to ensure that each tilde-statement involves at most one transformation.\n\n","title":"Why not let init() determine the transform?"},{"category":"section","location":"fixed_transforms/#fixed-transforms","page":"Fixed transforms","text":"For some models, it may be known that the support of a variable does not change, and that the transformations should be fixed. This allows us to avoid the overhead of recomputing the transformation at every model evaluation.\n\nYou can write your own transform strategy to do this (as described on the main transforms page), but DynamicPPL also provides a built-in strategy for this, namely WithTransforms. This takes a VarNamedTuple of transforms plus a fallback strategy for when the target variable is not in the VarNamedTuple.\n\nWe'll first show the full usage of WithTransforms here. It should be noted that there are more convenient ways to enable this at a high level, which will be discussed later.\n\nusing DynamicPPL: WithTransforms, FixedTransform, Unlink, @vnt, LinkAll\n\n# Make your own custom transform. Note that by convention, the stored function\n# always converts *from* the transformed value *to* the raw value.\nfunction my_transform end\n\n# Store them in a VarNamedTuple. You can mix and match different transform\n# types here, as long as they are subtypes of `AbstractTransform`.\nvnt = @vnt begin\n    a := FixedTransform(my_transform)\n    b := Unlink()\nend\n\n# Use the WithTransforms strategy to specify that these transforms should be\n# used. All other variables will use the fallback strategy, here `LinkAll()`\n# (note that the fallback must be a transform *strategy*, not a *transform*).\ntfm_strategy = WithTransforms(vnt, LinkAll())","title":"Fixing transformations"},{"category":"section","location":"fixed_transforms/#Requirements-for-fixed-transforms","page":"Fixed transforms","text":"tfm_strategy will eventually be the transform strategy that is passed to various DynamicPPL functions, including init!! and LogDensityFunction. However, before we can do that, we need to make sure that the transform we are using is properly defined. (Notice we avoided doing so above!) Specifically, the minimum interface required is:\n\nInverseFunctions.inverse must be implemented for the transform; this allows DynamicPPL to generate transformed values.\nChangesOfVariables.with_logabsdet_jacobian must be implemented for both the transform and its inverse; this allows DynamicPPL to apply the log-Jacobian correction.\n\n# Bijectors re-exports both functions, so we can import from there.\nimport Bijectors\n\nmy_inverse_transform(x::Real) = [log(x)]\nBijectors.inverse(::typeof(my_transform)) = my_inverse_transform\nBijectors.inverse(::typeof(my_inverse_transform)) = my_transform\n\nfunction Bijectors.with_logabsdet_jacobian(::typeof(my_transform), tfmx::AbstractVector)\n    y = only(tfmx)\n    return (exp(y), y)\nend\nfunction Bijectors.with_logabsdet_jacobian(::typeof(my_inverse_transform), x::Real)\n    logx = log(x)\n    return ([logx], -logx)\nend\n\n(Often, if the transform must store some kind of parameter (e.g. size), it is easier to make it a callable struct.)\n\nOnce that's done, you can use this transform strategy:\n\nusing DynamicPPL, Distributions, LogDensityProblems\n\n@model function f()\n    a ~ Exponential()\n    b ~ Normal()\n    return nothing\nend\nmodel = f()\n\nldf = LogDensityFunction(model, getlogjoint_internal, tfm_strategy)\n\n# Notice that the transformed value of `a` is negative here.\nparams = [-5.0, 2.0]\nLogDensityProblems.logdensity(ldf, params)\n\nIn this specific instance, the transform we have chosen correlates exactly to the transform that DynamicLink would have chosen for a, so we can verify that the log-density is the same as when using LinkAll():\n\nldf_link_all = LogDensityFunction(model, getlogjoint_internal, LinkAll())\nLogDensityProblems.logdensity(ldf_link_all, params)","title":"Requirements for fixed transforms"},{"category":"section","location":"fixed_transforms/#Deriving-a-set-of-fixed-transforms","page":"Fixed transforms","text":"In the above example, we manually defined a transform and a VarNamedTuple thereof. Bijectors.jl already provides a large number of transforms (which are exactly the transforms that are derived at runtime), and you may want to use those directly. To do so, you will have to run the model once with the desired transform strategy (e.g. LinkAll()), and collect the transforms that were used for each variable.\n\nThis is most easily done via\n\nget_fixed_transforms(model, LinkAll())","title":"Deriving a set of fixed transforms"},{"category":"section","location":"fixed_transforms/#Faster-constructors","page":"Fixed transforms","text":"While the sections above clearly demonstrate how to construct a set of fixed transforms, it is still slightly verbose. For this reason, LogDensityFunction in particular provides a keyword argument fix_transforms that allows you to specify a transform strategy for which you want to fix the transforms.\n\nFor example,\n\nldf1 = LogDensityFunction(model, getlogjoint_internal, LinkAll(); fix_transforms=true)\n\nis equivalent to writing\n\ntfm_strategy = WithTransforms(get_fixed_transforms(model, LinkAll()), LinkAll())\nldf2 = LogDensityFunction(model, getlogjoint_internal, tfm_strategy)","title":"Faster constructors"},{"category":"section","location":"fixed_transforms/#Correctness","page":"Fixed transforms","text":"If you are thinking of using fixed transforms to speed up model evaluation, you should be aware that this can lead to incorrect results if the support of a variable changes during sampling.\n\nFor example, consider the following model:\n\n@model function changing_support()\n    x ~ Normal()\n    if x > 0\n        y ~ Exponential()\n    else\n        y ~ Normal()\n    end\n    return nothing\nend\nnothing # hide\n\nThe transform needed for y here depends on what value x takes: in one case it is the identity transform, and in another case it is the log transform. If you were to use fixed transforms here, you would have to choose only one of these transforms for y, and this would lead to incorrect results when the other transform is needed.\n\nThis can also manifest in more subtle ways, especially with truncated or uniform distributions:\n\n@model function changing_support_2()\n    x ~ Normal()\n    y ~ truncated(Normal(); lower=x)\n    return nothing\nend\nnothing # hide\n\n@model function changing_support_3()\n    x ~ Normal()\n    absx = abs(x)\n    y ~ Uniform(-absx, absx)\n    return nothing\nend\nnothing # hide","title":"Correctness"},{"category":"section","location":"fixed_transforms/#Performance","page":"Fixed transforms","text":"It should be noted that using fixed transforms does not always lead to speedups, since the calculation of the transform is often very cheap and comparable to the cost of looking up the cached transform. If you want to use this feature, we recommend benchmarking your model. For example:\n\nusing DynamicPPL, Distributions, LogDensityProblems, Chairmarks, LinearAlgebra, ADTypes\nimport ForwardDiff\n\nadtype = AutoForwardDiff()\n\n@model function eightsch(J, y, sigma)\n    mu ~ Normal(0, 5)\n    tau ~ truncated(Cauchy(0, 5); lower=0)\n    theta ~ MvNormal(fill(mu, J), tau^2 * I)\n    for i in 1:J\n        y[i] ~ Normal(theta[i], sigma[i])\n    end\nend\nJ = 8\ny = [28, 8, -3, 7, -1, 1, 18, 12]\nsigma = [15, 10, 16, 11, 9, 11, 10, 18]\nmodel = eightsch(J, y, sigma)\n\nldf_dynamic = LogDensityFunction(model, getlogjoint_internal, LinkAll(); adtype=adtype)\nx = rand(ldf_dynamic)\n\n@b LogDensityProblems.logdensity($ldf_dynamic, $x)\n\n@b LogDensityProblems.logdensity_and_gradient($ldf_dynamic, $x)\n\nIn the following code blocks, you should see that the fixed transform takes almost exactly the same time as the dynamic transform (although the exact number will of course have some variance). This is because the distributions in the eight-schools model above are 'simple' enough that re-deriving them is essentially instantaneous.\n\nldf_fixed = LogDensityFunction(\n    model, getlogjoint_internal, LinkAll(); fix_transforms=true, adtype=adtype\n)\n@b LogDensityProblems.logdensity($ldf_fixed, $x)\n\n@b LogDensityProblems.logdensity_and_gradient($ldf_fixed, $x)\n\nFor some distributions, however, the fixed transform can sometimes be much faster. For example:\n\ndists = (\n    Normal(),\n    Beta(2, 2),\n    MvNormal(zeros(3), I),\n    Dirichlet(ones(3)),\n    LKJCholesky(3, 0.5),\n    product_distribution([Normal(), Beta(2, 2)]),\n    product_distribution((a=Normal(), b=Beta(2, 2))),\n)\n\nfunction benchmark_transforms(i, dist)\n    @model m() = x ~ dist\n    model = m()\n    ldf_dynamic = LogDensityFunction(model, getlogjoint_internal, LinkAll())\n    p = rand(ldf_dynamic)\n    ldf_fixed = LogDensityFunction(\n        model, getlogjoint_internal, LinkAll(); fix_transforms=true\n    )\n\n    fixed = repr(MIME(\"text/plain\"), (@b LogDensityProblems.logdensity($ldf_fixed, $p)))\n    dynamic = repr(MIME(\"text/plain\"), (@b LogDensityProblems.logdensity($ldf_dynamic, $p)))\n    return println(\"$i      $(rpad(fixed, 35)) $(rpad(dynamic, 35))\")\nend\n\nprintln(\"dist   $(rpad(\"fixed\", 35)) $(rpad(\"dynamic\", 35))\")\nfor (i, dist) in enumerate(dists)\n    benchmark_transforms(i, dist)\nend\n\n","title":"Performance"},{"category":"section","location":"tilde/#Tilde-statements-in-depth","page":"Tilde-statements","text":"The Model evaluation page gives a high-level overview of how to control model evaluation in DynamicPPL. This page goes into more detail about exactly how initialisation strategies, transform strategies, and accumulators are implemented, and how they combine to give the overall semantics described on that page.\n\nwarning: Warning\nThis page is a fairly advanced discussion of DynamicPPL's internal implementation details. Most users will not need to understand these to use DynamicPPL effectively; if the Model evaluation page was sufficiently clear, you can safely skip this page. However, if you are looking to contribute to DynamicPPL, this is a very important page to read!","title":"Tilde-statements in-depth"},{"category":"section","location":"tilde/#The-@model-macro","page":"Tilde-statements","text":"Each tilde-statement, say, x ~ Normal(), is transformed by the @model macro into something like the following pseudocode (the functions like is_fixed do not have those exact names in DynamicPPL but are conceptually equivalent). If you're interested in the gory details you can run @macroexpand @model f() = x ~ Normal() in the REPL.\n\nbegin\n    vn = @varname(x)\n    dist = Normal()\n    # To understand the need for `template`, see the VarNamedTuple docs.\n    template = x\n\n    if is_fixed(vn)\n        raw_x = get_fixed_value(vn)\n\n    elseif is_conditioned(vn)\n        conditioned_x = get_conditioned_value(vn)\n        raw_x, __varinfo__ = tilde_observe!!(\n            ctx, dist, conditioned_x, vn, template, __varinfo__\n        )\n\n    elseif is_model_argument(vn)\n        arg_x = x\n        raw_x, __varinfo__ = tilde_observe!!(ctx, dist, arg_x, vn, template, __varinfo__)\n\n    else\n        raw_x, __varinfo__ = tilde_assume!!(ctx, dist, vn, template, __varinfo__)\n    end\n\n    x = raw_x\nend\n\nWe won't go into detail about every part of this code; by far the most interesting part is the call to tilde_assume!!. Every tilde-statement vn ~ dist (where vn represents a random variable) is transformed into one such call.\n\nAs described on the Model evaluation page, there are three stages to every tilde-statement:\n\nInitialisation: get an TransformedValue from the initialisation strategy.\nTransformation: figure out the untransformed (raw) value and the transformed value (where necessary); compute the relevant log-Jacobian.\nAccumulation: pass all the relevant information to the accumulators, which individually decide what to do with it.\n\nThe method for tilde_assume!! (with InitContext) more or less implements this logic directly with three lines of code. At the time of writing, this is implemented in src/contexts/init.jl, and looks like:\n\nfunction DynamicPPL.tilde_assume!!(ctx::InitContext, dist, vn, template, vi)\n    # 1. Initialisation\n    init_tval = DynamicPPL.init(ctx.rng, vn, dist, ctx.strategy)\n\n    # 2. Transformation\n    x, tval, logjac = apply_transform_strategy(ctx.transform_strategy, init_tval, vn, dist)\n\n    # 3. Accumulation\n    vi = DynamicPPL.setindex_with_dist!!(vi, tval, dist, vn, template)\n    vi = DynamicPPL.accumulate_assume!!(vi, x, tval, logjac, vn, dist, template)\n    return x, vi\nend\n\nFor tilde_observe!!, the code is very similar, but even easier: the value can be read directly from the data provided to the model, so there is no need for an initialisation step. Since the value is already untransformed, we can skip the second step. Finally, accumulators must behave differently: e.g. incrementing the likelihood instead of the prior. That is accomplished by calling accumulate_observe!! instead of accumulate_assume!!.\n\nIn the following sections, we stick to the three sections of tilde_assume!!.\n\nnote: InitContext\nYou may have noticed that we specified that the method above is for InitContext. That is because different contexts are allowed to overload tilde_assume!! and tilde_observe!! and thereby endow them with new semantics.The reason why we recommend using InitContext (which init!! calls under the hood) is because it provides this framework for model evaluation that is both extensible and powerful. You can short-circuit all of this and define your own custom context that has completely different behaviour, but that means that it is less compatible with the rest of DynamicPPL.","title":"The @model macro"},{"category":"section","location":"tilde/#Initialisation","page":"Tilde-statements","text":"init_tval = DynamicPPL.init(ctx.rng, vn, dist, ctx.strategy)\n\nThe initialisation step is handled by the init function, which dispatches on the initialisation strategy. For example, if ctx.strategy is InitFromPrior(), then init() samples a value from the distribution dist.\n\nnote: DefaultContext\nFor DefaultContext, initialisation is handled by looking for the value stored inside vi.\n\nAs discussed in the Initialisation strategies page, this step, in general, does not return just the raw value (like rand(dist)). It returns an DynamicPPL.TransformedValue, which represents a value that may have been transformed. In the case of InitFromPrior(), the value is of course not transformed; we return a DynamicPPL.TransformedValue(..., NoTransform()) wrapping the sampled value.\n\nHowever, consider the case where we are using parameters stored inside a VarInfo: the value may have been stored either as a vectorised form, or as a linked vectorised form. In this case, init() will return a TransformedValue wrapping the vectorised value, whose transform is either Unlink() (i.e., vectorised but not linked), or DynamicLink() (i.e., vectorised and linked).\n\nThe reason why we return this wrapped value is because we want to avoid having to perform transformations multiple times. Each step is responsible for only performing the transformations it needs to. At this stage, there has not yet been any need for the raw value, so we do not perform any transformations yet. Thus, the TransformedValue is passed straight through and is used by the transformation step.\n\nnote: The return type of init() doesn't matter\nThe exact TransformedValue returned by init() has no impact on whether the value is considered to be transformed or not. That is determined solely by the transform strategy (see below). This separation allows us to perform the minimum amount of transformations necessary inside init(). If we were to eagerly transform the value inside init(), we could easily end up performing the same transformation multiple times across the different steps.","title":"Initialisation"},{"category":"section","location":"tilde/#Transformation","page":"Tilde-statements","text":"x, tval, logjac = apply_transform_strategy(ctx.transform_strategy, init_tval, vn, dist)\n\nThere are three return values in this step, and they correspond to the three things that this step needs to do. They are all interconnected, which is why they are computed together inside apply_transform_strategy(): by doing so we can ensure that with_logabsdet_jacobian is only called a maximum of once per tilde-statement.\n\nGet the raw (untransformed) value x\nAt some point, we do need to perform the transformation to get the actual raw value. This is because DynamicPPL promises in the model that the variables on the left-hand side of the tilde are actual raw values.\n@model function f()\n    x ~ dist\n    # Here, `x` _must_ be the actual raw value.\n    @show x\nend\nThus, regardless of what we are accumulating, we will have to unwrap the transformed value provided by init().\nGet the (possibly transformed) value tval\nIn addition to the raw value, if the transform strategy indicates that we should treat vn as being in transformed space, we also need to compute the transformed value. This is because some accumulators may need to work with the transformed value instead of the raw value.\n(If there is a full VarInfo being used, the transformed value will also have to be set inside the VarInfo.)\nCompute the log-Jacobian logjac\nlogjac is accumulated according to the transform specified by the transform strategy. The convention in DynamicPPL is that the log-Jacobian is always computed with respect to the forward transformation.\n\nIt is worth emphasising that whether a value is transformed or not is determined by the transform strategy provided to the model (i.e., ctx.transform_strategy), not the initialisation strategy (ctx.strategy). The reason for this is to allow a separation between the source of the values (initialisation) and how those values are to be interpreted (transform strategy).\n\nThis allows us to, for example, generate values from the (unlinked) prior but also calculate their log-density in transformed space and accumulate transformed values by combining InitFromPrior() with LinkAll(). It also allows us to read values from an existing VarInfo but interpret them as being in a different space by combining InitFromParams() with a different transform strategy: this corresponds exactly to the act of 'linking' a VarInfo.\n\nnote: DefaultContext\nFor DefaultContext, whether or not the variable is transformed will depend on the VarInfo used for evaluation. If the variable is stored as transformed in the VarInfo, then it will be treated as transformed here. Notice that both the initialisation strategy as well as the transform strategy are effectively determined by the VarInfo in this case. The separation described above is not possible when using DefaultContext.The move away from DefaultContext and towards InitContext is motivated by the desire to separate these two concerns, and to enable a more modular and declarative way of specifying how a model is to be evaluated.\n\nnote: Log-Jacobian computation\nIn principle, if the log-Jacobian is not of interest to any of the accumulators, we could skip computing it here. However, that is not easy to determine in practice. We also cannot defer the log-Jacobian computation to the accumulator, since it is often more efficient to compute it at the same time as the transformation (i.e., using with_logabsdet_jacobian). The current situation of computing it once here is the most sensible compromise (for now).One could envision a future where accumulators declare upfront (via their type) whether they need the log-Jacobian or not. We could then skip computing it if no accumulator needs it.","title":"Transformation"},{"category":"section","location":"tilde/#Accumulation","page":"Tilde-statements","text":"vi = DynamicPPL.setindex_with_dist!!(vi, tval, dist, vn, template)\nvi = DynamicPPL.accumulate_assume!!(vi, x, tval, logjac, vn, dist, template)\n\nnote: Note\nThe first line, setindex_with_dist!!, is only necessary when using a full VarInfo. It essentially stores the value tval inside the VarInfo, but makes sure to store a vectorised form (i.e., if tval is not vectorised, it will be). This is entirely equivalent to using a VectorValueAccumulator to store the values; it's just that when using a full VarInfo that accumulator is 'built-in' as vi.values.Since conceptually this is the same as an accumulator, we will not discuss it further here.\n\nHere, we pass all of the information we have gathered so far for this tilde-statement to the accumulators. accumulate_assume!!(vi::AbstractVarInfo, ...) will loop over all accumulators stored inside vi, and call each of their individual accumulate_assume!! methods. This method is responsible for deciding how to combine the information provided.\n\nAccumulators are described in much more detail on the Accumulators page; please read that for more information!\n\n","title":"Accumulation"},{"category":"section","location":"vnt/design/#Design","page":"Design","text":"There are two aspects to the design of VarNamedTuples: property access, and indexing. VarNamedTuple consists of recursively nested NamedTuples and PartialArrays which together support both kinds of access.","title":"Design"},{"category":"section","location":"vnt/design/#Property-access","page":"Design","text":"Let's first talk about the NamedTuple part. In a VarNamedTuple each level of a Property optic corresponds to a level of nested NamedTuples, with the Symbols of the lenses as keys. For instance, the VarNamedTuple mapping @varname(x) => 1, @varname(y.z) => 2 would be stored as\n\nVarNamedTuple(; x=1, y=VarNamedTuple(; z=2))\n\nwhere VarNamedTuple(; x=a, y=b) is just a thin wrapper around the NamedTuple (; x=a, y=b). In fact, if vnt is a VarNamedTuple, then vnt.data is exactly that underlying NamedTuple.\n\nIt's often handy to think of this as a tree, with each node being a VarNamedTuple, like so:\n\n   VNT\nx /   \\ y\n 1     VNT\n         \\ z\n          2\n\nBy virtue of these being NamedTuple, all variable access is completely type-stable.\n\nIf all VarNames consisted of only Propertyes we would be done designing the data structure. Sadly, that isn't the case!","title":"Property access"},{"category":"section","location":"vnt/design/#Indexing-and-PartialArrays","page":"Design","text":"Indexing is much more complicated to handle than property access, for a number of reasons.\n\nLet's start by talking about the most obvious issue: only some parts of an array may be set. For example, the user may set x[1] and x[10] but not the ones in the middle. This is accomplished using a PartialArray, which is a wrapper around an AbstractArray that holds the data of interest, together with a mask saying which elements have been set.\n\nThe array type and size of the mask is always the same as the data array (this is done using Base.similar).\n\ninfo: Info\nCurrently this is not enforced in an inner constructor, because that sometimes leads to extra allocations. It would be nice to investigate if this invariant can be enforced.\n\nIf pa.mask[i] is true, then pa.data[i] has been set; otherwise, pa.data[i] may contain some value, but it is not valid to index into that part of the array.\n\nHere is an example:\n\nusing DynamicPPL.VarNamedTuples: PartialArray\n\ndata = randn(3)\nmask = similar(data, Bool)\nfill!(mask, false)\npa = PartialArray(data, mask)\n\nThe main way of interacting with a PartialArray is to use BangBang.setindex!!. This sets one or more elements of the PartialArray's data, and marks the corresponding elements in the mask as true.\n\nusing BangBang: setindex!!\n\nsetindex!!(pa, 12.0, 2)\npa.data, pa.mask\n\nWhen printed, pa shows only the elements that are set:\n\npa\n\nIt is invalid to index into the unset elements:\n\npa[1]\n\nbut you can access the set elements:\n\npa[2]\n\nIt follows from this, that because the PartialArray is actually backed by a regular Array, it is constructive. The call to getindex will return the values stored in pa.data, as long as all the elements in pa.mask are set. For example:\n\nsetindex!!(pa, 11.0, 1)\npa[1:2]\n\nYou can even get the entirety of the array, once you have set all three elements. In this case, the PartialArray is (morally) equivalent to a single Array.\n\nsetindex!!(pa, 13.0, 3)\npa[:]\n\nAll Index optics in VarNames correspond to PartialArrays in VarNamedTuples. For example, let's say we want to store the mappings @varname(x[1].a) => 1.0, and y.b[2,3] => 2.0. The corresponding VarNamedTuple would look like this:\n\n         VNT\n        /   \\y  \n      x/     \\    b\n      /     VNT------PA[[ _, _, _  ],\n     /                  [ _, _, 2.0]]\n    PA [VNT]\n         |\n        a|\n         |\n        1.0\n\nwhere _ indicates masked elements in a PartialArray. To demonstrate:\n\nusing DynamicPPL\n\nvnt = VarNamedTuple()\nvnt = setindex!!(vnt, 1.0, @varname(x[1].a))\nvnt = setindex!!(vnt, 2.0, @varname(y.b[2, 3]))\n\nvnt.data.x  # This is a PartialArray\n\nvnt.data.x.data[1]   # This is a VNT\n\nvnt.data.x.data[1].data.a  # This is 1.0\n\nvnt.data.y  # This is a VNT\n\nvnt.data.y.data.b  # This is a PartialArray\n\nvnt.data.y.data.b.data[2, 3]  # This is 2.0\n\nThis illustrates the fundamental structure of VarNamedTuples. From this example, one can see that getting data from a VarNamedTuple is as type-stable as possible:\n\nAll property accesses are NamedTuple accesses, which are type-stable.\nAll indexing is done into PartialArrays: as long as indexing into the underlying data is type-stable (i.e., the element type of the data array is concrete), indexing into the PartialArray is type-stable as well.\n\ninfo: Info\nIn fact, the underlying data need not all have the same (concrete) type: all that is needed is that the unmasked elements have the same (concrete) type. The function _concretise_eltype!! is an attempt to force this to be the case: if the element type is abstract, but all the set elements have the same concrete type, the entire data array's element type will be changed to that concrete type (with junk in the unset elements).\n\nOne immediate question here is: how do we know what kind of array the PartialArray should use for its data and mask?","title":"Indexing and PartialArrays"},{"category":"section","location":"vnt/design/#GrowableArrays","page":"Design","text":"It's not obvious in the code above, but in the example above, we are implicitly making an assumption based on the indices that we see in the VarNames. For example, for @varname(x[1].a), based on the index 1 we assume that x should be a vector with a length of at least 1. Similarly, for @varname(y.b[2,3]), we assume that y.b should be a matrix with at least 2 rows and 3 columns.\n\nWe can inspect this by looking into the PartialArrays:\n\nvnt.data.x.data\n\nvnt.data.y.data.b.data\n\nSo, these PartialArrays are backed by something called GrowableArray. A GrowableArray is an array type, defined in DynamicPPL, that can grow in size as needed when setindex!! is called with indices outside of its current bounds (with other arrays that would error). The reason for such an array type is that you may want to do something like\n\nbegin\n    local vnt = VarNamedTuple()\n    for i in 1:5\n        vnt = setindex!!(vnt, i, @varname(x[i]))\n    end\n    vnt\nend\n\nand we don't have the ability to know in advance that x will eventually need to be at least of size 5. So, every call to setindex!! here will cause the underlying GrowableArray to grow in size as needed.\n\nThe problem with this is that it makes a huge number of implicit assumptions about what kind of array x actually is, and consequently it forbids a huge number of indexing operations in Julia.\n\nThese include, for example, linear indexing. In this example, the first call will create a GrowableArray with one dimension (i.e., a vector); and the second call will fail since we can't index a vector with two indices.\n\nvnt = setindex!!(VarNamedTuple(), 10.0, @varname(x[1]))\nvnt = setindex!!(vnt, 20.0, @varname(x[2, 2]))\n\nColons also don't work.\n\nvnt = setindex!!(VarNamedTuple(), randn(2), @varname(x[:]))\n\nOther things like OffsetArrays don't work (because x[11] would create a length-11 GrowableArray, which might not be appropriate; and x[-1] will just straight-up error). DimArrays will also fail if you use an index that isn't an integer.\n\nFinally, we don't know how large the final size of the array should be. If you try to access the entire array after setting some elements, it will work, but it will warn you:\n\nvnt = setindex!!(VarNamedTuple(), 10.0, @varname(x[1]))\nvnt[@varname(x)]","title":"GrowableArrays"},{"category":"section","location":"vnt/design/#Templated-arrays","page":"Design","text":"The general solution to this problem is for the user to provide a template for the array x in advance, so that we know what kind of array to create when we see @varname(x[...]).\n\nAt a low level, this is done using the DynamicPPL.templated_setindex!! function, which takes an extra argument that specifies the shape of the top-level symbol in the VarName.\n\nFor example, the linear-indexing example above now works if you tell the function that x is a 2-by-2 matrix.\n\nusing DynamicPPL: templated_setindex!!\n\nx = zeros(2, 2)\nvnt = VarNamedTuple()\nvnt = templated_setindex!!(VarNamedTuple(), 10.0, @varname(x[1]), x)\nvnt = setindex!!(vnt, 20.0, @varname(x[2, 2]))\n\n(The second call can also be templated_setindex!!, but it isn't necessary since the first call already establishes the shape of x, and indeed in such a case the template will be ignored.) Notice that the PartialArray is now backed by the correct Array:\n\nvnt.data.x.data\n\nIt is no longer growable, so if you try to set an out-of-bounds index, it will error:\n\nvnt = setindex!!(vnt, 30.0, @varname(x[3, 1]))\n\nThis mechanism makes it far more flexible to work with arrays in DynamicPPL models. Fundamentally, this resolves the inconsistency between indexing semantics in the model, and indexing semantics inside the VarNamedTuple.\n\nFor example, you can use DimArrays:\n\nimport DimensionalData as DD\n\nx = DD.DimArray(zeros(2, 3), (DD.X, DD.Y))\n\nvnt = VarNamedTuple()\nvnt = templated_setindex!!(vnt, 1.0, @varname(x[DD.X(1), DD.Y(2)]), x)\n\nvnt.data.x.data\n\nYou can access the data back again in any way you like, for example using linear indexing here:\n\ngetindex(vnt, @varname(x[3]))\n\ninfo: Info\nNote that support for such arrays is contingent on the provider of the array type, as well as BangBang.jl. There may be bugs that prevent some array types from fully working correctly. For example, BangBang.setindex!! does not accept keyword arguments, which precludes the use of keyword indices in DimArrays. However, DynamicPPL itself does not inherently prevent you from using such arrays. We would definitely like to fix upstream issues like these, but we don't always have the time to do so: help is very greatly appreciated!","title":"Templated arrays"},{"category":"section","location":"vnt/design/#How-do-we-provide-the-template?","page":"Design","text":"At this point, it would seem like a major faff for users to have to provide templates any time they wanted to use a VarNamedTuple. The good news is, within a DynamicPPL model, the template always exists. For example, consider:\n\nusing Distributions\n@model function bad_index()\n    return x[1] ~ Normal()\nend\nnothing # hide\n\nIf you were to attempt to execute this model, even without any interaction with VarNamedTuples, this would error, because there is no array x to set the first element in. It would be like writing a function that does x[1] = 10 without ever defining y:\n\nfunction bad_index_not_model()\n    return x[1] = 10\nend\nbad_index_not_model()\n\nThe model can only work if x is already provided, e.g. as an argument or a variable inside the model:\n\n@model function ok_index1(x::AbstractArray)\n    return x[1] ~ Normal()\nend\n\n@model function ok_index2()\n    x = Vector{Float64}(undef, 10)\n    return x[1] ~ Normal()\nend\nnothing # hide\n\nIn both cases, we do have access to the template for x. Thus, this is just a matter of plumbing this information through to DynamicPPL.tilde_assume!! so that it can be used when setting values in the VarNamedTuple. In the macro output below, you can see that x is passed as one of the arguments to tilde_assume!!.\n\n@macroexpand @model function bad_index()\n    return x[1] ~ Normal()\nend\n\nWhat this means is that in the core use case of VarNamedTuple (i.e., for storing random variables in DynamicPPL models), templates will always be provided. There are, for the most part, only two places where templates are unavailable, and we have to fall back on the GrowableArray approach:\n\nLoading data from chains.\nProviding conditioned or fixed values.\n\nThe first of these can be fixed by rerunning the model once to pick up the templates. The second cannot be truly fixed, but the DynamicPPL.@vnt macro allows users to manually provide templates themselves in cases where they are really needed.\n\n","title":"How do we provide the template?"},{"category":"section","location":"accs/overview/#accumulators-overview","page":"Overview","text":"Accumulators are objects in DynamicPPL which collect the results of computations in each tilde-statement.\n\nConsider a tilde-statement x ~ Beta(2, 2). There are several things going on in here (we will discuss this in full detail in the Model evaluation page). Loosely speaking:\n\nWe need to get a value for x that is consistent with the distribution Beta(2, 2);\nThat value may or may not be transformed to linked space;\nWe need to calculate the log-density of that value.\n\nOn top of that, we may want to store other information, such as:\n\nThe VarName itself, i.e. @varname(x);\nThe distribution;\n\nand so on.\n\nThese pieces of information are collected by accumulators. Each different accumulator is responsible for a different piece of information: thus, for example, log-likelihoods and log-priors are collected in separate accumulators. By choosing the right set of accumulators, we can control what information is collected during model evaluation. This allows us to perform exactly the necessary amount of computation we need to do.\n\nFurthermore, accumulators are completely independent: inside the accumulate_...!! method of an accumulator, it is not possible to access the state of other accumulators. This creates a modular design, where new accumulators can be defined and added without worrying about potential interactions with existing ones.","title":"Overview"},{"category":"section","location":"accs/overview/#The-AbstractAccumulator-API","page":"Overview","text":"Accumulators must subtype DynamicPPL.AbstractAccumulator, whose docstring explains the required interface:\n\nThe reason each accumulator must have a different name is because (for type stability reasons) accumulators are stored in a NamedTuple, and Julia requires that the names of fields in a NamedTuple be unique.\n\nreset is called when starting a new model evaluation, to ensure that previously accumulated results do not affect the current evaluation.\n\nThe central two methods though are accumulate_assume!! and accumulate_observe!!. These two methods are called whenever a tilde-statement is encountered during model evaluation. As can be seen from their signatures, they receive all the information that we have discussed above about tilde-statements.\n\nFormally, accumulators can be seen as a monoidal structure where:\n\nreset specifies the identity (indeed for some accumulators in DynamicPPL we define a function called zero);\naccumulate_assume!! and accumulate_observe!! collectively specify the binary operation;\nthe accumulated result is a fold over all tilde-statements in the model.\n\nNote that there is no guarantee that the binary operation be commutative (although in practice for many accumulators it is).","title":"The AbstractAccumulator API"},{"category":"section","location":"accs/overview/#An-example","page":"Overview","text":"As an example, let's consider an accumulator that associates VarNames with their log-densities. We will store these in an OrderedDict{VarName,Tuple{Bool,Float64}} where the Bool indicates whether the variable was observed (true) or assumed (false), and the Float64 is the log-density.\n\nnote: Note\nThis is very similar to the pointwise_logdensities functionality in DynamicPPL.\n\nWe define the accumulator as follows. As one may have guessed from the above, much of the definition is really just boilerplate to make things run smoothly. The interesting behaviour is in the two accumulate_...!! methods, where we compute the log-density and store it in the OrderedDict.\n\nusing DynamicPPL, OrderedCollections, Distributions\n\nstruct VarNameLogpAccumulator <: DynamicPPL.AbstractAccumulator\n    logps::OrderedDict{VarName,Tuple{Bool,Float64}}\n\n    VarNameLogpAccumulator() = new(OrderedDict{VarName,Tuple{Bool,Float64}}())\nend\n\n# Note, it's a good idea to define the symbol as a top-level `const`, since it's often\n# necessary to refer to it elsewhere. We'll see this happen later.\nconst VARNAMELOGP_NAME = :VarNameLogp\nDynamicPPL.accumulator_name(::VarNameLogpAccumulator) = VARNAMELOGP_NAME\nDynamicPPL.reset(acc::VarNameLogpAccumulator) = VarNameLogpAccumulator()\nDynamicPPL.copy(acc::VarNameLogpAccumulator) = VarNameLogpAccumulator(copy(acc.logps))\n\nfunction DynamicPPL.accumulate_assume!!(\n    acc::VarNameLogpAccumulator,\n    val,\n    tval,\n    logjac,\n    vn::VarName,\n    dist::Distribution,\n    template,\n)\n    acc.logps[vn] = (false, logpdf(dist, val))\n    return acc\nend\n\nfunction DynamicPPL.accumulate_observe!!(\n    acc::VarNameLogpAccumulator, dist, val, vn, template\n)\n    acc.logps[vn] = (true, logpdf(dist, val))\n    return acc\nend\n\nIn this implementation, our accumulate_...!! methods actually mutate the accumulator in place. This is not mandatory; you can return a new accumulator if you prefer an immutable style.\n\nTo use this accumulator in a model evaluation, we need to wrap it in an OnlyAccsVarInfo.\n\nnote: OnlyAccsVarInfo\nAs described on the Model evaluation page, an OnlyAccsVarInfo is just a wrapper around a set of accumulators. Don't worry about the name; it's mostly a historical artifact.\n\nWe can either do this by creating it from scratch, or by modifying an existing one (see the next section for details). In this example, we'll create an OnlyAccsVarInfo from scratch using only our new accumulator. Once we've evaluated the model, we can access the accumulated log-densities by reading it back from the accumulator:\n\n@model function f(y)\n    x ~ Normal()\n    return y ~ Normal(x)\nend\nmodel = f(2.0)\n\naccs = DynamicPPL.OnlyAccsVarInfo(VarNameLogpAccumulator())\n_, accs = DynamicPPL.init!!(model, accs, InitFromParams((; x=1.0)), UnlinkAll())\n\n# This is why we used a const.\noutput_acc = DynamicPPL.getacc(accs, Val(VARNAMELOGP_NAME))\n\nSince we specified that x should be initialised to 1.0, we should have that\n\noutput_acc.logps[@varname(x)] == (false, logpdf(Normal(), 1.0))\n\nand\n\noutput_acc.logps[@varname(y)] == (true, logpdf(Normal(1.0), 2.0))\n\n(Notice that because here we used an OrderedDict, the accumulation process is not commutative.)","title":"An example"},{"category":"section","location":"accs/overview/#Working-with-OnlyAccsVarInfo","page":"Overview","text":"As shown above, users can choose exactly which accumulators to use during model evaluation in order to control what information is collected. This section explains how to specify which accumulators to use, and how to extract the information again afterwards.\n\nWhen creating an OnlyAccsVarInfo, if no accumulators are specified, there is a set of default accumulators that are used:\n\nOnlyAccsVarInfo()\n\nAs alluded to above, one can control this by passing the accumulators to the OnlyAccsVarInfo constructor:\n\naccs = OnlyAccsVarInfo(LogLikelihoodAccumulator())\n\nIf you then use this OnlyAccsVarInfo to evaluate a model, only the specified accumulators will be used.\n\n@model function demo_likelihood()\n    x ~ Normal()\n    return 1.0 ~ Normal(x)\nend\nmodel = demo_likelihood()\n_, new_accs = init!!(model, accs, InitFromPrior(), UnlinkAll())\nnew_accs\n\nInstead of creating an OnlyAccsVarInfo from scratch, one can also add accumulators to an existing OnlyAccsVarInfo using setacc!! and setaccs!!. Note that these functions have very similar names! setacc!! adds a single accumulator, while setaccs!! replaces the entire set of accumulators with a new set.\n\naccs = OnlyAccsVarInfo()\n# Add a new accumulator\naccs = setacc!!(accs, RawValueAccumulator(false))\n\nIn the case of setacc!!, the accumulator will be added to the existing set if it has a name (as defined by accumulator_name) that is not already present. If an accumulator with the same name is already present, it will be replaced!\n\nThis means that you have to be careful to not accidentally create a name clash with an existing accumulator. If you are defining your own accumulator, it is a good idea to define its name with a prefix that is unique to your package.\n\nHowever, it does also mean that you can customise the behaviour of existing accumulators. For example, if you intentionally define an accumulator with the same name as an existing one, you can use setacc!! to replace it. Turing.jl uses this mechanism in SMC inference to replace the default log-likelihood accumulator with one that triggers particle reweighting each time the likelihood is incremented (i.e., each time a new observation is made).\n\nsetaccs!! is simpler; it replaces whatever came before it. It takes a tuple of accumulators as input.\n\n# This will create a set of default accumulators.\naccs = OnlyAccsVarInfo()\n\n# Replace all accumulators with just a RawValueAccumulator\nnew_acc = RawValueAccumulator(false)\naccs = setaccs!!(accs, (new_acc,))\n\nOnce you have evaluated a model with your desired accumulators, you can extract the accumulated results using getacc.\n\n_, accs = init!!(model, accs, InitFromPrior(), UnlinkAll())\n\n# `getacc` returns the accumulator itself; `.values` accesses the actual values inside it.\nraw_values = getacc(accs, Val(accumulator_name(new_acc))).values\n\nThere are convenience functions getlogprior, getloglikelihood, getlogprior, and get_raw_values that extract the corresponding accumulators' wrapped values directly from the OnlyAccsVarInfo. Note that these functions will throw an error if the corresponding accumulator is not present in the OnlyAccsVarInfo.\n\nget_raw_values(accs)\n\nThe next page gives an overview of the existing accumulators in DynamicPPL; these provide common functionality which you can make use of, without having to write your own accumulators from scratch.\n\n","title":"Working with OnlyAccsVarInfo"},{"category":"type","location":"accs/overview/#DynamicPPL.AbstractAccumulator-accs-overview","page":"Overview","text":"AbstractAccumulator\n\nAn abstract type for accumulators.\n\nAn accumulator is an object that may change its value at every tilde_assume!! or tilde_observe!! call based on the random variable in question. The obvious examples of accumulators are the log prior and log likelihood. Other examples might be a variable that counts the number of observations in a trace, or a list of the names of random variables seen so far.\n\nAn accumulator type T <: AbstractAccumulator must implement the following methods:\n\naccumulator_name(acc::T) or accumulator_name(::Type{T})\naccumulate_observe!!(acc::T, dist, val, vn, template)\naccumulate_assume!!(acc::T, val, tval, logjac, vn, dist, template)\nreset(acc::T)\nBase.copy(acc::T)\n\nIn these functions:\n\nval is the new value of the random variable sampled from a distribution (always in the original unlinked space), or the value on the left-hand side of an observe statement.\ntval is the original TransformedValue that was obtained from the initialisation strategy. This is passed through unchanged to accumulate_assume!! since it can be reused for some accumulators (e.g. when storing linked values, if the linked value was already provided, it is faster to reuse it than to re-link val).\ndist is the distribution on the RHS of the tilde statement.\nvn is the VarName that is on the left-hand side of the tilde-statement. If the tilde-statement is a literal observation like 0.0 ~ Normal(), then vn is nothing.\nlogjac is the log determinant of the Jacobian of the link transformation, if the variable is stored as a linked value in the VarInfo. If the variable is stored in its original, unlinked form, then logjac is zero.\ntemplate is a value that conveys the shape of the top-level symbol in vn, and is used specifically for accumulators that carry VarNamedTuples.\n\nTo be able to work with multi-threading, it should also implement:\n\nsplit(acc::T)\ncombine(acc::T, acc2::T)\n\nSee the documentation for each of these functions for more details.\n\n\n\n\n\n","title":"DynamicPPL.AbstractAccumulator"},{"category":"section","location":"#DynamicPPL.jl","page":"Home","text":"A domain-specific language and backend for probabilistic programming languages, used by Turing.jl.\n\n","title":"DynamicPPL.jl"},{"category":"section","location":"evaluation/#Model-evaluation","page":"Model evaluation","text":"Once you have defined a DynamicPPL model, let's say,\n\nusing DynamicPPL, Distributions\n\n@model function f()\n    x ~ Normal()\n    y ~ Beta(2, 2)\n    return (x=x, y=y)\nend\n\nmodel = f();\n\nyou will want to be able to evaluate it in some way.\n\nMuch like how a typical Julia function specifies some computation that involves variables and operations, the model definition defines a generative process, its random variables, and the relationships between them. However, it still leaves open many questions. For example,\n\nwhat values of x and y should be used?\nshould those values be somehow transformed, e.g., do we want to constrain y to be in its original interval (0, 1), or do we want to treat it as an unconstrained variable in ℝ (which possibly requires a Jacobian term to correct for the probability density)?\nwhat information do we want to know about the model? Do we want to know the values of x and y, the log-probability of the model, ...?\n\nDynamicPPL offers a powerful and modular evaluation framework which lets you control each of these aspects individually.\n\nThe following table offers a high-level summary of each of these different parts. Each of these are described in more detail on the linked pages; this page shows some examples of how they can be composed.\n\nConcept Subtype Purpose\nInitialisation strategy AbstractInitStrategy Specifies how parameter values are generated\nTransform strategy AbstractTransformStrategy Specifies how parameter values are transformed and how the log-Jacobian is computed\nAccumulators AbstractAccumulator Specifies how the outputs of the model are aggregated\n\nTo evaluate a model with these three components, you can use the method DynamicPPL.init!!:\n\nretval, accs = DynamicPPL.init!!(\n    [rng::Random.AbstractRNG]model::DynamicPPL.Model,\n    accs::DynamicPPL.OnlyAccsVarInfo,\n    init_strategy::DynamicPPL.AbstractInitStrategy,\n    transform_strategy::DynamicPPL.AbstractTransformStrategy,\n)\n\nwhich returns a tuple of the model's return value (the NamedTuple (x=x, y=y) in the example above) and the accumulators after evaluation.\n\nnote: OnlyAccsVarInfo\nOnlyAccsVarInfo is a thin wrapper around a set of accumulators. You can construct it using OnlyAccsVarInfo(acc1, acc2, ...), where acc1, acc2, ... are the accumulators that you want to use during evaluation.The main reason why OnlyAccsVarInfo exists is that it acts as a bridge between older code that expects a VarInfo and the new evaluation framework that is described above. OnlyAccsVarInfo contains only accumulators (as its name suggests), but implements a subset of the AbstractVarInfo interface, which allows it to be used in places where a VarInfo is expected.In the future it is likely that this will be removed, and you can directly pass the tuple of accumulators itself without having to wrap it.\n\nnote: What's happening to VarInfo?\nIf you have been using DynamicPPL in the past, you may be familiar with the general idea of VarInfo. (If that sounds completely foreign to you, you can ignore this!)While this still exists, we strongly encourage you to not work with it directly. The reason for this is because VarInfo conflates all three of the concepts described above into a single stateful object, which makes evaluation difficult to control and to reason about. In the long term we would like to remove VarInfo entirely.If you are using DynamicPPL internals and are unsure how to adapt your old code that uses VarInfo to the new evaluation framework, please check out the Migration guide, or open an issue on DynamicPPL. We are happy to help!","title":"Model evaluation"},{"category":"section","location":"evaluation/#Accumulators","page":"Model evaluation","text":"We will talk about accumulators first, since we will need to use them to demonstrate the other concepts.\n\nAccumulators are used to collect information during the evaluation of a model. Each accumulator has a different function: there is a LogPriorAccumulator for accumulating the log-probability of the prior, a LogLikelihoodAccumulator for accumulating the log-probability of the likelihood, a RawValueAccumulator for collecting raw (i.e. untransformed) parameter values, and so on.\n\nThe beauty of accumulators is that they are completely separate from one another; that means that you can mix and match them as needed, and avoid computing any information that you don't need. For example, if you don't need to know the likelihood, you can drop the LogLikelihoodAccumulator, which will avoid unnecessary calls to logpdf(dist, x) for any observed x.\n\nYou can specify which accumulators you want to use by passing them as arguments to OnlyAccsVarInfo. If no arguments are passed, a set of default accumulators (log-prior, log-likelihood, and log-Jacobian) are used.\n\n# Here, we set up an `OnlyAccsVarInfo` that only contains one accumulator.\naccs = OnlyAccsVarInfo(LogPriorAccumulator())\n\n# When calling init!!, we need to specify all three components. For now, just\n# focus on the accumulators, and we'll talk about the other two components later.\ninit_strategy = InitFromPrior()\ntransform_strategy = UnlinkAll()\n\nretval, accs = DynamicPPL.init!!(model, accs, init_strategy, transform_strategy)\naccs\n\nThere are a number of functions that you can call on an OnlyAccsVarInfo to extract the information. The most low-level one is getacc, which given an accumulator name (a Symbol) returns a specific accumulator; see the accumulator docs for more details on this function.\n\ngetacc(accs, Val(:LogPrior)).logp\n\nIt is often more convenient though to work with higher-level functions which directly extract the information that you need. For example, getlogprior will extract the log-prior from the LogPriorAccumulator (if one exists):\n\ngetlogprior(accs)\n\nThe page on existing accumulators describes the ones that are provided in DynamicPPL. Many of these will come with higher-level convenience functions: currently we define (and export) getlogprior, getloglikelihood, getlogjac, getlogjoint, getlogprior_internal, getlogjoint_internal, get_raw_values, and get_vector_values.\n\nDynamicPPL also allows you to add your own custom accumulators, which can be used to extract (or process) information obtained during model evaluation. This often means that you can avoid running the model multiple times just to extract different pieces of information.","title":"Accumulators"},{"category":"section","location":"evaluation/#Initialisation-strategies","page":"Model evaluation","text":"When evaluating a model, we need to assign values to the random variables in the model. An initialisation strategy specifies how these values are generated.\n\nAs a very simple example, let's say we want to generate values for x and y by sampling from the prior. DynamicPPL provides InitFromPrior() for this purpose:\n\naccs = OnlyAccsVarInfo()\ninit_strategy = InitFromPrior()\ntransform_strategy = UnlinkAll()\n\nretval, accs = DynamicPPL.init!!(model, accs, init_strategy, transform_strategy)\nretval\n\nIn the return value, we see that both x and y have been drawn from the prior. This is an inherently random process; if you run the above code multiple times, you will get different values for x and y each time. Initialisation strategies that involve randomness can be controlled by passing an rng object as the first argument to DynamicPPL.init!!:\n\nusing Random\n\nretval1 = first(init!!(Xoshiro(468), model, accs, init_strategy, transform_strategy))\nretval2 = first(init!!(Xoshiro(468), model, accs, init_strategy, transform_strategy))\nretval1 == retval2\n\nApart from InitFromPrior(), the main initialisation strategy that you are likely to use is InitFromParams(), where you can manually specify the values of the parameters that you are interested in.\n\n# See the VarNamedTuple docs for examples.\nparams = @vnt begin\n    x := 1.0\n    y := 0.5\nend\n\ninit_strategy = InitFromParams(params)\nretval, accs = DynamicPPL.init!!(model, accs, init_strategy, transform_strategy)\n\nretval\n\nHow do we know that the values of x and y that we specified in params are actually being used? We can determine this by inspecting the data inside the accumulators. Because both x and y are random variables (i.e., not conditioned data), their log-probabilities fall under the prior. (Note that specifying InitFromParams is not the same as conditioning the model on those values!)\n\ngetlogprior(accs)\n\nWe can compare this to what we would get if we were to manually evaluate the log-probability:\n\nlogpdf(Normal(), 1.0) + logpdf(Beta(2, 2), 0.5)","title":"Initialisation strategies"},{"category":"section","location":"evaluation/#Transform-strategies","page":"Model evaluation","text":"Let's finally turn our attention to the transform strategy argument. In the example above, we used UnlinkAll(), which means that the model is to be evaluated in 'unlinked' space: in DynamicPPL this refers to the original space of the parameters, without any transformations.\n\nOften it is necessary to evaluate the model in a different space. For example, we might be using an optimisation algorithm to find the maximum likelihood estimate. In such cases it is often more convenient to work in unconstrained Euclidean space, where we pass in a value transformed_y which can be any real number, and the actual value of y in the model is obtained by raw_y = logistic(transformed_y), which maps real numbers to the interval (0, 1).\n\nusing StatsFuns: logistic, logit\n\ntransformed_y = 3.0\nraw_y = logistic(transformed_y)\n\nThe use of transformations also means that we need to be careful about computing log-probabilities, because the probability associated with transformed_y is not equivalent to\n\nlogpdf(Beta(2, 2), raw_y)\n\nbut rather\n\nusing ChangesOfVariables: with_logabsdet_jacobian\n\nlogpdf(Beta(2, 2), raw_y) + last(with_logabsdet_jacobian(logistic, transformed_y))\n\nwhere the Jacobian term accounts for the change of variables. (If you aren't familiar with this concept, the main Turing docs have an introduction on it.)\n\nThe transform strategy allows you to specify which variables are to be transformed to Euclidean space, which in turn determines whether the Jacobian term is accumulated or not.\n\nImportantly, the transform strategy is separate from the initialisation strategy: this means that the initialisation strategy can provide values in untransformed space, and the transform strategy can 'reinterpret' them as being in transformed space, and then apply the necessary transformations and Jacobian corrections.\n\nFor example:\n\nparams = @vnt begin\n    # These are always in untransformed space.\n    x := 1.0\n    y := 0.5\nend\ninit_strategy = InitFromParams(params)\n\n# This transform strategy specifies that all variables should be linked.\ntransform_strategy = LinkAll()\n\n_, accs = DynamicPPL.init!!(model, accs, init_strategy, transform_strategy)\naccs\n\nWe see that the prior term is unchanged from the UnlinkAll() evaluation before. However, in constrast, the LogJacobianAccumulator is no longer empty; it contains the log-Jacobian term for the forward transform (to unconstrained space). Since x is already unconstrained, this term is zero for x, but for y it is non-zero, and it is equal to\n\n# `logit` is the *forward* transform from (0, 1) to ℝ.\nlast(with_logabsdet_jacobian(logit, 0.5))\n\nThat means that the log-probability in the transformed space is given by\n\ngetlogprior(accs) - getlogjac(accs)\n\nYou might ask: given that we specified parameters in untransformed space, how do we then retrieve the parameters in transformed space? The answer to this is to use an accumulator (no surprises there!) that collects the transformed values. Specifically, a VectorValueAccumulator collects vectorised forms of the parameters: that is, TransformedValue{V,T} where V<:AbstractVector.\n\naccs = OnlyAccsVarInfo(VectorValueAccumulator())\n_, accs = DynamicPPL.init!!(model, accs, init_strategy, transform_strategy)\naccs\n\nOf course, in an actual application you should probably use all the accumulators at the same time so that you only run the model once.\n\nIf you need to extract a concatenated vector of parameters from this, e.g. to pass to an optimisation algorithm, you can use\n\nget_vector_values(accs)\n\nIf you are thinking of doing something like this, you probably also want to use LogDensityFunction instead, and should skip ahead to that page.","title":"Transform strategies"},{"category":"section","location":"evaluation/#Further-reading","page":"Model evaluation","text":"The rest of the DynamicPPL documentation goes into these three components in much more detail. We also show you there how you can create your own custom initialisation strategies, transform strategies, and accumulators, so that you can extend the evaluation framework to suit your own needs.\n\n","title":"Further reading"}]
}
